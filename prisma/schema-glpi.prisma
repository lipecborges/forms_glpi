generator client {
  provider = "prisma-client-js"
  output   = "./generated/glpi"
  binaryTargets = ["native", "rhel-openssl-1.0.x", "windows"]
}

datasource db {
  provider = "mysql"
  url      = env("DEV_DB_GLPI_URL")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_agents {
  id                              Int       @id @default(autoincrement()) @db.UnsignedInt
  deviceid                        String    @unique(map: "deviceid") @db.VarChar(255)
  entities_id                     Int       @default(0) @db.UnsignedInt
  is_recursive                    Int       @default(0) @db.TinyInt
  name                            String?   @db.VarChar(255)
  agenttypes_id                   Int       @db.UnsignedInt
  last_contact                    DateTime? @db.Timestamp(0)
  version                         String?   @db.VarChar(255)
  locked                          Int       @default(0) @db.TinyInt
  itemtype                        String    @db.VarChar(100)
  items_id                        Int       @db.UnsignedInt
  useragent                       String?   @db.VarChar(255)
  tag                             String?   @db.VarChar(255)
  port                            String?   @db.VarChar(6)
  remote_addr                     String?   @db.VarChar(255)
  threads_networkdiscovery        Int       @default(1)
  threads_networkinventory        Int       @default(1)
  timeout_networkdiscovery        Int       @default(0)
  timeout_networkinventory        Int       @default(0)
  use_module_wake_on_lan          Int       @default(0) @db.TinyInt
  use_module_computer_inventory   Int       @default(0) @db.TinyInt
  use_module_esx_remote_inventory Int       @default(0) @db.TinyInt
  use_module_remote_inventory     Int       @default(0) @db.TinyInt
  use_module_network_inventory    Int       @default(0) @db.TinyInt
  use_module_network_discovery    Int       @default(0) @db.TinyInt
  use_module_package_deployment   Int       @default(0) @db.TinyInt
  use_module_collect_data         Int       @default(0) @db.TinyInt

  @@index([agenttypes_id], map: "agenttypes_id")
  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([itemtype, items_id], map: "item")
  @@index([name], map: "name")
}

model glpi_agenttypes {
  id   Int     @id @default(autoincrement()) @db.UnsignedInt
  name String? @db.VarChar(255)

  @@index([name], map: "name")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_alerts {
  id       Int      @id @default(autoincrement()) @db.UnsignedInt
  itemtype String   @db.VarChar(100)
  items_id Int      @default(0) @db.UnsignedInt
  type     Int      @default(0)
  date     DateTime @default(now()) @db.Timestamp(0)

  @@unique([itemtype, items_id, type], map: "unicity")
  @@index([date], map: "date")
  @@index([type], map: "type")
}

model glpi_apiclients {
  id               Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id      Int       @default(0) @db.UnsignedInt
  is_recursive     Int       @default(0) @db.TinyInt
  name             String?   @db.VarChar(255)
  date_mod         DateTime? @db.Timestamp(0)
  date_creation    DateTime? @db.Timestamp(0)
  is_active        Int       @default(0) @db.TinyInt
  ipv4_range_start BigInt?
  ipv4_range_end   BigInt?
  ipv6             String?   @db.VarChar(255)
  app_token        String?   @db.VarChar(255)
  app_token_date   DateTime? @db.Timestamp(0)
  dolog_method     Int       @default(0) @db.TinyInt
  comment          String?   @db.Text

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([is_active], map: "is_active")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
}

model glpi_applianceenvironments {
  id      Int     @id @default(autoincrement()) @db.UnsignedInt
  name    String? @db.VarChar(255)
  comment String? @db.Text

  @@index([name], map: "name")
}

model glpi_appliances {
  id                       Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id              Int       @default(0) @db.UnsignedInt
  is_recursive             Int       @default(0) @db.TinyInt
  name                     String    @default("") @db.VarChar(255)
  is_deleted               Int       @default(0) @db.TinyInt
  appliancetypes_id        Int       @default(0) @db.UnsignedInt
  comment                  String?   @db.Text
  locations_id             Int       @default(0) @db.UnsignedInt
  manufacturers_id         Int       @default(0) @db.UnsignedInt
  applianceenvironments_id Int       @default(0) @db.UnsignedInt
  users_id                 Int       @default(0) @db.UnsignedInt
  users_id_tech            Int       @default(0) @db.UnsignedInt
  groups_id                Int       @default(0) @db.UnsignedInt
  groups_id_tech           Int       @default(0) @db.UnsignedInt
  date_mod                 DateTime? @db.Timestamp(0)
  date_creation            DateTime? @db.Timestamp(0)
  states_id                Int       @default(0) @db.UnsignedInt
  externalidentifier       String?   @unique(map: "unicity") @db.VarChar(255)
  serial                   String?   @db.VarChar(255)
  otherserial              String?   @db.VarChar(255)
  contact                  String?   @db.VarChar(255)
  contact_num              String?   @db.VarChar(255)
  is_helpdesk_visible      Int       @default(1) @db.TinyInt
  pictures                 String?   @db.Text

  @@index([applianceenvironments_id], map: "applianceenvironments_id")
  @@index([appliancetypes_id], map: "appliancetypes_id")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([groups_id], map: "groups_id")
  @@index([groups_id_tech], map: "groups_id_tech")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_helpdesk_visible], map: "is_helpdesk_visible")
  @@index([is_recursive], map: "is_recursive")
  @@index([locations_id], map: "locations_id")
  @@index([manufacturers_id], map: "manufacturers_id")
  @@index([name], map: "name")
  @@index([otherserial], map: "otherserial")
  @@index([serial], map: "serial")
  @@index([states_id], map: "states_id")
  @@index([users_id], map: "users_id")
  @@index([users_id_tech], map: "users_id_tech")
}

model glpi_appliances_items {
  id            Int    @id @default(autoincrement()) @db.UnsignedInt
  appliances_id Int    @default(0) @db.UnsignedInt
  items_id      Int    @default(0) @db.UnsignedInt
  itemtype      String @default("") @db.VarChar(100)

  @@unique([appliances_id, items_id, itemtype], map: "unicity")
  @@index([itemtype, items_id], map: "item")
}

model glpi_appliances_items_relations {
  id                  Int    @id @default(autoincrement()) @db.UnsignedInt
  appliances_items_id Int    @default(0) @db.UnsignedInt
  itemtype            String @db.VarChar(100)
  items_id            Int    @default(0) @db.UnsignedInt

  @@index([appliances_items_id], map: "appliances_items_id")
  @@index([itemtype, items_id], map: "item")
}

model glpi_appliancetypes {
  id                 Int     @id @default(autoincrement()) @db.UnsignedInt
  entities_id        Int     @default(0) @db.UnsignedInt
  is_recursive       Int     @default(0) @db.TinyInt
  name               String  @default("") @db.VarChar(255)
  comment            String? @db.Text
  externalidentifier String? @unique(map: "externalidentifier") @db.VarChar(255)

  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
}

model glpi_authldapreplicates {
  id           Int     @id @default(autoincrement()) @db.UnsignedInt
  authldaps_id Int     @default(0) @db.UnsignedInt
  host         String? @db.VarChar(255)
  port         Int     @default(389)
  name         String? @db.VarChar(255)
  timeout      Int     @default(10)

  @@index([authldaps_id], map: "authldaps_id")
  @@index([name], map: "name")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_authldaps {
  id                        Int       @id @default(autoincrement()) @db.UnsignedInt
  name                      String?   @db.VarChar(255)
  host                      String?   @db.VarChar(255)
  basedn                    String?   @db.VarChar(255)
  rootdn                    String?   @db.VarChar(255)
  port                      Int       @default(389)
  condition                 String?   @db.Text
  login_field               String?   @default("uid") @db.VarChar(255)
  sync_field                String?   @db.VarChar(255)
  use_tls                   Int       @default(0) @db.TinyInt
  group_field               String?   @db.VarChar(255)
  group_condition           String?   @db.Text
  group_search_type         Int       @default(0)
  group_member_field        String?   @db.VarChar(255)
  email1_field              String?   @db.VarChar(255)
  realname_field            String?   @db.VarChar(255)
  firstname_field           String?   @db.VarChar(255)
  phone_field               String?   @db.VarChar(255)
  phone2_field              String?   @db.VarChar(255)
  mobile_field              String?   @db.VarChar(255)
  comment_field             String?   @db.VarChar(255)
  use_dn                    Int       @default(1) @db.TinyInt
  time_offset               Int       @default(0)
  deref_option              Int       @default(0)
  title_field               String?   @db.VarChar(255)
  category_field            String?   @db.VarChar(255)
  language_field            String?   @db.VarChar(255)
  entity_field              String?   @db.VarChar(255)
  entity_condition          String?   @db.Text
  date_mod                  DateTime? @db.Timestamp(0)
  comment                   String?   @db.Text
  is_default                Int       @default(0) @db.TinyInt
  is_active                 Int       @default(0) @db.TinyInt
  rootdn_passwd             String?   @db.VarChar(255)
  registration_number_field String?   @db.VarChar(255)
  email2_field              String?   @db.VarChar(255)
  email3_field              String?   @db.VarChar(255)
  email4_field              String?   @db.VarChar(255)
  location_field            String?   @db.VarChar(255)
  responsible_field         String?   @db.VarChar(255)
  pagesize                  Int       @default(0)
  ldap_maxlimit             Int       @default(0)
  can_support_pagesize      Int       @default(0) @db.TinyInt
  picture_field             String?   @db.VarChar(255)
  date_creation             DateTime? @db.Timestamp(0)
  inventory_domain          String?   @db.VarChar(255)
  tls_certfile              String?   @db.Text
  tls_keyfile               String?   @db.Text
  use_bind                  Int       @default(1) @db.TinyInt
  timeout                   Int       @default(10)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([is_active], map: "is_active")
  @@index([is_default], map: "is_default")
  @@index([name], map: "name")
  @@index([sync_field], map: "sync_field")
}

model glpi_authmails {
  id             Int       @id @default(autoincrement()) @db.UnsignedInt
  name           String?   @db.VarChar(255)
  connect_string String?   @db.VarChar(255)
  host           String?   @db.VarChar(255)
  date_mod       DateTime? @db.Timestamp(0)
  date_creation  DateTime? @db.Timestamp(0)
  comment        String?   @db.Text
  is_active      Int       @default(0) @db.TinyInt

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([is_active], map: "is_active")
  @@index([name], map: "name")
}

model glpi_autoupdatesystems {
  id      Int     @id @default(autoincrement()) @db.UnsignedInt
  name    String? @db.VarChar(255)
  comment String? @db.Text

  @@index([name], map: "name")
}

model glpi_blacklistedmailcontents {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  content       String?   @db.Text
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_blacklists {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  type          Int       @default(0)
  name          String?   @db.VarChar(255)
  value         String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
  @@index([type], map: "type")
}

model glpi_budgets {
  id             Int       @id @default(autoincrement()) @db.UnsignedInt
  name           String?   @db.VarChar(255)
  entities_id    Int       @default(0) @db.UnsignedInt
  is_recursive   Int       @default(0) @db.TinyInt
  comment        String?   @db.Text
  is_deleted     Int       @default(0) @db.TinyInt
  begin_date     DateTime? @db.Date
  end_date       DateTime? @db.Date
  value          Decimal   @default(0.0000) @db.Decimal(20, 4)
  is_template    Int       @default(0) @db.TinyInt
  template_name  String?   @db.VarChar(255)
  date_mod       DateTime? @db.Timestamp(0)
  date_creation  DateTime? @db.Timestamp(0)
  locations_id   Int       @default(0) @db.UnsignedInt
  budgettypes_id Int       @default(0) @db.UnsignedInt

  @@index([begin_date], map: "begin_date")
  @@index([budgettypes_id], map: "budgettypes_id")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([end_date], map: "end_date")
  @@index([entities_id], map: "entities_id")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_recursive], map: "is_recursive")
  @@index([is_template], map: "is_template")
  @@index([locations_id], map: "locations_id")
  @@index([name], map: "name")
}

model glpi_budgettypes {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_businesscriticities {
  id                     Int       @id @default(autoincrement()) @db.UnsignedInt
  name                   String?   @db.VarChar(255)
  entities_id            Int       @default(0) @db.UnsignedInt
  is_recursive           Int       @default(0) @db.TinyInt
  comment                String?   @db.Text
  date_mod               DateTime? @db.Timestamp(0)
  date_creation          DateTime? @db.Timestamp(0)
  businesscriticities_id Int       @default(0) @db.UnsignedInt
  completename           String?   @db.Text
  level                  Int       @default(0)
  ancestors_cache        String?   @db.LongText
  sons_cache             String?   @db.LongText

  @@unique([businesscriticities_id, name], map: "unicity")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([level], map: "level")
  @@index([name], map: "name")
}

model glpi_cables {
  id                         Int       @id @default(autoincrement()) @db.UnsignedInt
  name                       String?   @db.VarChar(255)
  entities_id                Int       @default(0) @db.UnsignedInt
  is_recursive               Int       @default(0) @db.TinyInt
  itemtype_endpoint_a        String?   @db.VarChar(255)
  itemtype_endpoint_b        String?   @db.VarChar(255)
  items_id_endpoint_a        Int       @default(0) @db.UnsignedInt
  items_id_endpoint_b        Int       @default(0) @db.UnsignedInt
  socketmodels_id_endpoint_a Int       @default(0) @db.UnsignedInt
  socketmodels_id_endpoint_b Int       @default(0) @db.UnsignedInt
  sockets_id_endpoint_a      Int       @default(0) @db.UnsignedInt
  sockets_id_endpoint_b      Int       @default(0) @db.UnsignedInt
  cablestrands_id            Int       @default(0) @db.UnsignedInt
  color                      String?   @db.VarChar(255)
  otherserial                String?   @db.VarChar(255)
  states_id                  Int       @default(0) @db.UnsignedInt
  users_id_tech              Int       @default(0) @db.UnsignedInt
  cabletypes_id              Int       @default(0) @db.UnsignedInt
  comment                    String?   @db.Text
  date_mod                   DateTime? @db.Timestamp(0)
  date_creation              DateTime? @db.Timestamp(0)
  is_deleted                 Int       @default(0) @db.TinyInt

  @@index([cablestrands_id], map: "cablestrands_id")
  @@index([cabletypes_id], map: "cabletypes_id")
  @@index([entities_id, name], map: "complete")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_recursive], map: "is_recursive")
  @@index([itemtype_endpoint_a, items_id_endpoint_a], map: "item_endpoint_a")
  @@index([itemtype_endpoint_b, items_id_endpoint_b], map: "item_endpoint_b")
  @@index([items_id_endpoint_a], map: "items_id_endpoint_a")
  @@index([items_id_endpoint_b], map: "items_id_endpoint_b")
  @@index([name], map: "name")
  @@index([socketmodels_id_endpoint_a], map: "socketmodels_id_endpoint_a")
  @@index([socketmodels_id_endpoint_b], map: "socketmodels_id_endpoint_b")
  @@index([sockets_id_endpoint_a], map: "sockets_id_endpoint_a")
  @@index([sockets_id_endpoint_b], map: "sockets_id_endpoint_b")
  @@index([states_id], map: "states_id")
  @@index([users_id_tech], map: "users_id_tech")
}

model glpi_cablestrands {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_cabletypes {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_calendars {
  id             Int       @id @default(autoincrement()) @db.UnsignedInt
  name           String?   @db.VarChar(255)
  entities_id    Int       @default(0) @db.UnsignedInt
  is_recursive   Int       @default(0) @db.TinyInt
  comment        String?   @db.Text
  date_mod       DateTime? @db.Timestamp(0)
  cache_duration String?   @db.Text
  date_creation  DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
}

model glpi_calendars_holidays {
  id           Int @id @default(autoincrement()) @db.UnsignedInt
  calendars_id Int @default(0) @db.UnsignedInt
  holidays_id  Int @default(0) @db.UnsignedInt

  @@unique([calendars_id, holidays_id], map: "unicity")
  @@index([holidays_id], map: "holidays_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_calendarsegments {
  id           Int       @id @default(autoincrement()) @db.UnsignedInt
  calendars_id Int       @default(0) @db.UnsignedInt
  entities_id  Int       @default(0) @db.UnsignedInt
  is_recursive Int       @default(0) @db.TinyInt
  day          Int       @default(1) @db.TinyInt
  begin        DateTime? @db.Time(0)
  end          DateTime? @db.Time(0)

  @@index([calendars_id], map: "calendars_id")
  @@index([day], map: "day")
  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
}

model glpi_cartridgeitems {
  id                    Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id           Int       @default(0) @db.UnsignedInt
  is_recursive          Int       @default(0) @db.TinyInt
  name                  String?   @db.VarChar(255)
  ref                   String?   @db.VarChar(255)
  locations_id          Int       @default(0) @db.UnsignedInt
  cartridgeitemtypes_id Int       @default(0) @db.UnsignedInt
  manufacturers_id      Int       @default(0) @db.UnsignedInt
  users_id_tech         Int       @default(0) @db.UnsignedInt
  groups_id_tech        Int       @default(0) @db.UnsignedInt
  is_deleted            Int       @default(0) @db.TinyInt
  comment               String?   @db.Text
  alarm_threshold       Int       @default(10)
  stock_target          Int       @default(0)
  date_mod              DateTime? @db.Timestamp(0)
  date_creation         DateTime? @db.Timestamp(0)
  pictures              String?   @db.Text

  @@index([alarm_threshold], map: "alarm_threshold")
  @@index([cartridgeitemtypes_id], map: "cartridgeitemtypes_id")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([groups_id_tech], map: "groups_id_tech")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_recursive], map: "is_recursive")
  @@index([locations_id], map: "locations_id")
  @@index([manufacturers_id], map: "manufacturers_id")
  @@index([name], map: "name")
  @@index([users_id_tech], map: "users_id_tech")
}

model glpi_cartridgeitems_printermodels {
  id                Int @id @default(autoincrement()) @db.UnsignedInt
  cartridgeitems_id Int @default(0) @db.UnsignedInt
  printermodels_id  Int @default(0) @db.UnsignedInt

  @@unique([printermodels_id, cartridgeitems_id], map: "unicity")
  @@index([cartridgeitems_id], map: "cartridgeitems_id")
}

model glpi_cartridgeitemtypes {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_cartridges {
  id                Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id       Int       @default(0) @db.UnsignedInt
  cartridgeitems_id Int       @default(0) @db.UnsignedInt
  printers_id       Int       @default(0) @db.UnsignedInt
  date_in           DateTime? @db.Date
  date_use          DateTime? @db.Date
  date_out          DateTime? @db.Date
  pages             Int       @default(0)
  date_mod          DateTime? @db.Timestamp(0)
  date_creation     DateTime? @db.Timestamp(0)

  @@index([cartridgeitems_id], map: "cartridgeitems_id")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([printers_id], map: "printers_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_certificates {
  id                  Int       @id @default(autoincrement()) @db.UnsignedInt
  name                String?   @db.VarChar(255)
  serial              String?   @db.VarChar(255)
  otherserial         String?   @db.VarChar(255)
  entities_id         Int       @default(0) @db.UnsignedInt
  is_recursive        Int       @default(0) @db.TinyInt
  comment             String?   @db.Text
  is_deleted          Int       @default(0) @db.TinyInt
  is_template         Int       @default(0) @db.TinyInt
  template_name       String?   @db.VarChar(255)
  certificatetypes_id Int       @default(0) @db.UnsignedInt
  dns_name            String?   @db.VarChar(255)
  dns_suffix          String?   @db.VarChar(255)
  users_id_tech       Int       @default(0) @db.UnsignedInt
  groups_id_tech      Int       @default(0) @db.UnsignedInt
  locations_id        Int       @default(0) @db.UnsignedInt
  manufacturers_id    Int       @default(0) @db.UnsignedInt
  contact             String?   @db.VarChar(255)
  contact_num         String?   @db.VarChar(255)
  users_id            Int       @default(0) @db.UnsignedInt
  groups_id           Int       @default(0) @db.UnsignedInt
  is_autosign         Int       @default(0) @db.TinyInt
  date_expiration     DateTime? @db.Date
  states_id           Int       @default(0) @db.UnsignedInt
  command             String?   @db.Text
  certificate_request String?   @db.Text
  certificate_item    String?   @db.Text
  date_creation       DateTime? @db.Timestamp(0)
  date_mod            DateTime? @db.Timestamp(0)

  @@index([certificatetypes_id], map: "certificatetypes_id")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([groups_id], map: "groups_id")
  @@index([groups_id_tech], map: "groups_id_tech")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_recursive], map: "is_recursive")
  @@index([is_template], map: "is_template")
  @@index([locations_id], map: "locations_id")
  @@index([manufacturers_id], map: "manufacturers_id")
  @@index([name], map: "name")
  @@index([states_id], map: "states_id")
  @@index([users_id], map: "users_id")
  @@index([users_id_tech], map: "users_id_tech")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_certificates_items {
  id              Int       @id @default(autoincrement()) @db.UnsignedInt
  certificates_id Int       @default(0) @db.UnsignedInt
  items_id        Int       @default(0) @db.UnsignedInt
  itemtype        String    @db.VarChar(100)
  date_creation   DateTime? @db.Timestamp(0)
  date_mod        DateTime? @db.Timestamp(0)

  @@unique([certificates_id, itemtype, items_id], map: "unicity")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([itemtype, items_id], map: "item")
}

model glpi_certificatetypes {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id   Int       @default(0) @db.UnsignedInt
  is_recursive  Int       @default(0) @db.TinyInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_creation DateTime? @db.Timestamp(0)
  date_mod      DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
}

model glpi_changecosts {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  changes_id    Int       @default(0) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  begin_date    DateTime? @db.Date
  end_date      DateTime? @db.Date
  actiontime    Int       @default(0)
  cost_time     Decimal   @default(0.0000) @db.Decimal(20, 4)
  cost_fixed    Decimal   @default(0.0000) @db.Decimal(20, 4)
  cost_material Decimal   @default(0.0000) @db.Decimal(20, 4)
  budgets_id    Int       @default(0) @db.UnsignedInt
  entities_id   Int       @default(0) @db.UnsignedInt
  is_recursive  Int       @default(0) @db.TinyInt

  @@index([begin_date], map: "begin_date")
  @@index([budgets_id], map: "budgets_id")
  @@index([changes_id], map: "changes_id")
  @@index([end_date], map: "end_date")
  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
}

model glpi_changes {
  id                   Int       @id @default(autoincrement()) @db.UnsignedInt
  name                 String?   @db.VarChar(255)
  entities_id          Int       @default(0) @db.UnsignedInt
  is_recursive         Int       @default(0) @db.TinyInt
  is_deleted           Int       @default(0) @db.TinyInt
  status               Int       @default(1)
  content              String?   @db.LongText
  date_mod             DateTime? @db.Timestamp(0)
  date                 DateTime? @db.Timestamp(0)
  solvedate            DateTime? @db.Timestamp(0)
  closedate            DateTime? @db.Timestamp(0)
  time_to_resolve      DateTime? @db.Timestamp(0)
  users_id_recipient   Int       @default(0) @db.UnsignedInt
  users_id_lastupdater Int       @default(0) @db.UnsignedInt
  urgency              Int       @default(1)
  impact               Int       @default(1)
  priority             Int       @default(1)
  itilcategories_id    Int       @default(0) @db.UnsignedInt
  impactcontent        String?   @db.LongText
  controlistcontent    String?   @db.LongText
  rolloutplancontent   String?   @db.LongText
  backoutplancontent   String?   @db.LongText
  checklistcontent     String?   @db.LongText
  global_validation    Int       @default(1)
  validation_percent   Int       @default(0)
  actiontime           Int       @default(0)
  begin_waiting_date   DateTime? @db.Timestamp(0)
  waiting_duration     Int       @default(0)
  close_delay_stat     Int       @default(0)
  solve_delay_stat     Int       @default(0)
  date_creation        DateTime? @db.Timestamp(0)
  locations_id         Int       @default(0) @db.UnsignedInt

  @@index([closedate], map: "closedate")
  @@index([date], map: "date")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([global_validation], map: "global_validation")
  @@index([impact], map: "impact")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_recursive], map: "is_recursive")
  @@index([itilcategories_id], map: "itilcategories_id")
  @@index([locations_id], map: "locations_id")
  @@index([name], map: "name")
  @@index([priority], map: "priority")
  @@index([solvedate], map: "solvedate")
  @@index([status], map: "status")
  @@index([time_to_resolve], map: "time_to_resolve")
  @@index([urgency], map: "urgency")
  @@index([users_id_lastupdater], map: "users_id_lastupdater")
  @@index([users_id_recipient], map: "users_id_recipient")
}

model glpi_changes_groups {
  id         Int @id @default(autoincrement()) @db.UnsignedInt
  changes_id Int @default(0) @db.UnsignedInt
  groups_id  Int @default(0) @db.UnsignedInt
  type       Int @default(1)

  @@unique([changes_id, type, groups_id], map: "unicity")
  @@index([groups_id, type], map: "group")
}

model glpi_changes_items {
  id         Int     @id @default(autoincrement()) @db.UnsignedInt
  changes_id Int     @default(0) @db.UnsignedInt
  itemtype   String? @db.VarChar(100)
  items_id   Int     @default(0) @db.UnsignedInt

  @@unique([changes_id, itemtype, items_id], map: "unicity")
  @@index([itemtype, items_id], map: "item")
}

model glpi_changes_problems {
  id          Int @id @default(autoincrement()) @db.UnsignedInt
  changes_id  Int @default(0) @db.UnsignedInt
  problems_id Int @default(0) @db.UnsignedInt

  @@unique([changes_id, problems_id], map: "unicity")
  @@index([problems_id], map: "problems_id")
}

model glpi_changes_suppliers {
  id                Int     @id @default(autoincrement()) @db.UnsignedInt
  changes_id        Int     @default(0) @db.UnsignedInt
  suppliers_id      Int     @default(0) @db.UnsignedInt
  type              Int     @default(1)
  use_notification  Int     @default(0) @db.TinyInt
  alternative_email String? @db.VarChar(255)

  @@unique([changes_id, type, suppliers_id], map: "unicity")
  @@index([suppliers_id, type], map: "group")
}

model glpi_changes_tickets {
  id         Int @id @default(autoincrement()) @db.UnsignedInt
  changes_id Int @default(0) @db.UnsignedInt
  tickets_id Int @default(0) @db.UnsignedInt

  @@unique([changes_id, tickets_id], map: "unicity")
  @@index([tickets_id], map: "tickets_id")
}

model glpi_changes_users {
  id                Int     @id @default(autoincrement()) @db.UnsignedInt
  changes_id        Int     @default(0) @db.UnsignedInt
  users_id          Int     @default(0) @db.UnsignedInt
  type              Int     @default(1)
  use_notification  Int     @default(0) @db.TinyInt
  alternative_email String? @db.VarChar(255)

  @@unique([changes_id, type, users_id, alternative_email], map: "unicity")
  @@index([users_id, type], map: "user")
}

model glpi_changetasks {
  id                Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid              String?   @unique(map: "uuid") @db.VarChar(255)
  changes_id        Int       @default(0) @db.UnsignedInt
  taskcategories_id Int       @default(0) @db.UnsignedInt
  state             Int       @default(0)
  date              DateTime? @db.Timestamp(0)
  begin             DateTime? @db.Timestamp(0)
  end               DateTime? @db.Timestamp(0)
  users_id          Int       @default(0) @db.UnsignedInt
  users_id_editor   Int       @default(0) @db.UnsignedInt
  users_id_tech     Int       @default(0) @db.UnsignedInt
  groups_id_tech    Int       @default(0) @db.UnsignedInt
  content           String?   @db.LongText
  actiontime        Int       @default(0)
  date_mod          DateTime? @db.Timestamp(0)
  date_creation     DateTime? @db.Timestamp(0)
  tasktemplates_id  Int       @default(0) @db.UnsignedInt
  timeline_position Int       @default(0) @db.TinyInt
  is_private        Int       @default(0) @db.TinyInt

  @@index([begin], map: "begin")
  @@index([changes_id], map: "changes_id")
  @@index([date], map: "date")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([end], map: "end")
  @@index([groups_id_tech], map: "groups_id_tech")
  @@index([is_private], map: "is_private")
  @@index([state], map: "state")
  @@index([taskcategories_id], map: "taskcategories_id")
  @@index([tasktemplates_id], map: "tasktemplates_id")
  @@index([users_id], map: "users_id")
  @@index([users_id_editor], map: "users_id_editor")
  @@index([users_id_tech], map: "users_id_tech")
}

model glpi_changetemplatehiddenfields {
  id                 Int @id @default(autoincrement()) @db.UnsignedInt
  changetemplates_id Int @default(0) @db.UnsignedInt
  num                Int @default(0)

  @@unique([changetemplates_id, num], map: "unicity")
}

model glpi_changetemplatemandatoryfields {
  id                 Int @id @default(autoincrement()) @db.UnsignedInt
  changetemplates_id Int @default(0) @db.UnsignedInt
  num                Int @default(0)

  @@unique([changetemplates_id, num], map: "unicity")
}

model glpi_changetemplatepredefinedfields {
  id                 Int     @id @default(autoincrement()) @db.UnsignedInt
  changetemplates_id Int     @default(0) @db.UnsignedInt
  num                Int     @default(0)
  value              String? @db.Text

  @@index([changetemplates_id], map: "changetemplates_id")
}

model glpi_changetemplates {
  id           Int     @id @default(autoincrement()) @db.UnsignedInt
  name         String? @db.VarChar(255)
  entities_id  Int     @default(0) @db.UnsignedInt
  is_recursive Int     @default(0) @db.TinyInt
  comment      String? @db.Text

  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
}

model glpi_changevalidations {
  id                 Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id        Int       @default(0) @db.UnsignedInt
  is_recursive       Int       @default(0) @db.TinyInt
  users_id           Int       @default(0) @db.UnsignedInt
  changes_id         Int       @default(0) @db.UnsignedInt
  users_id_validate  Int       @default(0) @db.UnsignedInt
  comment_submission String?   @db.Text
  comment_validation String?   @db.Text
  status             Int       @default(2)
  submission_date    DateTime? @db.Timestamp(0)
  validation_date    DateTime? @db.Timestamp(0)
  timeline_position  Int       @default(0) @db.TinyInt

  @@index([changes_id], map: "changes_id")
  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([status], map: "status")
  @@index([submission_date], map: "submission_date")
  @@index([users_id], map: "users_id")
  @@index([users_id_validate], map: "users_id_validate")
  @@index([validation_date], map: "validation_date")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_clusters {
  id                   Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id          Int       @default(0) @db.UnsignedInt
  is_recursive         Int       @default(0) @db.TinyInt
  name                 String?   @db.VarChar(255)
  uuid                 String?   @db.VarChar(255)
  version              String?   @db.VarChar(255)
  users_id_tech        Int       @default(0) @db.UnsignedInt
  groups_id_tech       Int       @default(0) @db.UnsignedInt
  is_deleted           Int       @default(0) @db.TinyInt
  states_id            Int       @default(0) @db.UnsignedInt
  comment              String?   @db.Text
  clustertypes_id      Int       @default(0) @db.UnsignedInt
  autoupdatesystems_id Int       @default(0) @db.UnsignedInt
  date_mod             DateTime? @db.Timestamp(0)
  date_creation        DateTime? @db.Timestamp(0)

  @@index([autoupdatesystems_id], map: "autoupdatesystems_id")
  @@index([clustertypes_id], map: "clustertypes_id")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([groups_id_tech], map: "group_id_tech")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
  @@index([states_id], map: "states_id")
  @@index([users_id_tech], map: "users_id_tech")
}

model glpi_clustertypes {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id   Int       @default(0) @db.UnsignedInt
  is_recursive  Int       @default(0) @db.TinyInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_creation DateTime? @db.Timestamp(0)
  date_mod      DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
}

model glpi_computerantiviruses {
  id                Int       @id @default(autoincrement()) @db.UnsignedInt
  computers_id      Int       @default(0) @db.UnsignedInt
  name              String?   @db.VarChar(255)
  manufacturers_id  Int       @default(0) @db.UnsignedInt
  antivirus_version String?   @db.VarChar(255)
  signature_version String?   @db.VarChar(255)
  is_active         Int       @default(0) @db.TinyInt
  is_deleted        Int       @default(0) @db.TinyInt
  is_uptodate       Int       @default(0) @db.TinyInt
  is_dynamic        Int       @default(0) @db.TinyInt
  date_expiration   DateTime? @db.Timestamp(0)
  date_mod          DateTime? @db.Timestamp(0)
  date_creation     DateTime? @db.Timestamp(0)

  @@index([antivirus_version], map: "antivirus_version")
  @@index([computers_id], map: "computers_id")
  @@index([date_creation], map: "date_creation")
  @@index([date_expiration], map: "date_expiration")
  @@index([date_mod], map: "date_mod")
  @@index([is_active], map: "is_active")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_dynamic], map: "is_dynamic")
  @@index([is_uptodate], map: "is_uptodate")
  @@index([manufacturers_id], map: "manufacturers_id")
  @@index([name], map: "name")
  @@index([signature_version], map: "signature_version")
}

model glpi_computermodels {
  id                Int       @id @default(autoincrement()) @db.UnsignedInt
  name              String?   @db.VarChar(255)
  comment           String?   @db.Text
  product_number    String?   @db.VarChar(255)
  weight            Int       @default(0)
  required_units    Int       @default(1)
  depth             Float     @default(1) @db.Float
  power_connections Int       @default(0)
  power_consumption Int       @default(0)
  is_half_rack      Int       @default(0) @db.TinyInt
  picture_front     String?   @db.Text
  picture_rear      String?   @db.Text
  pictures          String?   @db.Text
  date_mod          DateTime? @db.Timestamp(0)
  date_creation     DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
  @@index([product_number], map: "product_number")
}

model glpi_computers {
  id                    Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id           Int       @default(0) @db.UnsignedInt
  name                  String?   @db.VarChar(255)
  serial                String?   @db.VarChar(255)
  otherserial           String?   @db.VarChar(255)
  contact               String?   @db.VarChar(255)
  contact_num           String?   @db.VarChar(255)
  users_id_tech         Int       @default(0) @db.UnsignedInt
  groups_id_tech        Int       @default(0) @db.UnsignedInt
  comment               String?   @db.Text
  date_mod              DateTime? @db.Timestamp(0)
  autoupdatesystems_id  Int       @default(0) @db.UnsignedInt
  locations_id          Int       @default(0) @db.UnsignedInt
  networks_id           Int       @default(0) @db.UnsignedInt
  computermodels_id     Int       @default(0) @db.UnsignedInt
  computertypes_id      Int       @default(0) @db.UnsignedInt
  is_template           Int       @default(0) @db.TinyInt
  template_name         String?   @db.VarChar(255)
  manufacturers_id      Int       @default(0) @db.UnsignedInt
  is_deleted            Int       @default(0) @db.TinyInt
  is_dynamic            Int       @default(0) @db.TinyInt
  users_id              Int       @default(0) @db.UnsignedInt
  groups_id             Int       @default(0) @db.UnsignedInt
  states_id             Int       @default(0) @db.UnsignedInt
  ticket_tco            Decimal?  @default(0.0000) @db.Decimal(20, 4)
  uuid                  String?   @db.VarChar(255)
  date_creation         DateTime? @db.Timestamp(0)
  is_recursive          Int       @default(0) @db.TinyInt
  last_inventory_update DateTime? @db.Timestamp(0)
  last_boot             DateTime? @db.Timestamp(0)

  @@index([autoupdatesystems_id], map: "autoupdatesystems_id")
  @@index([computermodels_id], map: "computermodels_id")
  @@index([computertypes_id], map: "computertypes_id")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([groups_id], map: "groups_id")
  @@index([groups_id_tech], map: "groups_id_tech")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_dynamic], map: "is_dynamic")
  @@index([is_recursive], map: "is_recursive")
  @@index([is_template], map: "is_template")
  @@index([locations_id], map: "locations_id")
  @@index([manufacturers_id], map: "manufacturers_id")
  @@index([name], map: "name")
  @@index([networks_id], map: "networks_id")
  @@index([otherserial], map: "otherserial")
  @@index([serial], map: "serial")
  @@index([states_id], map: "states_id")
  @@index([users_id], map: "users_id")
  @@index([users_id_tech], map: "users_id_tech")
  @@index([uuid], map: "uuid")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_computers_items {
  id           Int    @id @default(autoincrement()) @db.UnsignedInt
  items_id     Int    @default(0) @db.UnsignedInt
  computers_id Int    @default(0) @db.UnsignedInt
  itemtype     String @db.VarChar(100)
  is_deleted   Int    @default(0) @db.TinyInt
  is_dynamic   Int    @default(0) @db.TinyInt

  @@index([computers_id], map: "computers_id")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_dynamic], map: "is_dynamic")
  @@index([itemtype, items_id], map: "item")
}

model glpi_computertypes {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_computervirtualmachines {
  id                       Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id              Int       @default(0) @db.UnsignedInt
  computers_id             Int       @default(0) @db.UnsignedInt
  name                     String    @default("") @db.VarChar(255)
  virtualmachinestates_id  Int       @default(0) @db.UnsignedInt
  virtualmachinesystems_id Int       @default(0) @db.UnsignedInt
  virtualmachinetypes_id   Int       @default(0) @db.UnsignedInt
  uuid                     String    @default("") @db.VarChar(255)
  vcpu                     Int       @default(0)
  ram                      Int?      @db.UnsignedInt
  is_deleted               Int       @default(0) @db.TinyInt
  is_dynamic               Int       @default(0) @db.TinyInt
  comment                  String?   @db.Text
  date_mod                 DateTime? @db.Timestamp(0)
  date_creation            DateTime? @db.Timestamp(0)

  @@index([computers_id], map: "computers_id")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_dynamic], map: "is_dynamic")
  @@index([name], map: "name")
  @@index([ram], map: "ram")
  @@index([uuid], map: "uuid")
  @@index([vcpu], map: "vcpu")
  @@index([virtualmachinestates_id], map: "virtualmachinestates_id")
  @@index([virtualmachinesystems_id], map: "virtualmachinesystems_id")
  @@index([virtualmachinetypes_id], map: "virtualmachinetypes_id")
}

model glpi_configs {
  id      Int     @id @default(autoincrement()) @db.UnsignedInt
  context String? @db.VarChar(150)
  name    String? @db.VarChar(150)
  value   String? @db.Text

  @@unique([context, name], map: "unicity")
  @@index([name], map: "name")
}

model glpi_consumableitems {
  id                     Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id            Int       @default(0) @db.UnsignedInt
  is_recursive           Int       @default(0) @db.TinyInt
  name                   String?   @db.VarChar(255)
  ref                    String?   @db.VarChar(255)
  locations_id           Int       @default(0) @db.UnsignedInt
  consumableitemtypes_id Int       @default(0) @db.UnsignedInt
  manufacturers_id       Int       @default(0) @db.UnsignedInt
  users_id_tech          Int       @default(0) @db.UnsignedInt
  groups_id_tech         Int       @default(0) @db.UnsignedInt
  is_deleted             Int       @default(0) @db.TinyInt
  comment                String?   @db.Text
  alarm_threshold        Int       @default(10)
  stock_target           Int       @default(0)
  date_mod               DateTime? @db.Timestamp(0)
  date_creation          DateTime? @db.Timestamp(0)
  otherserial            String?   @db.VarChar(255)
  pictures               String?   @db.Text

  @@index([alarm_threshold], map: "alarm_threshold")
  @@index([consumableitemtypes_id], map: "consumableitemtypes_id")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([groups_id_tech], map: "groups_id_tech")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_recursive], map: "is_recursive")
  @@index([locations_id], map: "locations_id")
  @@index([manufacturers_id], map: "manufacturers_id")
  @@index([name], map: "name")
  @@index([otherserial], map: "otherserial")
  @@index([users_id_tech], map: "users_id_tech")
}

model glpi_consumableitemtypes {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_consumables {
  id                 Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id        Int       @default(0) @db.UnsignedInt
  consumableitems_id Int       @default(0) @db.UnsignedInt
  date_in            DateTime? @db.Date
  date_out           DateTime? @db.Date
  itemtype           String?   @db.VarChar(100)
  items_id           Int       @default(0) @db.UnsignedInt
  date_mod           DateTime? @db.Timestamp(0)
  date_creation      DateTime? @db.Timestamp(0)

  @@index([consumableitems_id], map: "consumableitems_id")
  @@index([date_creation], map: "date_creation")
  @@index([date_in], map: "date_in")
  @@index([date_mod], map: "date_mod")
  @@index([date_out], map: "date_out")
  @@index([entities_id], map: "entities_id")
  @@index([itemtype, items_id], map: "item")
}

model glpi_contacts {
  id                  Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id         Int       @default(0) @db.UnsignedInt
  is_recursive        Int       @default(0) @db.TinyInt
  name                String?   @db.VarChar(255)
  firstname           String?   @db.VarChar(255)
  registration_number String?   @db.VarChar(255)
  phone               String?   @db.VarChar(255)
  phone2              String?   @db.VarChar(255)
  mobile              String?   @db.VarChar(255)
  fax                 String?   @db.VarChar(255)
  email               String?   @db.VarChar(255)
  contacttypes_id     Int       @default(0) @db.UnsignedInt
  comment             String?   @db.Text
  is_deleted          Int       @default(0) @db.TinyInt
  usertitles_id       Int       @default(0) @db.UnsignedInt
  address             String?   @db.Text
  postcode            String?   @db.VarChar(255)
  town                String?   @db.VarChar(255)
  state               String?   @db.VarChar(255)
  country             String?   @db.VarChar(255)
  date_mod            DateTime? @db.Timestamp(0)
  date_creation       DateTime? @db.Timestamp(0)
  pictures            String?   @db.Text

  @@index([contacttypes_id], map: "contacttypes_id")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
  @@index([usertitles_id], map: "usertitles_id")
}

model glpi_contacts_suppliers {
  id           Int @id @default(autoincrement()) @db.UnsignedInt
  suppliers_id Int @default(0) @db.UnsignedInt
  contacts_id  Int @default(0) @db.UnsignedInt

  @@unique([suppliers_id, contacts_id], map: "unicity")
  @@index([contacts_id], map: "contacts_id")
}

model glpi_contacttypes {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_contractcosts {
  id           Int       @id @default(autoincrement()) @db.UnsignedInt
  contracts_id Int       @default(0) @db.UnsignedInt
  name         String?   @db.VarChar(255)
  comment      String?   @db.Text
  begin_date   DateTime? @db.Date
  end_date     DateTime? @db.Date
  cost         Decimal   @default(0.0000) @db.Decimal(20, 4)
  budgets_id   Int       @default(0) @db.UnsignedInt
  entities_id  Int       @default(0) @db.UnsignedInt
  is_recursive Int       @default(0) @db.TinyInt

  @@index([begin_date], map: "begin_date")
  @@index([budgets_id], map: "budgets_id")
  @@index([contracts_id], map: "contracts_id")
  @@index([end_date], map: "end_date")
  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
}

model glpi_contracts {
  id                  Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id         Int       @default(0) @db.UnsignedInt
  is_recursive        Int       @default(0) @db.TinyInt
  name                String?   @db.VarChar(255)
  num                 String?   @db.VarChar(255)
  contracttypes_id    Int       @default(0) @db.UnsignedInt
  locations_id        Int       @default(0) @db.UnsignedInt
  begin_date          DateTime? @db.Date
  duration            Int       @default(0)
  notice              Int       @default(0)
  periodicity         Int       @default(0)
  billing             Int       @default(0)
  comment             String?   @db.Text
  accounting_number   String?   @db.VarChar(255)
  is_deleted          Int       @default(0) @db.TinyInt
  week_begin_hour     DateTime  @default(dbgenerated("('00:00:00')")) @db.Time(0)
  week_end_hour       DateTime  @default(dbgenerated("('00:00:00')")) @db.Time(0)
  saturday_begin_hour DateTime  @default(dbgenerated("('00:00:00')")) @db.Time(0)
  saturday_end_hour   DateTime  @default(dbgenerated("('00:00:00')")) @db.Time(0)
  use_saturday        Int       @default(0) @db.TinyInt
  sunday_begin_hour   DateTime  @default(dbgenerated("('00:00:00')")) @db.Time(0)
  sunday_end_hour     DateTime  @default(dbgenerated("('00:00:00')")) @db.Time(0)
  use_sunday          Int       @default(0) @db.TinyInt
  max_links_allowed   Int       @default(0)
  alert               Int       @default(0)
  renewal             Int       @default(0)
  template_name       String?   @db.VarChar(255)
  is_template         Int       @default(0) @db.TinyInt
  states_id           Int       @default(0) @db.UnsignedInt
  date_mod            DateTime? @db.Timestamp(0)
  date_creation       DateTime? @db.Timestamp(0)

  @@index([alert], map: "alert")
  @@index([begin_date], map: "begin_date")
  @@index([contracttypes_id], map: "contracttypes_id")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_recursive], map: "is_recursive")
  @@index([is_template], map: "is_template")
  @@index([locations_id], map: "locations_id")
  @@index([name], map: "name")
  @@index([states_id], map: "states_id")
  @@index([use_saturday], map: "use_saturday")
  @@index([use_sunday], map: "use_sunday")
}

model glpi_contracts_items {
  id           Int    @id @default(autoincrement()) @db.UnsignedInt
  contracts_id Int    @default(0) @db.UnsignedInt
  items_id     Int    @default(0) @db.UnsignedInt
  itemtype     String @db.VarChar(100)

  @@unique([contracts_id, itemtype, items_id], map: "unicity")
  @@index([itemtype, items_id], map: "item")
}

model glpi_contracts_suppliers {
  id           Int @id @default(autoincrement()) @db.UnsignedInt
  suppliers_id Int @default(0) @db.UnsignedInt
  contracts_id Int @default(0) @db.UnsignedInt

  @@unique([suppliers_id, contracts_id], map: "unicity")
  @@index([contracts_id], map: "contracts_id")
}

model glpi_contracttypes {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_crontasklogs {
  id              Int      @id @default(autoincrement()) @db.UnsignedInt
  crontasks_id    Int      @db.UnsignedInt
  crontasklogs_id Int      @db.UnsignedInt
  date            DateTime @default(now()) @db.Timestamp(0)
  state           Int
  elapsed         Float    @db.Float
  volume          Int
  content         String?  @db.VarChar(255)

  @@index([crontasklogs_id, state], map: "crontasklogs_id_state")
  @@index([crontasks_id], map: "crontasks_id")
  @@index([date], map: "date")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_crontasks {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  itemtype      String    @db.VarChar(100)
  name          String    @db.VarChar(150)
  frequency     Int
  param         Int?
  state         Int       @default(1)
  mode          Int       @default(1)
  allowmode     Int       @default(3)
  hourmin       Int       @default(0)
  hourmax       Int       @default(24)
  logs_lifetime Int       @default(30)
  lastrun       DateTime? @db.Timestamp(0)
  lastcode      Int?
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@unique([itemtype, name], map: "unicity")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([mode], map: "mode")
  @@index([name], map: "name")
}

model glpi_dashboards_dashboards {
  id       Int    @id @default(autoincrement()) @db.UnsignedInt
  key      String @unique(map: "key") @db.VarChar(100)
  name     String @db.VarChar(100)
  context  String @default("core") @db.VarChar(100)
  users_id Int    @default(0) @db.UnsignedInt

  @@index([name], map: "name")
  @@index([users_id], map: "users_id")
}

model glpi_dashboards_filters {
  id                       Int     @id @default(autoincrement()) @db.UnsignedInt
  dashboards_dashboards_id Int     @default(0) @db.UnsignedInt
  users_id                 Int     @default(0) @db.UnsignedInt
  filter                   String? @db.LongText

  @@index([dashboards_dashboards_id], map: "dashboards_dashboards_id")
  @@index([users_id], map: "users_id")
}

model glpi_dashboards_items {
  id                       Int     @id @default(autoincrement()) @db.UnsignedInt
  dashboards_dashboards_id Int     @db.UnsignedInt
  gridstack_id             String  @db.VarChar(255)
  card_id                  String  @db.VarChar(255)
  x                        Int?
  y                        Int?
  width                    Int?
  height                   Int?
  card_options             String? @db.Text

  @@index([dashboards_dashboards_id], map: "dashboards_dashboards_id")
}

model glpi_dashboards_rights {
  id                       Int    @id @default(autoincrement()) @db.UnsignedInt
  dashboards_dashboards_id Int    @db.UnsignedInt
  itemtype                 String @db.VarChar(100)
  items_id                 Int    @db.UnsignedInt

  @@unique([dashboards_dashboards_id, itemtype, items_id], map: "unicity")
  @@index([itemtype, items_id], map: "item")
}

model glpi_databaseinstancecategories {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_databaseinstances {
  id                            Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id                   Int       @default(0) @db.UnsignedInt
  is_recursive                  Int       @default(0) @db.TinyInt
  name                          String    @default("") @db.VarChar(255)
  version                       String    @default("") @db.VarChar(255)
  port                          String    @default("") @db.VarChar(10)
  path                          String    @default("") @db.VarChar(255)
  size                          Int       @default(0)
  databaseinstancetypes_id      Int       @default(0) @db.UnsignedInt
  databaseinstancecategories_id Int       @default(0) @db.UnsignedInt
  locations_id                  Int       @default(0) @db.UnsignedInt
  manufacturers_id              Int       @default(0) @db.UnsignedInt
  users_id_tech                 Int       @default(0) @db.UnsignedInt
  groups_id_tech                Int       @default(0) @db.UnsignedInt
  states_id                     Int       @default(0) @db.UnsignedInt
  itemtype                      String    @default("") @db.VarChar(100)
  items_id                      Int       @default(0) @db.UnsignedInt
  is_onbackup                   Int       @default(0) @db.TinyInt
  is_active                     Int       @default(0) @db.TinyInt
  is_deleted                    Int       @default(0) @db.TinyInt
  is_helpdesk_visible           Int       @default(1) @db.TinyInt
  is_dynamic                    Int       @default(0) @db.TinyInt
  autoupdatesystems_id          Int       @default(0) @db.UnsignedInt
  date_creation                 DateTime? @db.Timestamp(0)
  date_mod                      DateTime? @db.Timestamp(0)
  date_lastboot                 DateTime? @db.Timestamp(0)
  date_lastbackup               DateTime? @db.Timestamp(0)
  comment                       String?   @db.Text

  @@index([autoupdatesystems_id], map: "autoupdatesystems_id")
  @@index([databaseinstancecategories_id], map: "databaseinstancecategories_id")
  @@index([databaseinstancetypes_id], map: "databaseinstancetypes_id")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([groups_id_tech], map: "groups_id_tech")
  @@index([is_active], map: "is_active")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_dynamic], map: "is_dynamic")
  @@index([is_helpdesk_visible], map: "is_helpdesk_visible")
  @@index([is_recursive], map: "is_recursive")
  @@index([itemtype, items_id], map: "item")
  @@index([locations_id], map: "locations_id")
  @@index([manufacturers_id], map: "manufacturers_id")
  @@index([name], map: "name")
  @@index([states_id], map: "states_id")
  @@index([users_id_tech], map: "users_id_tech")
}

model glpi_databaseinstancetypes {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_databases {
  id                   Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id          Int       @default(0) @db.UnsignedInt
  is_recursive         Int       @default(0) @db.TinyInt
  name                 String    @default("") @db.VarChar(255)
  size                 Int       @default(0)
  databaseinstances_id Int       @default(0) @db.UnsignedInt
  is_onbackup          Int       @default(0) @db.TinyInt
  is_active            Int       @default(0) @db.TinyInt
  is_deleted           Int       @default(0) @db.TinyInt
  is_dynamic           Int       @default(0) @db.TinyInt
  date_creation        DateTime? @db.Timestamp(0)
  date_mod             DateTime? @db.Timestamp(0)
  date_update          DateTime? @db.Timestamp(0)
  date_lastbackup      DateTime? @db.Timestamp(0)

  @@index([databaseinstances_id], map: "databaseinstances_id")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([is_active], map: "is_active")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_dynamic], map: "is_dynamic")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
}

model glpi_datacenters {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  entities_id   Int       @default(0) @db.UnsignedInt
  is_recursive  Int       @default(0) @db.TinyInt
  locations_id  Int       @default(0) @db.UnsignedInt
  is_deleted    Int       @default(0) @db.TinyInt
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)
  pictures      String?   @db.Text

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_recursive], map: "is_recursive")
  @@index([locations_id], map: "locations_id")
  @@index([name], map: "name")
}

model glpi_dcrooms {
  id             Int       @id @default(autoincrement()) @db.UnsignedInt
  name           String?   @db.VarChar(255)
  entities_id    Int       @default(0) @db.UnsignedInt
  is_recursive   Int       @default(0) @db.TinyInt
  locations_id   Int       @default(0) @db.UnsignedInt
  vis_cols       Int?
  vis_rows       Int?
  blueprint      String?   @db.Text
  datacenters_id Int       @default(0) @db.UnsignedInt
  is_deleted     Int       @default(0) @db.TinyInt
  date_mod       DateTime? @db.Timestamp(0)
  date_creation  DateTime? @db.Timestamp(0)

  @@index([datacenters_id], map: "datacenters_id")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_recursive], map: "is_recursive")
  @@index([locations_id], map: "locations_id")
  @@index([name], map: "name")
}

model glpi_devicebatteries {
  id                     Int       @id @default(autoincrement()) @db.UnsignedInt
  designation            String?   @db.VarChar(255)
  comment                String?   @db.Text
  manufacturers_id       Int       @default(0) @db.UnsignedInt
  voltage                Int?
  capacity               Int?
  devicebatterytypes_id  Int       @default(0) @db.UnsignedInt
  entities_id            Int       @default(0) @db.UnsignedInt
  is_recursive           Int       @default(0) @db.TinyInt
  devicebatterymodels_id Int?      @db.UnsignedInt
  date_mod               DateTime? @db.Timestamp(0)
  date_creation          DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([designation], map: "designation")
  @@index([devicebatterymodels_id], map: "devicebatterymodels_id")
  @@index([devicebatterytypes_id], map: "devicebatterytypes_id")
  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([manufacturers_id], map: "manufacturers_id")
}

model glpi_devicebatterymodels {
  id             Int     @id @default(autoincrement()) @db.UnsignedInt
  name           String? @db.VarChar(255)
  comment        String? @db.Text
  product_number String? @db.VarChar(255)

  @@index([name], map: "name")
  @@index([product_number], map: "product_number")
}

model glpi_devicebatterytypes {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_devicecameramodels {
  id             Int     @id @default(autoincrement()) @db.UnsignedInt
  name           String? @db.VarChar(255)
  comment        String? @db.Text
  product_number String? @db.VarChar(255)

  @@index([name], map: "name")
  @@index([product_number], map: "product_number")
}

model glpi_devicecameras {
  id                    Int       @id @default(autoincrement()) @db.UnsignedInt
  designation           String?   @db.VarChar(255)
  flashunit             Int       @default(0) @db.TinyInt
  lensfacing            String?   @db.VarChar(255)
  orientation           String?   @db.VarChar(255)
  focallength           String?   @db.VarChar(255)
  sensorsize            String?   @db.VarChar(255)
  comment               String?   @db.Text
  manufacturers_id      Int       @default(0) @db.UnsignedInt
  entities_id           Int       @default(0) @db.UnsignedInt
  is_recursive          Int       @default(0) @db.TinyInt
  devicecameramodels_id Int?      @db.UnsignedInt
  support               String?   @db.VarChar(255)
  date_mod              DateTime? @db.Timestamp(0)
  date_creation         DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([designation], map: "designation")
  @@index([devicecameramodels_id], map: "devicecameramodels_id")
  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([manufacturers_id], map: "manufacturers_id")
}

model glpi_devicecasemodels {
  id             Int     @id @default(autoincrement()) @db.UnsignedInt
  name           String? @db.VarChar(255)
  comment        String? @db.Text
  product_number String? @db.VarChar(255)

  @@index([name], map: "name")
  @@index([product_number], map: "product_number")
}

model glpi_devicecases {
  id                  Int       @id @default(autoincrement()) @db.UnsignedInt
  designation         String?   @db.VarChar(255)
  devicecasetypes_id  Int       @default(0) @db.UnsignedInt
  comment             String?   @db.Text
  manufacturers_id    Int       @default(0) @db.UnsignedInt
  entities_id         Int       @default(0) @db.UnsignedInt
  is_recursive        Int       @default(0) @db.TinyInt
  devicecasemodels_id Int?      @db.UnsignedInt
  date_mod            DateTime? @db.Timestamp(0)
  date_creation       DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([designation], map: "designation")
  @@index([devicecasemodels_id], map: "devicecasemodels_id")
  @@index([devicecasetypes_id], map: "devicecasetypes_id")
  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([manufacturers_id], map: "manufacturers_id")
}

model glpi_devicecasetypes {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_devicecontrolmodels {
  id             Int     @id @default(autoincrement()) @db.UnsignedInt
  name           String? @db.VarChar(255)
  comment        String? @db.Text
  product_number String? @db.VarChar(255)

  @@index([name], map: "name")
  @@index([product_number], map: "product_number")
}

model glpi_devicecontrols {
  id                     Int       @id @default(autoincrement()) @db.UnsignedInt
  designation            String?   @db.VarChar(255)
  is_raid                Int       @default(0) @db.TinyInt
  comment                String?   @db.Text
  manufacturers_id       Int       @default(0) @db.UnsignedInt
  interfacetypes_id      Int       @default(0) @db.UnsignedInt
  entities_id            Int       @default(0) @db.UnsignedInt
  is_recursive           Int       @default(0) @db.TinyInt
  devicecontrolmodels_id Int?      @db.UnsignedInt
  date_mod               DateTime? @db.Timestamp(0)
  date_creation          DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([designation], map: "designation")
  @@index([devicecontrolmodels_id], map: "devicecontrolmodels_id")
  @@index([entities_id], map: "entities_id")
  @@index([interfacetypes_id], map: "interfacetypes_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([manufacturers_id], map: "manufacturers_id")
}

model glpi_devicedrivemodels {
  id             Int     @id @default(autoincrement()) @db.UnsignedInt
  name           String? @db.VarChar(255)
  comment        String? @db.Text
  product_number String? @db.VarChar(255)

  @@index([name], map: "name")
  @@index([product_number], map: "product_number")
}

model glpi_devicedrives {
  id                   Int       @id @default(autoincrement()) @db.UnsignedInt
  designation          String?   @db.VarChar(255)
  is_writer            Int       @default(1) @db.TinyInt
  speed                String?   @db.VarChar(255)
  comment              String?   @db.Text
  manufacturers_id     Int       @default(0) @db.UnsignedInt
  interfacetypes_id    Int       @default(0) @db.UnsignedInt
  entities_id          Int       @default(0) @db.UnsignedInt
  is_recursive         Int       @default(0) @db.TinyInt
  devicedrivemodels_id Int?      @db.UnsignedInt
  date_mod             DateTime? @db.Timestamp(0)
  date_creation        DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([designation], map: "designation")
  @@index([devicedrivemodels_id], map: "devicedrivemodels_id")
  @@index([entities_id], map: "entities_id")
  @@index([interfacetypes_id], map: "interfacetypes_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([manufacturers_id], map: "manufacturers_id")
}

model glpi_devicefirmwaremodels {
  id             Int     @id @default(autoincrement()) @db.UnsignedInt
  name           String? @db.VarChar(255)
  comment        String? @db.Text
  product_number String? @db.VarChar(255)

  @@index([name], map: "name")
  @@index([product_number], map: "product_number")
}

model glpi_devicefirmwares {
  id                      Int       @id @default(autoincrement()) @db.UnsignedInt
  designation             String?   @db.VarChar(255)
  comment                 String?   @db.Text
  manufacturers_id        Int       @default(0) @db.UnsignedInt
  date                    DateTime? @db.Date
  version                 String?   @db.VarChar(255)
  devicefirmwaretypes_id  Int       @default(0) @db.UnsignedInt
  entities_id             Int       @default(0) @db.UnsignedInt
  is_recursive            Int       @default(0) @db.TinyInt
  devicefirmwaremodels_id Int?      @db.UnsignedInt
  date_mod                DateTime? @db.Timestamp(0)
  date_creation           DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([designation], map: "designation")
  @@index([devicefirmwaremodels_id], map: "devicefirmwaremodels_id")
  @@index([devicefirmwaretypes_id], map: "devicefirmwaretypes_id")
  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([manufacturers_id], map: "manufacturers_id")
}

model glpi_devicefirmwaretypes {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_devicegenericmodels {
  id             Int     @id @default(autoincrement()) @db.UnsignedInt
  name           String? @db.VarChar(255)
  comment        String? @db.Text
  product_number String? @db.VarChar(255)

  @@index([name], map: "name")
  @@index([product_number], map: "product_number")
}

model glpi_devicegenerics {
  id                     Int       @id @default(autoincrement()) @db.UnsignedInt
  designation            String?   @db.VarChar(255)
  devicegenerictypes_id  Int       @default(0) @db.UnsignedInt
  comment                String?   @db.Text
  manufacturers_id       Int       @default(0) @db.UnsignedInt
  entities_id            Int       @default(0) @db.UnsignedInt
  is_recursive           Int       @default(0) @db.TinyInt
  locations_id           Int       @default(0) @db.UnsignedInt
  states_id              Int       @default(0) @db.UnsignedInt
  devicegenericmodels_id Int?      @db.UnsignedInt
  date_mod               DateTime? @db.Timestamp(0)
  date_creation          DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([designation], map: "designation")
  @@index([devicegenericmodels_id], map: "devicegenericmodels_id")
  @@index([devicegenerictypes_id], map: "devicegenerictypes_id")
  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([locations_id], map: "locations_id")
  @@index([manufacturers_id], map: "manufacturers_id")
  @@index([states_id], map: "states_id")
}

model glpi_devicegenerictypes {
  id      Int     @id @default(autoincrement()) @db.UnsignedInt
  name    String? @db.VarChar(255)
  comment String? @db.Text

  @@index([name], map: "name")
}

model glpi_devicegraphiccardmodels {
  id             Int     @id @default(autoincrement()) @db.UnsignedInt
  name           String? @db.VarChar(255)
  comment        String? @db.Text
  product_number String? @db.VarChar(255)

  @@index([name], map: "name")
  @@index([product_number], map: "product_number")
}

model glpi_devicegraphiccards {
  id                         Int       @id @default(autoincrement()) @db.UnsignedInt
  designation                String?   @db.VarChar(255)
  interfacetypes_id          Int       @default(0) @db.UnsignedInt
  comment                    String?   @db.Text
  manufacturers_id           Int       @default(0) @db.UnsignedInt
  memory_default             Int       @default(0)
  entities_id                Int       @default(0) @db.UnsignedInt
  is_recursive               Int       @default(0) @db.TinyInt
  devicegraphiccardmodels_id Int?      @db.UnsignedInt
  chipset                    String?   @db.VarChar(255)
  date_mod                   DateTime? @db.Timestamp(0)
  date_creation              DateTime? @db.Timestamp(0)

  @@index([chipset], map: "chipset")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([designation], map: "designation")
  @@index([devicegraphiccardmodels_id], map: "devicegraphiccardmodels_id")
  @@index([entities_id], map: "entities_id")
  @@index([interfacetypes_id], map: "interfacetypes_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([manufacturers_id], map: "manufacturers_id")
}

model glpi_deviceharddrivemodels {
  id             Int     @id @default(autoincrement()) @db.UnsignedInt
  name           String? @db.VarChar(255)
  comment        String? @db.Text
  product_number String? @db.VarChar(255)

  @@index([name], map: "name")
  @@index([product_number], map: "product_number")
}

model glpi_deviceharddrives {
  id                       Int       @id @default(autoincrement()) @db.UnsignedInt
  designation              String?   @db.VarChar(255)
  rpm                      String?   @db.VarChar(255)
  interfacetypes_id        Int       @default(0) @db.UnsignedInt
  cache                    String?   @db.VarChar(255)
  comment                  String?   @db.Text
  manufacturers_id         Int       @default(0) @db.UnsignedInt
  capacity_default         Int       @default(0)
  entities_id              Int       @default(0) @db.UnsignedInt
  is_recursive             Int       @default(0) @db.TinyInt
  deviceharddrivemodels_id Int?      @db.UnsignedInt
  date_mod                 DateTime? @db.Timestamp(0)
  date_creation            DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([designation], map: "designation")
  @@index([deviceharddrivemodels_id], map: "deviceharddrivemodels_id")
  @@index([entities_id], map: "entities_id")
  @@index([interfacetypes_id], map: "interfacetypes_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([manufacturers_id], map: "manufacturers_id")
}

model glpi_devicememories {
  id                    Int       @id @default(autoincrement()) @db.UnsignedInt
  designation           String?   @db.VarChar(255)
  frequence             String?   @db.VarChar(255)
  comment               String?   @db.Text
  manufacturers_id      Int       @default(0) @db.UnsignedInt
  size_default          Int       @default(0)
  devicememorytypes_id  Int       @default(0) @db.UnsignedInt
  entities_id           Int       @default(0) @db.UnsignedInt
  is_recursive          Int       @default(0) @db.TinyInt
  devicememorymodels_id Int?      @db.UnsignedInt
  date_mod              DateTime? @db.Timestamp(0)
  date_creation         DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([designation], map: "designation")
  @@index([devicememorymodels_id], map: "devicememorymodels_id")
  @@index([devicememorytypes_id], map: "devicememorytypes_id")
  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([manufacturers_id], map: "manufacturers_id")
}

model glpi_devicememorymodels {
  id             Int     @id @default(autoincrement()) @db.UnsignedInt
  name           String? @db.VarChar(255)
  comment        String? @db.Text
  product_number String? @db.VarChar(255)

  @@index([name], map: "name")
  @@index([product_number], map: "product_number")
}

model glpi_devicememorytypes {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_devicemotherboardmodels {
  id             Int     @id @default(autoincrement()) @db.UnsignedInt
  name           String? @db.VarChar(255)
  comment        String? @db.Text
  product_number String? @db.VarChar(255)

  @@index([name], map: "name")
  @@index([product_number], map: "product_number")
}

model glpi_devicemotherboards {
  id                         Int       @id @default(autoincrement()) @db.UnsignedInt
  designation                String?   @db.VarChar(255)
  chipset                    String?   @db.VarChar(255)
  comment                    String?   @db.Text
  manufacturers_id           Int       @default(0) @db.UnsignedInt
  entities_id                Int       @default(0) @db.UnsignedInt
  is_recursive               Int       @default(0) @db.TinyInt
  devicemotherboardmodels_id Int?      @db.UnsignedInt
  date_mod                   DateTime? @db.Timestamp(0)
  date_creation              DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([designation], map: "designation")
  @@index([devicemotherboardmodels_id], map: "devicemotherboardmodels_id")
  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([manufacturers_id], map: "manufacturers_id")
}

model glpi_devicenetworkcardmodels {
  id             Int     @id @default(autoincrement()) @db.UnsignedInt
  name           String? @db.VarChar(255)
  comment        String? @db.Text
  product_number String? @db.VarChar(255)

  @@index([name], map: "name")
  @@index([product_number], map: "product_number")
}

model glpi_devicenetworkcards {
  id                         Int       @id @default(autoincrement()) @db.UnsignedInt
  designation                String?   @db.VarChar(255)
  bandwidth                  String?   @db.VarChar(255)
  comment                    String?   @db.Text
  manufacturers_id           Int       @default(0) @db.UnsignedInt
  mac_default                String?   @db.VarChar(255)
  entities_id                Int       @default(0) @db.UnsignedInt
  is_recursive               Int       @default(0) @db.TinyInt
  devicenetworkcardmodels_id Int?      @db.UnsignedInt
  date_mod                   DateTime? @db.Timestamp(0)
  date_creation              DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([designation], map: "designation")
  @@index([devicenetworkcardmodels_id], map: "devicenetworkcardmodels_id")
  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([manufacturers_id], map: "manufacturers_id")
}

model glpi_devicepcimodels {
  id             Int     @id @default(autoincrement()) @db.UnsignedInt
  name           String? @db.VarChar(255)
  comment        String? @db.Text
  product_number String? @db.VarChar(255)

  @@index([name], map: "name")
  @@index([product_number], map: "product_number")
}

model glpi_devicepcis {
  id                         Int       @id @default(autoincrement()) @db.UnsignedInt
  designation                String?   @db.VarChar(255)
  comment                    String?   @db.Text
  manufacturers_id           Int       @default(0) @db.UnsignedInt
  devicenetworkcardmodels_id Int       @default(0) @db.UnsignedInt
  entities_id                Int       @default(0) @db.UnsignedInt
  is_recursive               Int       @default(0) @db.TinyInt
  devicepcimodels_id         Int?      @db.UnsignedInt
  date_mod                   DateTime? @db.Timestamp(0)
  date_creation              DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([designation], map: "designation")
  @@index([devicenetworkcardmodels_id], map: "devicenetworkcardmodels_id")
  @@index([devicepcimodels_id], map: "devicepcimodels_id")
  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([manufacturers_id], map: "manufacturers_id")
}

model glpi_devicepowersupplies {
  id                         Int       @id @default(autoincrement()) @db.UnsignedInt
  designation                String?   @db.VarChar(255)
  power                      String?   @db.VarChar(255)
  is_atx                     Int       @default(1) @db.TinyInt
  comment                    String?   @db.Text
  manufacturers_id           Int       @default(0) @db.UnsignedInt
  entities_id                Int       @default(0) @db.UnsignedInt
  is_recursive               Int       @default(0) @db.TinyInt
  devicepowersupplymodels_id Int?      @db.UnsignedInt
  date_mod                   DateTime? @db.Timestamp(0)
  date_creation              DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([designation], map: "designation")
  @@index([devicepowersupplymodels_id], map: "devicepowersupplymodels_id")
  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([manufacturers_id], map: "manufacturers_id")
}

model glpi_devicepowersupplymodels {
  id             Int     @id @default(autoincrement()) @db.UnsignedInt
  name           String? @db.VarChar(255)
  comment        String? @db.Text
  product_number String? @db.VarChar(255)

  @@index([name], map: "name")
  @@index([product_number], map: "product_number")
}

model glpi_deviceprocessormodels {
  id             Int     @id @default(autoincrement()) @db.UnsignedInt
  name           String? @db.VarChar(255)
  comment        String? @db.Text
  product_number String? @db.VarChar(255)

  @@index([name], map: "name")
  @@index([product_number], map: "product_number")
}

model glpi_deviceprocessors {
  id                       Int       @id @default(autoincrement()) @db.UnsignedInt
  designation              String?   @db.VarChar(255)
  frequence                Int       @default(0)
  comment                  String?   @db.Text
  manufacturers_id         Int       @default(0) @db.UnsignedInt
  frequency_default        Int       @default(0)
  nbcores_default          Int?
  nbthreads_default        Int?
  entities_id              Int       @default(0) @db.UnsignedInt
  is_recursive             Int       @default(0) @db.TinyInt
  deviceprocessormodels_id Int?      @db.UnsignedInt
  date_mod                 DateTime? @db.Timestamp(0)
  date_creation            DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([designation], map: "designation")
  @@index([deviceprocessormodels_id], map: "deviceprocessormodels_id")
  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([manufacturers_id], map: "manufacturers_id")
}

model glpi_devicesensormodels {
  id             Int     @id @default(autoincrement()) @db.UnsignedInt
  name           String? @db.VarChar(255)
  comment        String? @db.Text
  product_number String? @db.VarChar(255)

  @@index([name], map: "name")
  @@index([product_number], map: "product_number")
}

model glpi_devicesensors {
  id                    Int       @id @default(autoincrement()) @db.UnsignedInt
  designation           String?   @db.VarChar(255)
  devicesensortypes_id  Int       @default(0) @db.UnsignedInt
  devicesensormodels_id Int       @default(0) @db.UnsignedInt
  comment               String?   @db.Text
  manufacturers_id      Int       @default(0) @db.UnsignedInt
  entities_id           Int       @default(0) @db.UnsignedInt
  is_recursive          Int       @default(0) @db.TinyInt
  locations_id          Int       @default(0) @db.UnsignedInt
  states_id             Int       @default(0) @db.UnsignedInt
  date_mod              DateTime? @db.Timestamp(0)
  date_creation         DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([designation], map: "designation")
  @@index([devicesensormodels_id], map: "devicesensormodels_id")
  @@index([devicesensortypes_id], map: "devicesensortypes_id")
  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([locations_id], map: "locations_id")
  @@index([manufacturers_id], map: "manufacturers_id")
  @@index([states_id], map: "states_id")
}

model glpi_devicesensortypes {
  id      Int     @id @default(autoincrement()) @db.UnsignedInt
  name    String? @db.VarChar(255)
  comment String? @db.Text

  @@index([name], map: "name")
}

model glpi_devicesimcards {
  id                    Int       @id @default(autoincrement()) @db.UnsignedInt
  designation           String?   @db.VarChar(255)
  comment               String?   @db.Text
  entities_id           Int       @default(0) @db.UnsignedInt
  is_recursive          Int       @default(0) @db.TinyInt
  manufacturers_id      Int       @default(0) @db.UnsignedInt
  voltage               Int?
  devicesimcardtypes_id Int       @default(0) @db.UnsignedInt
  date_mod              DateTime? @db.Timestamp(0)
  date_creation         DateTime? @db.Timestamp(0)
  allow_voip            Int       @default(0) @db.TinyInt

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([designation], map: "designation")
  @@index([devicesimcardtypes_id], map: "devicesimcardtypes_id")
  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([manufacturers_id], map: "manufacturers_id")
}

model glpi_devicesimcardtypes {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String    @default("") @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_devicesoundcardmodels {
  id             Int     @id @default(autoincrement()) @db.UnsignedInt
  name           String? @db.VarChar(255)
  comment        String? @db.Text
  product_number String? @db.VarChar(255)

  @@index([name], map: "name")
  @@index([product_number], map: "product_number")
}

model glpi_devicesoundcards {
  id                       Int       @id @default(autoincrement()) @db.UnsignedInt
  designation              String?   @db.VarChar(255)
  type                     String?   @db.VarChar(255)
  comment                  String?   @db.Text
  manufacturers_id         Int       @default(0) @db.UnsignedInt
  entities_id              Int       @default(0) @db.UnsignedInt
  is_recursive             Int       @default(0) @db.TinyInt
  devicesoundcardmodels_id Int?      @db.UnsignedInt
  date_mod                 DateTime? @db.Timestamp(0)
  date_creation            DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([designation], map: "designation")
  @@index([devicesoundcardmodels_id], map: "devicesoundcardmodels_id")
  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([manufacturers_id], map: "manufacturers_id")
}

model glpi_displaypreferences {
  id       Int    @id @default(autoincrement()) @db.UnsignedInt
  itemtype String @db.VarChar(100)
  num      Int    @default(0)
  rank     Int    @default(0)
  users_id Int    @default(0) @db.UnsignedInt

  @@unique([users_id, itemtype, num], map: "unicity")
  @@index([itemtype], map: "itemtype")
  @@index([num], map: "num")
  @@index([rank], map: "rank")
}

model glpi_documentcategories {
  id                    Int       @id @default(autoincrement()) @db.UnsignedInt
  name                  String?   @db.VarChar(255)
  comment               String?   @db.Text
  documentcategories_id Int       @default(0) @db.UnsignedInt
  completename          String?   @db.Text
  level                 Int       @default(0)
  ancestors_cache       String?   @db.LongText
  sons_cache            String?   @db.LongText
  date_mod              DateTime? @db.Timestamp(0)
  date_creation         DateTime? @db.Timestamp(0)

  @@unique([documentcategories_id, name], map: "unicity")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([level], map: "level")
  @@index([name], map: "name")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_documents {
  id                    Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id           Int       @default(0) @db.UnsignedInt
  is_recursive          Int       @default(0) @db.TinyInt
  name                  String?   @db.VarChar(255)
  filename              String?   @db.VarChar(255)
  filepath              String?   @db.VarChar(255)
  documentcategories_id Int       @default(0) @db.UnsignedInt
  mime                  String?   @db.VarChar(255)
  date_mod              DateTime? @db.Timestamp(0)
  comment               String?   @db.Text
  is_deleted            Int       @default(0) @db.TinyInt
  link                  String?   @db.VarChar(255)
  users_id              Int       @default(0) @db.UnsignedInt
  tickets_id            Int       @default(0) @db.UnsignedInt
  sha1sum               String?   @db.Char(40)
  is_blacklisted        Int       @default(0) @db.TinyInt
  tag                   String?   @db.VarChar(255)
  date_creation         DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([documentcategories_id], map: "documentcategories_id")
  @@index([entities_id], map: "entities_id")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
  @@index([sha1sum], map: "sha1sum")
  @@index([tag], map: "tag")
  @@index([tickets_id], map: "tickets_id")
  @@index([users_id], map: "users_id")
}

model glpi_documents_items {
  id                Int       @id @default(autoincrement()) @db.UnsignedInt
  documents_id      Int       @default(0) @db.UnsignedInt
  items_id          Int       @default(0) @db.UnsignedInt
  itemtype          String    @db.VarChar(100)
  entities_id       Int       @default(0) @db.UnsignedInt
  is_recursive      Int       @default(0) @db.TinyInt
  date_mod          DateTime? @db.Timestamp(0)
  users_id          Int?      @default(0) @db.UnsignedInt
  timeline_position Int       @default(0) @db.TinyInt
  date_creation     DateTime? @db.Timestamp(0)
  date              DateTime? @db.Timestamp(0)

  @@unique([documents_id, itemtype, items_id, timeline_position], map: "unicity")
  @@index([date], map: "date")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([itemtype, items_id, entities_id, is_recursive], map: "item")
  @@index([timeline_position], map: "timeline_position")
  @@index([users_id], map: "users_id")
}

model glpi_documenttypes {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  ext           String?   @unique(map: "unicity") @db.VarChar(255)
  icon          String?   @db.VarChar(255)
  mime          String?   @db.VarChar(255)
  is_uploadable Int       @default(1) @db.TinyInt
  date_mod      DateTime? @db.Timestamp(0)
  comment       String?   @db.Text
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([is_uploadable], map: "is_uploadable")
  @@index([name], map: "name")
}

model glpi_domainrecords {
  id                   Int       @id @default(autoincrement()) @db.UnsignedInt
  name                 String?   @db.VarChar(255)
  data                 String?   @db.Text
  data_obj             String?   @db.Text
  entities_id          Int       @default(0) @db.UnsignedInt
  is_recursive         Int       @default(0) @db.TinyInt
  domains_id           Int       @default(0) @db.UnsignedInt
  domainrecordtypes_id Int       @default(0) @db.UnsignedInt
  ttl                  Int
  users_id_tech        Int       @default(0) @db.UnsignedInt
  groups_id_tech       Int       @default(0) @db.UnsignedInt
  is_deleted           Int       @default(0) @db.TinyInt
  comment              String?   @db.Text
  date_mod             DateTime? @db.Timestamp(0)
  date_creation        DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([domainrecordtypes_id], map: "domainrecordtypes_id")
  @@index([domains_id], map: "domains_id")
  @@index([entities_id], map: "entities_id")
  @@index([groups_id_tech], map: "groups_id_tech")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
  @@index([users_id_tech], map: "users_id_tech")
}

model glpi_domainrecordtypes {
  id           Int     @id @default(autoincrement()) @db.UnsignedInt
  name         String? @db.VarChar(255)
  fields       String? @db.Text
  entities_id  Int     @default(0) @db.UnsignedInt
  is_recursive Int     @default(0) @db.TinyInt
  comment      String? @db.Text

  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
}

model glpi_domainrelations {
  id           Int     @id @default(autoincrement()) @db.UnsignedInt
  name         String? @db.VarChar(255)
  entities_id  Int     @default(0) @db.UnsignedInt
  is_recursive Int     @default(0) @db.TinyInt
  comment      String? @db.Text

  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
}

model glpi_domains {
  id                  Int       @id @default(autoincrement()) @db.UnsignedInt
  name                String?   @db.VarChar(255)
  entities_id         Int       @default(0) @db.UnsignedInt
  is_recursive        Int       @default(0) @db.TinyInt
  domaintypes_id      Int       @default(0) @db.UnsignedInt
  date_expiration     DateTime? @db.Timestamp(0)
  date_domaincreation DateTime? @db.Timestamp(0)
  users_id_tech       Int       @default(0) @db.UnsignedInt
  groups_id_tech      Int       @default(0) @db.UnsignedInt
  is_deleted          Int       @default(0) @db.TinyInt
  comment             String?   @db.Text
  is_template         Int       @default(0) @db.TinyInt
  template_name       String?   @db.VarChar(255)
  is_active           Int       @default(0) @db.TinyInt
  date_mod            DateTime? @db.Timestamp(0)
  date_creation       DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_domaincreation], map: "date_domaincreation")
  @@index([date_expiration], map: "date_expiration")
  @@index([date_mod], map: "date_mod")
  @@index([domaintypes_id], map: "domaintypes_id")
  @@index([entities_id], map: "entities_id")
  @@index([groups_id_tech], map: "groups_id_tech")
  @@index([is_active], map: "is_active")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_recursive], map: "is_recursive")
  @@index([is_template], map: "is_template")
  @@index([name], map: "name")
  @@index([users_id_tech], map: "users_id_tech")
}

model glpi_domains_items {
  id                 Int    @id @default(autoincrement()) @db.UnsignedInt
  domains_id         Int    @default(0) @db.UnsignedInt
  items_id           Int    @default(0) @db.UnsignedInt
  itemtype           String @db.VarChar(100)
  domainrelations_id Int    @default(0) @db.UnsignedInt
  is_dynamic         Int    @default(0) @db.TinyInt
  is_deleted         Int    @default(0) @db.TinyInt

  @@unique([domains_id, itemtype, items_id], map: "unicity")
  @@index([domainrelations_id], map: "domainrelations_id")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_dynamic], map: "is_dynamic")
  @@index([itemtype, items_id], map: "item")
}

model glpi_domaintypes {
  id           Int     @id @default(autoincrement()) @db.UnsignedInt
  name         String? @db.VarChar(255)
  entities_id  Int     @default(0) @db.UnsignedInt
  is_recursive Int     @default(0) @db.TinyInt
  comment      String? @db.Text

  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
}

model glpi_dropdowntranslations {
  id       Int     @id @default(autoincrement()) @db.UnsignedInt
  items_id Int     @default(0) @db.UnsignedInt
  itemtype String? @db.VarChar(100)
  language String? @db.VarChar(10)
  field    String? @db.VarChar(100)
  value    String? @db.Text

  @@unique([itemtype, items_id, language, field], map: "unicity")
  @@index([field], map: "field")
  @@index([language], map: "language")
}

model glpi_enclosuremodels {
  id                Int       @id @default(autoincrement()) @db.UnsignedInt
  name              String?   @db.VarChar(255)
  comment           String?   @db.Text
  product_number    String?   @db.VarChar(255)
  weight            Int       @default(0)
  required_units    Int       @default(1)
  depth             Float     @default(1) @db.Float
  power_connections Int       @default(0)
  power_consumption Int       @default(0)
  is_half_rack      Int       @default(0) @db.TinyInt
  picture_front     String?   @db.Text
  picture_rear      String?   @db.Text
  pictures          String?   @db.Text
  date_mod          DateTime? @db.Timestamp(0)
  date_creation     DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
  @@index([product_number], map: "product_number")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_enclosures {
  id                 Int       @id @default(autoincrement()) @db.UnsignedInt
  name               String?   @db.VarChar(255)
  entities_id        Int       @default(0) @db.UnsignedInt
  is_recursive       Int       @default(0) @db.TinyInt
  locations_id       Int       @default(0) @db.UnsignedInt
  serial             String?   @db.VarChar(255)
  otherserial        String?   @db.VarChar(255)
  enclosuremodels_id Int?      @db.UnsignedInt
  users_id_tech      Int       @default(0) @db.UnsignedInt
  groups_id_tech     Int       @default(0) @db.UnsignedInt
  is_template        Int       @default(0) @db.TinyInt
  template_name      String?   @db.VarChar(255)
  is_deleted         Int       @default(0) @db.TinyInt
  orientation        Int?      @db.TinyInt
  power_supplies     Int       @default(0) @db.TinyInt
  states_id          Int       @default(0) @db.UnsignedInt
  comment            String?   @db.Text
  manufacturers_id   Int       @default(0) @db.UnsignedInt
  date_mod           DateTime? @db.Timestamp(0)
  date_creation      DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([enclosuremodels_id], map: "enclosuremodels_id")
  @@index([entities_id], map: "entities_id")
  @@index([groups_id_tech], map: "group_id_tech")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_recursive], map: "is_recursive")
  @@index([is_template], map: "is_template")
  @@index([locations_id], map: "locations_id")
  @@index([manufacturers_id], map: "manufacturers_id")
  @@index([name], map: "name")
  @@index([states_id], map: "states_id")
  @@index([users_id_tech], map: "users_id_tech")
}

model glpi_entities {
  id                                      Int       @id @default(0) @db.UnsignedInt
  name                                    String?   @db.VarChar(255)
  entities_id                             Int?      @default(0) @db.UnsignedInt
  completename                            String?   @db.Text
  comment                                 String?   @db.Text
  level                                   Int       @default(0)
  sons_cache                              String?   @db.LongText
  ancestors_cache                         String?   @db.LongText
  registration_number                     String?   @db.VarChar(255)
  address                                 String?   @db.Text
  postcode                                String?   @db.VarChar(255)
  town                                    String?   @db.VarChar(255)
  state                                   String?   @db.VarChar(255)
  country                                 String?   @db.VarChar(255)
  website                                 String?   @db.VarChar(255)
  phonenumber                             String?   @db.VarChar(255)
  fax                                     String?   @db.VarChar(255)
  email                                   String?   @db.VarChar(255)
  admin_email                             String?   @db.VarChar(255)
  admin_email_name                        String?   @db.VarChar(255)
  from_email                              String?   @db.VarChar(255)
  from_email_name                         String?   @db.VarChar(255)
  noreply_email                           String?   @db.VarChar(255)
  noreply_email_name                      String?   @db.VarChar(255)
  replyto_email                           String?   @db.VarChar(255)
  replyto_email_name                      String?   @db.VarChar(255)
  notification_subject_tag                String?   @db.VarChar(255)
  ldap_dn                                 String?   @db.VarChar(255)
  tag                                     String?   @db.VarChar(255)
  authldaps_id                            Int       @default(0) @db.UnsignedInt
  mail_domain                             String?   @db.VarChar(255)
  entity_ldapfilter                       String?   @db.Text
  mailing_signature                       String?   @db.Text
  cartridges_alert_repeat                 Int       @default(-2)
  consumables_alert_repeat                Int       @default(-2)
  use_licenses_alert                      Int       @default(-2)
  send_licenses_alert_before_delay        Int       @default(-2)
  use_certificates_alert                  Int       @default(-2)
  send_certificates_alert_before_delay    Int       @default(-2)
  certificates_alert_repeat_interval      Int       @default(-2)
  use_contracts_alert                     Int       @default(-2)
  send_contracts_alert_before_delay       Int       @default(-2)
  use_infocoms_alert                      Int       @default(-2)
  send_infocoms_alert_before_delay        Int       @default(-2)
  use_reservations_alert                  Int       @default(-2)
  use_domains_alert                       Int       @default(-2)
  send_domains_alert_close_expiries_delay Int       @default(-2)
  send_domains_alert_expired_delay        Int       @default(-2)
  autoclose_delay                         Int       @default(-2)
  autopurge_delay                         Int       @default(-2)
  notclosed_delay                         Int       @default(-2)
  calendars_strategy                      Int       @default(-2) @db.TinyInt
  calendars_id                            Int       @default(0) @db.UnsignedInt
  auto_assign_mode                        Int       @default(-2)
  tickettype                              Int       @default(-2)
  max_closedate                           DateTime? @db.Timestamp(0)
  inquest_config                          Int       @default(-2)
  inquest_rate                            Int       @default(0)
  inquest_delay                           Int       @default(-10)
  inquest_URL                             String?   @db.VarChar(255)
  autofill_warranty_date                  String    @default("-2") @db.VarChar(255)
  autofill_use_date                       String    @default("-2") @db.VarChar(255)
  autofill_buy_date                       String    @default("-2") @db.VarChar(255)
  autofill_delivery_date                  String    @default("-2") @db.VarChar(255)
  autofill_order_date                     String    @default("-2") @db.VarChar(255)
  tickettemplates_strategy                Int       @default(-2) @db.TinyInt
  tickettemplates_id                      Int       @default(0) @db.UnsignedInt
  changetemplates_strategy                Int       @default(-2) @db.TinyInt
  changetemplates_id                      Int       @default(0) @db.UnsignedInt
  problemtemplates_strategy               Int       @default(-2) @db.TinyInt
  problemtemplates_id                     Int       @default(0) @db.UnsignedInt
  entities_strategy_software              Int       @default(-2) @db.TinyInt
  entities_id_software                    Int       @default(0) @db.UnsignedInt
  default_contract_alert                  Int       @default(-2)
  default_infocom_alert                   Int       @default(-2)
  default_cartridges_alarm_threshold      Int       @default(-2)
  default_consumables_alarm_threshold     Int       @default(-2)
  delay_send_emails                       Int       @default(-2)
  is_notif_enable_default                 Int       @default(-2)
  inquest_duration                        Int       @default(0)
  date_mod                                DateTime? @db.Timestamp(0)
  date_creation                           DateTime? @db.Timestamp(0)
  autofill_decommission_date              String    @default("-2") @db.VarChar(255)
  suppliers_as_private                    Int       @default(-2)
  anonymize_support_agents                Int       @default(-2)
  display_users_initials                  Int       @default(-2)
  contracts_strategy_default              Int       @default(-2) @db.TinyInt
  contracts_id_default                    Int       @default(0) @db.UnsignedInt
  enable_custom_css                       Int       @default(-2)
  custom_css_code                         String?   @db.Text
  latitude                                String?   @db.VarChar(255)
  longitude                               String?   @db.VarChar(255)
  altitude                                String?   @db.VarChar(255)
  transfers_strategy                      Int       @default(-2) @db.TinyInt
  transfers_id                            Int       @default(0) @db.UnsignedInt
  agent_base_url                          String?   @db.VarChar(255)

  @@unique([entities_id, name], map: "unicity")
  @@index([authldaps_id], map: "authldaps_id")
  @@index([calendars_id], map: "calendars_id")
  @@index([changetemplates_id], map: "changetemplates_id")
  @@index([contracts_id_default], map: "contracts_id_default")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id_software], map: "entities_id_software")
  @@index([level], map: "level")
  @@index([name], map: "name")
  @@index([problemtemplates_id], map: "problemtemplates_id")
  @@index([tickettemplates_id], map: "tickettemplates_id")
  @@index([transfers_id], map: "transfers_id")
}

model glpi_entities_knowbaseitems {
  id               Int @id @default(autoincrement()) @db.UnsignedInt
  knowbaseitems_id Int @default(0) @db.UnsignedInt
  entities_id      Int @default(0) @db.UnsignedInt
  is_recursive     Int @default(0) @db.TinyInt

  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([knowbaseitems_id], map: "knowbaseitems_id")
}

model glpi_entities_reminders {
  id           Int @id @default(autoincrement()) @db.UnsignedInt
  reminders_id Int @default(0) @db.UnsignedInt
  entities_id  Int @default(0) @db.UnsignedInt
  is_recursive Int @default(0) @db.TinyInt

  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([reminders_id], map: "reminders_id")
}

model glpi_entities_rssfeeds {
  id           Int @id @default(autoincrement()) @db.UnsignedInt
  rssfeeds_id  Int @default(0) @db.UnsignedInt
  entities_id  Int @default(0) @db.UnsignedInt
  is_recursive Int @default(0) @db.TinyInt

  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([rssfeeds_id], map: "rssfeeds_id")
}

model glpi_events {
  id       Int       @id @default(autoincrement()) @db.UnsignedInt
  items_id Int       @default(0) @db.UnsignedInt
  type     String?   @db.VarChar(255)
  date     DateTime? @db.Timestamp(0)
  service  String?   @db.VarChar(255)
  level    Int       @default(0)
  message  String?   @db.Text

  @@index([date], map: "date")
  @@index([type, items_id], map: "item")
  @@index([level], map: "level")
}

model glpi_fieldblacklists {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String    @default("") @db.VarChar(255)
  field         String    @default("") @db.VarChar(255)
  value         String    @default("") @db.VarChar(255)
  itemtype      String    @default("") @db.VarChar(255)
  entities_id   Int       @default(0) @db.UnsignedInt
  is_recursive  Int       @default(0) @db.TinyInt
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_fieldunicities {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String    @default("") @db.VarChar(255)
  is_recursive  Int       @default(0) @db.TinyInt
  itemtype      String    @default("") @db.VarChar(255)
  entities_id   Int       @default(0) @db.UnsignedInt
  fields        String?   @db.Text
  is_active     Int       @default(0) @db.TinyInt
  action_refuse Int       @default(0) @db.TinyInt
  action_notify Int       @default(0) @db.TinyInt
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([is_active], map: "is_active")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
}

model glpi_filesystems {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_fqdns {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id   Int       @default(0) @db.UnsignedInt
  is_recursive  Int       @default(0) @db.TinyInt
  name          String?   @db.VarChar(255)
  fqdn          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([fqdn], map: "fqdn")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
}

model glpi_groups {
  id              Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id     Int       @default(0) @db.UnsignedInt
  is_recursive    Int       @default(0) @db.TinyInt
  name            String?   @db.VarChar(255)
  comment         String?   @db.Text
  ldap_field      String?   @db.VarChar(255)
  ldap_value      String?   @db.Text
  ldap_group_dn   String?   @db.Text
  date_mod        DateTime? @db.Timestamp(0)
  groups_id       Int       @default(0) @db.UnsignedInt
  completename    String?   @db.Text
  level           Int       @default(0)
  ancestors_cache String?   @db.LongText
  sons_cache      String?   @db.LongText
  is_requester    Int       @default(1) @db.TinyInt
  is_watcher      Int       @default(1) @db.TinyInt
  is_assign       Int       @default(1) @db.TinyInt
  is_task         Int       @default(1) @db.TinyInt
  is_notify       Int       @default(1) @db.TinyInt
  is_itemgroup    Int       @default(1) @db.TinyInt
  is_usergroup    Int       @default(1) @db.TinyInt
  is_manager      Int       @default(1) @db.TinyInt
  date_creation   DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([groups_id], map: "groups_id")
  @@index([is_assign], map: "is_assign")
  @@index([is_itemgroup], map: "is_itemgroup")
  @@index([is_manager], map: "is_manager")
  @@index([is_notify], map: "is_notify")
  @@index([is_recursive], map: "is_recursive")
  @@index([is_requester], map: "is_requester")
  @@index([is_usergroup], map: "is_usergroup")
  @@index([is_watcher], map: "is_watcher")
  @@index([ldap_field], map: "ldap_field")
  @@index([ldap_group_dn(length: 200)], map: "ldap_group_dn")
  @@index([ldap_value(length: 200)], map: "ldap_value")
  @@index([level], map: "level")
  @@index([name], map: "name")
}

model glpi_groups_knowbaseitems {
  id                    Int  @id @default(autoincrement()) @db.UnsignedInt
  knowbaseitems_id      Int  @default(0) @db.UnsignedInt
  groups_id             Int  @default(0) @db.UnsignedInt
  entities_id           Int? @db.UnsignedInt
  is_recursive          Int  @default(0) @db.TinyInt
  no_entity_restriction Int  @default(0) @db.TinyInt

  @@index([entities_id], map: "entities_id")
  @@index([groups_id], map: "groups_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([knowbaseitems_id], map: "knowbaseitems_id")
}

model glpi_groups_problems {
  id          Int @id @default(autoincrement()) @db.UnsignedInt
  problems_id Int @default(0) @db.UnsignedInt
  groups_id   Int @default(0) @db.UnsignedInt
  type        Int @default(1)

  @@unique([problems_id, type, groups_id], map: "unicity")
  @@index([groups_id, type], map: "group")
}

model glpi_groups_reminders {
  id                    Int  @id @default(autoincrement()) @db.UnsignedInt
  reminders_id          Int  @default(0) @db.UnsignedInt
  groups_id             Int  @default(0) @db.UnsignedInt
  entities_id           Int? @db.UnsignedInt
  is_recursive          Int  @default(0) @db.TinyInt
  no_entity_restriction Int  @default(0) @db.TinyInt

  @@index([entities_id], map: "entities_id")
  @@index([groups_id], map: "groups_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([reminders_id], map: "reminders_id")
}

model glpi_groups_rssfeeds {
  id                    Int  @id @default(autoincrement()) @db.UnsignedInt
  rssfeeds_id           Int  @default(0) @db.UnsignedInt
  groups_id             Int  @default(0) @db.UnsignedInt
  entities_id           Int? @db.UnsignedInt
  is_recursive          Int  @default(0) @db.TinyInt
  no_entity_restriction Int  @default(0) @db.TinyInt

  @@index([entities_id], map: "entities_id")
  @@index([groups_id], map: "groups_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([rssfeeds_id], map: "rssfeeds_id")
}

model glpi_groups_tickets {
  id         Int @id @default(autoincrement()) @db.UnsignedInt
  tickets_id Int @default(0) @db.UnsignedInt
  groups_id  Int @default(0) @db.UnsignedInt
  type       Int @default(1)

  @@unique([tickets_id, type, groups_id], map: "unicity")
  @@index([groups_id, type], map: "group")
}

model glpi_groups_users {
  id              Int @id @default(autoincrement()) @db.UnsignedInt
  users_id        Int @default(0) @db.UnsignedInt
  groups_id       Int @default(0) @db.UnsignedInt
  is_dynamic      Int @default(0) @db.TinyInt
  is_manager      Int @default(0) @db.TinyInt
  is_userdelegate Int @default(0) @db.TinyInt

  @@unique([users_id, groups_id], map: "unicity")
  @@index([groups_id], map: "groups_id")
  @@index([is_dynamic], map: "is_dynamic")
  @@index([is_manager], map: "is_manager")
  @@index([is_userdelegate], map: "is_userdelegate")
}

model glpi_holidays {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  entities_id   Int       @default(0) @db.UnsignedInt
  is_recursive  Int       @default(0) @db.TinyInt
  comment       String?   @db.Text
  begin_date    DateTime? @db.Date
  end_date      DateTime? @db.Date
  is_perpetual  Int       @default(0) @db.TinyInt
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([begin_date], map: "begin_date")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([end_date], map: "end_date")
  @@index([entities_id], map: "entities_id")
  @@index([is_perpetual], map: "is_perpetual")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
}

model glpi_imageformats {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @unique(map: "unicity") @db.VarChar(255)
  date_mod      DateTime? @db.Timestamp(0)
  comment       String?   @db.Text
  date_creation DateTime? @db.Timestamp(0)
  entities_id   Int       @default(0) @db.UnsignedInt
  is_recursive  Int       @default(0) @db.TinyInt

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
}

model glpi_imageresolutions {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @unique(map: "unicity") @db.VarChar(255)
  is_video      Int       @default(0) @db.TinyInt
  date_mod      DateTime? @db.Timestamp(0)
  comment       String?   @db.Text
  date_creation DateTime? @db.Timestamp(0)
  entities_id   Int       @default(0) @db.UnsignedInt
  is_recursive  Int       @default(0) @db.TinyInt

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([is_video], map: "is_video")
}

model glpi_impactcompounds {
  id    Int     @id @default(autoincrement()) @db.UnsignedInt
  name  String? @default("") @db.VarChar(255)
  color String  @default("") @db.VarChar(255)

  @@index([name], map: "name")
}

model glpi_impactcontexts {
  id                       Int    @id @default(autoincrement()) @db.UnsignedInt
  positions                String @db.MediumText
  zoom                     Float  @default(0) @db.Float
  pan_x                    Float  @default(0) @db.Float
  pan_y                    Float  @default(0) @db.Float
  impact_color             String @default("") @db.VarChar(255)
  depends_color            String @default("") @db.VarChar(255)
  impact_and_depends_color String @default("") @db.VarChar(255)
  show_depends             Int    @default(1) @db.TinyInt
  show_impact              Int    @default(1) @db.TinyInt
  max_depth                Int    @default(5)
}

model glpi_impactitems {
  id                Int    @id @default(autoincrement()) @db.UnsignedInt
  itemtype          String @default("") @db.VarChar(255)
  items_id          Int    @default(0) @db.UnsignedInt
  parent_id         Int    @default(0) @db.UnsignedInt
  impactcontexts_id Int    @default(0) @db.UnsignedInt
  is_slave          Int    @default(1) @db.TinyInt

  @@unique([itemtype, items_id], map: "unicity")
  @@index([impactcontexts_id], map: "impactcontexts_id")
  @@index([parent_id], map: "parent_id")
  @@index([itemtype, items_id], map: "source")
}

model glpi_impactrelations {
  id                Int    @id @default(autoincrement()) @db.UnsignedInt
  itemtype_source   String @default("") @db.VarChar(255)
  items_id_source   Int    @default(0) @db.UnsignedInt
  itemtype_impacted String @default("") @db.VarChar(255)
  items_id_impacted Int    @default(0) @db.UnsignedInt

  @@unique([itemtype_source, items_id_source, itemtype_impacted, items_id_impacted], map: "unicity")
  @@index([itemtype_impacted, items_id_impacted], map: "impacted_asset")
}

model glpi_infocoms {
  id                     Int       @id @default(autoincrement()) @db.UnsignedInt
  items_id               Int       @default(0) @db.UnsignedInt
  itemtype               String    @db.VarChar(100)
  entities_id            Int       @default(0) @db.UnsignedInt
  is_recursive           Int       @default(0) @db.TinyInt
  buy_date               DateTime? @db.Date
  use_date               DateTime? @db.Date
  warranty_duration      Int       @default(0)
  warranty_info          String?   @db.VarChar(255)
  suppliers_id           Int       @default(0) @db.UnsignedInt
  order_number           String?   @db.VarChar(255)
  delivery_number        String?   @db.VarChar(255)
  immo_number            String?   @db.VarChar(255)
  value                  Decimal   @default(0.0000) @db.Decimal(20, 4)
  warranty_value         Decimal   @default(0.0000) @db.Decimal(20, 4)
  sink_time              Int       @default(0)
  sink_type              Int       @default(0)
  sink_coeff             Float     @default(0) @db.Float
  comment                String?   @db.Text
  bill                   String?   @db.VarChar(255)
  budgets_id             Int       @default(0) @db.UnsignedInt
  alert                  Int       @default(0)
  order_date             DateTime? @db.Date
  delivery_date          DateTime? @db.Date
  inventory_date         DateTime? @db.Date
  warranty_date          DateTime? @db.Date
  date_mod               DateTime? @db.Timestamp(0)
  date_creation          DateTime? @db.Timestamp(0)
  decommission_date      DateTime? @db.Timestamp(0)
  businesscriticities_id Int       @default(0) @db.UnsignedInt

  @@unique([itemtype, items_id], map: "unicity")
  @@index([alert], map: "alert")
  @@index([budgets_id], map: "budgets_id")
  @@index([businesscriticities_id], map: "businesscriticities_id")
  @@index([buy_date], map: "buy_date")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([suppliers_id], map: "suppliers_id")
}

model glpi_interfacetypes {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_ipaddresses {
  id           Int     @id @default(autoincrement()) @db.UnsignedInt
  entities_id  Int     @default(0) @db.UnsignedInt
  items_id     Int     @default(0) @db.UnsignedInt
  itemtype     String  @db.VarChar(100)
  version      Int?    @default(0) @db.UnsignedTinyInt
  name         String? @db.VarChar(255)
  binary_0     Int     @default(0) @db.UnsignedInt
  binary_1     Int     @default(0) @db.UnsignedInt
  binary_2     Int     @default(0) @db.UnsignedInt
  binary_3     Int     @default(0) @db.UnsignedInt
  is_deleted   Int     @default(0) @db.TinyInt
  is_dynamic   Int     @default(0) @db.TinyInt
  mainitems_id Int     @default(0) @db.UnsignedInt
  mainitemtype String? @db.VarChar(255)

  @@index([binary_0, binary_1, binary_2, binary_3], map: "binary")
  @@index([entities_id], map: "entities_id")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_dynamic], map: "is_dynamic")
  @@index([itemtype, items_id, is_deleted], map: "item")
  @@index([mainitemtype, mainitems_id, is_deleted], map: "mainitem")
  @@index([name], map: "name")
}

model glpi_ipaddresses_ipnetworks {
  id             Int @id @default(autoincrement()) @db.UnsignedInt
  ipaddresses_id Int @default(0) @db.UnsignedInt
  ipnetworks_id  Int @default(0) @db.UnsignedInt

  @@unique([ipaddresses_id, ipnetworks_id], map: "unicity")
  @@index([ipnetworks_id], map: "ipnetworks_id")
}

model glpi_ipnetworks {
  id              Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id     Int       @default(0) @db.UnsignedInt
  is_recursive    Int       @default(0) @db.TinyInt
  ipnetworks_id   Int       @default(0) @db.UnsignedInt
  completename    String?   @db.Text
  level           Int       @default(0)
  ancestors_cache String?   @db.LongText
  sons_cache      String?   @db.LongText
  addressable     Int       @default(0) @db.TinyInt
  version         Int?      @default(0) @db.UnsignedTinyInt
  name            String?   @db.VarChar(255)
  address         String?   @db.VarChar(40)
  address_0       Int       @default(0) @db.UnsignedInt
  address_1       Int       @default(0) @db.UnsignedInt
  address_2       Int       @default(0) @db.UnsignedInt
  address_3       Int       @default(0) @db.UnsignedInt
  netmask         String?   @db.VarChar(40)
  netmask_0       Int       @default(0) @db.UnsignedInt
  netmask_1       Int       @default(0) @db.UnsignedInt
  netmask_2       Int       @default(0) @db.UnsignedInt
  netmask_3       Int       @default(0) @db.UnsignedInt
  gateway         String?   @db.VarChar(40)
  gateway_0       Int       @default(0) @db.UnsignedInt
  gateway_1       Int       @default(0) @db.UnsignedInt
  gateway_2       Int       @default(0) @db.UnsignedInt
  gateway_3       Int       @default(0) @db.UnsignedInt
  comment         String?   @db.Text
  date_mod        DateTime? @db.Timestamp(0)
  date_creation   DateTime? @db.Timestamp(0)

  @@index([address_0, address_1, address_2, address_3], map: "address")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([gateway_0, gateway_1, gateway_2, gateway_3], map: "gateway")
  @@index([ipnetworks_id], map: "ipnetworks_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([level], map: "level")
  @@index([name], map: "name")
  @@index([netmask_0, netmask_1, netmask_2, netmask_3], map: "netmask")
  @@index([entities_id, address, netmask], map: "network_definition")
}

model glpi_ipnetworks_vlans {
  id            Int @id @default(autoincrement()) @db.UnsignedInt
  ipnetworks_id Int @default(0) @db.UnsignedInt
  vlans_id      Int @default(0) @db.UnsignedInt

  @@unique([ipnetworks_id, vlans_id], map: "link")
  @@index([vlans_id], map: "vlans_id")
}

model glpi_items_clusters {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  clusters_id Int     @default(0) @db.UnsignedInt
  itemtype    String? @db.VarChar(100)
  items_id    Int     @default(0) @db.UnsignedInt

  @@unique([clusters_id, itemtype, items_id], map: "unicity")
  @@index([itemtype, items_id], map: "item")
}

model glpi_items_devicebatteries {
  id                 Int       @id @default(autoincrement()) @db.UnsignedInt
  items_id           Int       @default(0) @db.UnsignedInt
  itemtype           String?   @db.VarChar(255)
  devicebatteries_id Int       @default(0) @db.UnsignedInt
  manufacturing_date DateTime? @db.Date
  is_deleted         Int       @default(0) @db.TinyInt
  is_dynamic         Int       @default(0) @db.TinyInt
  entities_id        Int       @default(0) @db.UnsignedInt
  is_recursive       Int       @default(0) @db.TinyInt
  serial             String?   @db.VarChar(255)
  otherserial        String?   @db.VarChar(255)
  locations_id       Int       @default(0) @db.UnsignedInt
  states_id          Int       @default(0) @db.UnsignedInt
  real_capacity      Int       @default(0)

  @@index([devicebatteries_id], map: "devicebatteries_id")
  @@index([entities_id], map: "entities_id")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_dynamic], map: "is_dynamic")
  @@index([is_recursive], map: "is_recursive")
  @@index([itemtype, items_id], map: "item")
  @@index([locations_id], map: "locations_id")
  @@index([otherserial], map: "otherserial")
  @@index([serial], map: "serial")
  @@index([states_id], map: "states_id")
}

model glpi_items_devicecameras {
  id               Int     @id @default(autoincrement()) @db.UnsignedInt
  items_id         Int     @default(0) @db.UnsignedInt
  itemtype         String? @db.VarChar(255)
  devicecameras_id Int     @default(0) @db.UnsignedInt
  is_deleted       Int     @default(0) @db.TinyInt
  is_dynamic       Int     @default(0) @db.TinyInt
  entities_id      Int     @default(0) @db.UnsignedInt
  is_recursive     Int     @default(0) @db.TinyInt
  locations_id     Int     @default(0) @db.UnsignedInt

  @@index([devicecameras_id], map: "devicecameras_id")
  @@index([entities_id], map: "entities_id")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_dynamic], map: "is_dynamic")
  @@index([is_recursive], map: "is_recursive")
  @@index([itemtype, items_id], map: "item")
  @@index([items_id], map: "items_id")
  @@index([locations_id], map: "locations_id")
}

model glpi_items_devicecameras_imageformats {
  id                     Int @id @default(autoincrement()) @db.UnsignedInt
  items_devicecameras_id Int @default(0) @db.UnsignedInt
  imageformats_id        Int @default(0) @db.UnsignedInt
  is_dynamic             Int @default(0) @db.TinyInt
  is_deleted             Int @default(0) @db.TinyInt

  @@index([imageformats_id], map: "imageformats_id")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_dynamic], map: "is_dynamic")
  @@index([items_devicecameras_id], map: "items_devicecameras_id")
}

model glpi_items_devicecameras_imageresolutions {
  id                     Int @id @default(autoincrement()) @db.UnsignedInt
  items_devicecameras_id Int @default(0) @db.UnsignedInt
  imageresolutions_id    Int @default(0) @db.UnsignedInt
  is_dynamic             Int @default(0) @db.TinyInt
  is_deleted             Int @default(0) @db.TinyInt

  @@index([imageresolutions_id], map: "imageresolutions_id")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_dynamic], map: "is_dynamic")
  @@index([items_devicecameras_id], map: "items_devicecameras_id")
}

model glpi_items_devicecases {
  id             Int     @id @default(autoincrement()) @db.UnsignedInt
  items_id       Int     @default(0) @db.UnsignedInt
  itemtype       String? @db.VarChar(255)
  devicecases_id Int     @default(0) @db.UnsignedInt
  is_deleted     Int     @default(0) @db.TinyInt
  is_dynamic     Int     @default(0) @db.TinyInt
  entities_id    Int     @default(0) @db.UnsignedInt
  is_recursive   Int     @default(0) @db.TinyInt
  serial         String? @db.VarChar(255)
  otherserial    String? @db.VarChar(255)
  locations_id   Int     @default(0) @db.UnsignedInt
  states_id      Int     @default(0) @db.UnsignedInt

  @@index([devicecases_id], map: "devicecases_id")
  @@index([entities_id], map: "entities_id")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_dynamic], map: "is_dynamic")
  @@index([is_recursive], map: "is_recursive")
  @@index([itemtype, items_id], map: "item")
  @@index([locations_id], map: "locations_id")
  @@index([otherserial], map: "otherserial")
  @@index([serial], map: "serial")
  @@index([states_id], map: "states_id")
}

model glpi_items_devicecontrols {
  id                Int     @id @default(autoincrement()) @db.UnsignedInt
  items_id          Int     @default(0) @db.UnsignedInt
  itemtype          String? @db.VarChar(255)
  devicecontrols_id Int     @default(0) @db.UnsignedInt
  is_deleted        Int     @default(0) @db.TinyInt
  is_dynamic        Int     @default(0) @db.TinyInt
  entities_id       Int     @default(0) @db.UnsignedInt
  is_recursive      Int     @default(0) @db.TinyInt
  serial            String? @db.VarChar(255)
  busID             String? @db.VarChar(255)
  otherserial       String? @db.VarChar(255)
  locations_id      Int     @default(0) @db.UnsignedInt
  states_id         Int     @default(0) @db.UnsignedInt

  @@index([busID], map: "busID")
  @@index([devicecontrols_id], map: "devicecontrols_id")
  @@index([entities_id], map: "entities_id")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_dynamic], map: "is_dynamic")
  @@index([is_recursive], map: "is_recursive")
  @@index([itemtype, items_id], map: "item")
  @@index([locations_id], map: "locations_id")
  @@index([otherserial], map: "otherserial")
  @@index([serial], map: "serial")
  @@index([states_id], map: "states_id")
}

model glpi_items_devicedrives {
  id              Int     @id @default(autoincrement()) @db.UnsignedInt
  items_id        Int     @default(0) @db.UnsignedInt
  itemtype        String? @db.VarChar(255)
  devicedrives_id Int     @default(0) @db.UnsignedInt
  is_deleted      Int     @default(0) @db.TinyInt
  is_dynamic      Int     @default(0) @db.TinyInt
  entities_id     Int     @default(0) @db.UnsignedInt
  is_recursive    Int     @default(0) @db.TinyInt
  serial          String? @db.VarChar(255)
  busID           String? @db.VarChar(255)
  otherserial     String? @db.VarChar(255)
  locations_id    Int     @default(0) @db.UnsignedInt
  states_id       Int     @default(0) @db.UnsignedInt

  @@index([busID], map: "busID")
  @@index([devicedrives_id], map: "devicedrives_id")
  @@index([entities_id], map: "entities_id")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_dynamic], map: "is_dynamic")
  @@index([is_recursive], map: "is_recursive")
  @@index([itemtype, items_id], map: "item")
  @@index([locations_id], map: "locations_id")
  @@index([otherserial], map: "otherserial")
  @@index([serial], map: "serial")
  @@index([states_id], map: "states_id")
}

model glpi_items_devicefirmwares {
  id                 Int     @id @default(autoincrement()) @db.UnsignedInt
  items_id           Int     @default(0) @db.UnsignedInt
  itemtype           String? @db.VarChar(255)
  devicefirmwares_id Int     @default(0) @db.UnsignedInt
  is_deleted         Int     @default(0) @db.TinyInt
  is_dynamic         Int     @default(0) @db.TinyInt
  entities_id        Int     @default(0) @db.UnsignedInt
  is_recursive       Int     @default(0) @db.TinyInt
  serial             String? @db.VarChar(255)
  otherserial        String? @db.VarChar(255)
  locations_id       Int     @default(0) @db.UnsignedInt
  states_id          Int     @default(0) @db.UnsignedInt

  @@index([devicefirmwares_id], map: "devicefirmwares_id")
  @@index([entities_id], map: "entities_id")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_dynamic], map: "is_dynamic")
  @@index([is_recursive], map: "is_recursive")
  @@index([itemtype, items_id], map: "item")
  @@index([locations_id], map: "locations_id")
  @@index([otherserial], map: "otherserial")
  @@index([serial], map: "serial")
  @@index([states_id], map: "states_id")
}

model glpi_items_devicegenerics {
  id                Int     @id @default(autoincrement()) @db.UnsignedInt
  items_id          Int     @default(0) @db.UnsignedInt
  itemtype          String? @db.VarChar(255)
  devicegenerics_id Int     @default(0) @db.UnsignedInt
  is_deleted        Int     @default(0) @db.TinyInt
  is_dynamic        Int     @default(0) @db.TinyInt
  entities_id       Int     @default(0) @db.UnsignedInt
  is_recursive      Int     @default(0) @db.TinyInt
  serial            String? @db.VarChar(255)
  otherserial       String? @db.VarChar(255)
  locations_id      Int     @default(0) @db.UnsignedInt
  states_id         Int     @default(0) @db.UnsignedInt

  @@index([devicegenerics_id], map: "devicegenerics_id")
  @@index([entities_id], map: "entities_id")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_dynamic], map: "is_dynamic")
  @@index([is_recursive], map: "is_recursive")
  @@index([itemtype, items_id], map: "item")
  @@index([locations_id], map: "locations_id")
  @@index([otherserial], map: "otherserial")
  @@index([serial], map: "serial")
  @@index([states_id], map: "states_id")
}

model glpi_items_devicegraphiccards {
  id                    Int     @id @default(autoincrement()) @db.UnsignedInt
  items_id              Int     @default(0) @db.UnsignedInt
  itemtype              String? @db.VarChar(255)
  devicegraphiccards_id Int     @default(0) @db.UnsignedInt
  memory                Int     @default(0)
  is_deleted            Int     @default(0) @db.TinyInt
  is_dynamic            Int     @default(0) @db.TinyInt
  entities_id           Int     @default(0) @db.UnsignedInt
  is_recursive          Int     @default(0) @db.TinyInt
  serial                String? @db.VarChar(255)
  busID                 String? @db.VarChar(255)
  otherserial           String? @db.VarChar(255)
  locations_id          Int     @default(0) @db.UnsignedInt
  states_id             Int     @default(0) @db.UnsignedInt

  @@index([busID], map: "busID")
  @@index([devicegraphiccards_id], map: "devicegraphiccards_id")
  @@index([entities_id], map: "entities_id")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_dynamic], map: "is_dynamic")
  @@index([is_recursive], map: "is_recursive")
  @@index([itemtype, items_id], map: "item")
  @@index([locations_id], map: "locations_id")
  @@index([otherserial], map: "otherserial")
  @@index([serial], map: "serial")
  @@index([memory], map: "specificity")
  @@index([states_id], map: "states_id")
}

model glpi_items_deviceharddrives {
  id                  Int     @id @default(autoincrement()) @db.UnsignedInt
  items_id            Int     @default(0) @db.UnsignedInt
  itemtype            String? @db.VarChar(255)
  deviceharddrives_id Int     @default(0) @db.UnsignedInt
  capacity            Int     @default(0)
  serial              String? @db.VarChar(255)
  is_deleted          Int     @default(0) @db.TinyInt
  is_dynamic          Int     @default(0) @db.TinyInt
  entities_id         Int     @default(0) @db.UnsignedInt
  is_recursive        Int     @default(0) @db.TinyInt
  busID               String? @db.VarChar(255)
  otherserial         String? @db.VarChar(255)
  locations_id        Int     @default(0) @db.UnsignedInt
  states_id           Int     @default(0) @db.UnsignedInt

  @@index([busID], map: "busID")
  @@index([deviceharddrives_id], map: "deviceharddrives_id")
  @@index([entities_id], map: "entities_id")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_dynamic], map: "is_dynamic")
  @@index([is_recursive], map: "is_recursive")
  @@index([itemtype, items_id], map: "item")
  @@index([locations_id], map: "locations_id")
  @@index([otherserial], map: "otherserial")
  @@index([serial], map: "serial")
  @@index([capacity], map: "specificity")
  @@index([states_id], map: "states_id")
}

model glpi_items_devicememories {
  id                Int     @id @default(autoincrement()) @db.UnsignedInt
  items_id          Int     @default(0) @db.UnsignedInt
  itemtype          String? @db.VarChar(255)
  devicememories_id Int     @default(0) @db.UnsignedInt
  size              Int     @default(0)
  serial            String? @db.VarChar(255)
  is_deleted        Int     @default(0) @db.TinyInt
  is_dynamic        Int     @default(0) @db.TinyInt
  entities_id       Int     @default(0) @db.UnsignedInt
  is_recursive      Int     @default(0) @db.TinyInt
  busID             String? @db.VarChar(255)
  otherserial       String? @db.VarChar(255)
  locations_id      Int     @default(0) @db.UnsignedInt
  states_id         Int     @default(0) @db.UnsignedInt

  @@index([busID], map: "busID")
  @@index([devicememories_id], map: "devicememories_id")
  @@index([entities_id], map: "entities_id")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_dynamic], map: "is_dynamic")
  @@index([is_recursive], map: "is_recursive")
  @@index([itemtype, items_id], map: "item")
  @@index([locations_id], map: "locations_id")
  @@index([otherserial], map: "otherserial")
  @@index([serial], map: "serial")
  @@index([size], map: "specificity")
  @@index([states_id], map: "states_id")
}

model glpi_items_devicemotherboards {
  id                    Int     @id @default(autoincrement()) @db.UnsignedInt
  items_id              Int     @default(0) @db.UnsignedInt
  itemtype              String? @db.VarChar(255)
  devicemotherboards_id Int     @default(0) @db.UnsignedInt
  is_deleted            Int     @default(0) @db.TinyInt
  is_dynamic            Int     @default(0) @db.TinyInt
  entities_id           Int     @default(0) @db.UnsignedInt
  is_recursive          Int     @default(0) @db.TinyInt
  serial                String? @db.VarChar(255)
  otherserial           String? @db.VarChar(255)
  locations_id          Int     @default(0) @db.UnsignedInt
  states_id             Int     @default(0) @db.UnsignedInt

  @@index([devicemotherboards_id], map: "devicemotherboards_id")
  @@index([entities_id], map: "entities_id")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_dynamic], map: "is_dynamic")
  @@index([is_recursive], map: "is_recursive")
  @@index([itemtype, items_id], map: "item")
  @@index([locations_id], map: "locations_id")
  @@index([otherserial], map: "otherserial")
  @@index([serial], map: "serial")
  @@index([states_id], map: "states_id")
}

model glpi_items_devicenetworkcards {
  id                    Int     @id @default(autoincrement()) @db.UnsignedInt
  items_id              Int     @default(0) @db.UnsignedInt
  itemtype              String? @db.VarChar(255)
  devicenetworkcards_id Int     @default(0) @db.UnsignedInt
  mac                   String? @db.VarChar(255)
  is_deleted            Int     @default(0) @db.TinyInt
  is_dynamic            Int     @default(0) @db.TinyInt
  entities_id           Int     @default(0) @db.UnsignedInt
  is_recursive          Int     @default(0) @db.TinyInt
  serial                String? @db.VarChar(255)
  busID                 String? @db.VarChar(255)
  otherserial           String? @db.VarChar(255)
  locations_id          Int     @default(0) @db.UnsignedInt
  states_id             Int     @default(0) @db.UnsignedInt

  @@index([busID], map: "busID")
  @@index([devicenetworkcards_id], map: "devicenetworkcards_id")
  @@index([entities_id], map: "entities_id")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_dynamic], map: "is_dynamic")
  @@index([is_recursive], map: "is_recursive")
  @@index([itemtype, items_id], map: "item")
  @@index([locations_id], map: "locations_id")
  @@index([otherserial], map: "otherserial")
  @@index([serial], map: "serial")
  @@index([mac], map: "specificity")
  @@index([states_id], map: "states_id")
}

model glpi_items_devicepcis {
  id            Int     @id @default(autoincrement()) @db.UnsignedInt
  items_id      Int     @default(0) @db.UnsignedInt
  itemtype      String? @db.VarChar(255)
  devicepcis_id Int     @default(0) @db.UnsignedInt
  is_deleted    Int     @default(0) @db.TinyInt
  is_dynamic    Int     @default(0) @db.TinyInt
  entities_id   Int     @default(0) @db.UnsignedInt
  is_recursive  Int     @default(0) @db.TinyInt
  serial        String? @db.VarChar(255)
  busID         String? @db.VarChar(255)
  otherserial   String? @db.VarChar(255)
  locations_id  Int     @default(0) @db.UnsignedInt
  states_id     Int     @default(0) @db.UnsignedInt

  @@index([busID], map: "busID")
  @@index([devicepcis_id], map: "devicepcis_id")
  @@index([entities_id], map: "entities_id")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_dynamic], map: "is_dynamic")
  @@index([is_recursive], map: "is_recursive")
  @@index([itemtype, items_id], map: "item")
  @@index([locations_id], map: "locations_id")
  @@index([otherserial], map: "otherserial")
  @@index([serial], map: "serial")
  @@index([states_id], map: "states_id")
}

model glpi_items_devicepowersupplies {
  id                     Int     @id @default(autoincrement()) @db.UnsignedInt
  items_id               Int     @default(0) @db.UnsignedInt
  itemtype               String? @db.VarChar(255)
  devicepowersupplies_id Int     @default(0) @db.UnsignedInt
  is_deleted             Int     @default(0) @db.TinyInt
  is_dynamic             Int     @default(0) @db.TinyInt
  entities_id            Int     @default(0) @db.UnsignedInt
  is_recursive           Int     @default(0) @db.TinyInt
  serial                 String? @db.VarChar(255)
  otherserial            String? @db.VarChar(255)
  locations_id           Int     @default(0) @db.UnsignedInt
  states_id              Int     @default(0) @db.UnsignedInt

  @@index([devicepowersupplies_id], map: "devicepowersupplies_id")
  @@index([entities_id], map: "entities_id")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_dynamic], map: "is_dynamic")
  @@index([is_recursive], map: "is_recursive")
  @@index([itemtype, items_id], map: "item")
  @@index([locations_id], map: "locations_id")
  @@index([otherserial], map: "otherserial")
  @@index([serial], map: "serial")
  @@index([states_id], map: "states_id")
}

model glpi_items_deviceprocessors {
  id                  Int     @id @default(autoincrement()) @db.UnsignedInt
  items_id            Int     @default(0) @db.UnsignedInt
  itemtype            String? @db.VarChar(255)
  deviceprocessors_id Int     @default(0) @db.UnsignedInt
  frequency           Int     @default(0)
  serial              String? @db.VarChar(255)
  is_deleted          Int     @default(0) @db.TinyInt
  is_dynamic          Int     @default(0) @db.TinyInt
  nbcores             Int?
  nbthreads           Int?
  entities_id         Int     @default(0) @db.UnsignedInt
  is_recursive        Int     @default(0) @db.TinyInt
  busID               String? @db.VarChar(255)
  otherserial         String? @db.VarChar(255)
  locations_id        Int     @default(0) @db.UnsignedInt
  states_id           Int     @default(0) @db.UnsignedInt

  @@index([busID], map: "busID")
  @@index([deviceprocessors_id], map: "deviceprocessors_id")
  @@index([entities_id], map: "entities_id")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_dynamic], map: "is_dynamic")
  @@index([is_recursive], map: "is_recursive")
  @@index([itemtype, items_id], map: "item")
  @@index([locations_id], map: "locations_id")
  @@index([nbcores], map: "nbcores")
  @@index([nbthreads], map: "nbthreads")
  @@index([otherserial], map: "otherserial")
  @@index([serial], map: "serial")
  @@index([frequency], map: "specificity")
  @@index([states_id], map: "states_id")
}

model glpi_items_devicesensors {
  id               Int     @id @default(autoincrement()) @db.UnsignedInt
  items_id         Int     @default(0) @db.UnsignedInt
  itemtype         String? @db.VarChar(255)
  devicesensors_id Int     @default(0) @db.UnsignedInt
  is_deleted       Int     @default(0) @db.TinyInt
  is_dynamic       Int     @default(0) @db.TinyInt
  entities_id      Int     @default(0) @db.UnsignedInt
  is_recursive     Int     @default(0) @db.TinyInt
  serial           String? @db.VarChar(255)
  otherserial      String? @db.VarChar(255)
  locations_id     Int     @default(0) @db.UnsignedInt
  states_id        Int     @default(0) @db.UnsignedInt

  @@index([devicesensors_id], map: "devicesensors_id")
  @@index([entities_id], map: "entities_id")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_dynamic], map: "is_dynamic")
  @@index([is_recursive], map: "is_recursive")
  @@index([itemtype, items_id], map: "item")
  @@index([locations_id], map: "locations_id")
  @@index([otherserial], map: "otherserial")
  @@index([serial], map: "serial")
  @@index([states_id], map: "states_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_items_devicesimcards {
  id                Int     @id @default(autoincrement()) @db.UnsignedInt
  items_id          Int     @default(0) @db.UnsignedInt
  itemtype          String  @db.VarChar(100)
  devicesimcards_id Int     @default(0) @db.UnsignedInt
  is_deleted        Int     @default(0) @db.TinyInt
  is_dynamic        Int     @default(0) @db.TinyInt
  entities_id       Int     @default(0) @db.UnsignedInt
  is_recursive      Int     @default(0) @db.TinyInt
  serial            String? @db.VarChar(255)
  otherserial       String? @db.VarChar(255)
  states_id         Int     @default(0) @db.UnsignedInt
  locations_id      Int     @default(0) @db.UnsignedInt
  lines_id          Int     @default(0) @db.UnsignedInt
  users_id          Int     @default(0) @db.UnsignedInt
  groups_id         Int     @default(0) @db.UnsignedInt
  pin               String  @default("") @db.VarChar(255)
  pin2              String  @default("") @db.VarChar(255)
  puk               String  @default("") @db.VarChar(255)
  puk2              String  @default("") @db.VarChar(255)
  msin              String  @default("") @db.VarChar(255)
  comment           String? @db.Text

  @@index([devicesimcards_id], map: "devicesimcards_id")
  @@index([entities_id], map: "entities_id")
  @@index([groups_id], map: "groups_id")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_dynamic], map: "is_dynamic")
  @@index([is_recursive], map: "is_recursive")
  @@index([itemtype, items_id], map: "item")
  @@index([lines_id], map: "lines_id")
  @@index([locations_id], map: "locations_id")
  @@index([otherserial], map: "otherserial")
  @@index([serial], map: "serial")
  @@index([states_id], map: "states_id")
  @@index([users_id], map: "users_id")
}

model glpi_items_devicesoundcards {
  id                  Int     @id @default(autoincrement()) @db.UnsignedInt
  items_id            Int     @default(0) @db.UnsignedInt
  itemtype            String? @db.VarChar(255)
  devicesoundcards_id Int     @default(0) @db.UnsignedInt
  is_deleted          Int     @default(0) @db.TinyInt
  is_dynamic          Int     @default(0) @db.TinyInt
  entities_id         Int     @default(0) @db.UnsignedInt
  is_recursive        Int     @default(0) @db.TinyInt
  serial              String? @db.VarChar(255)
  busID               String? @db.VarChar(255)
  otherserial         String? @db.VarChar(255)
  locations_id        Int     @default(0) @db.UnsignedInt
  states_id           Int     @default(0) @db.UnsignedInt

  @@index([busID], map: "busID")
  @@index([devicesoundcards_id], map: "devicesoundcards_id")
  @@index([entities_id], map: "entities_id")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_dynamic], map: "is_dynamic")
  @@index([is_recursive], map: "is_recursive")
  @@index([itemtype, items_id], map: "item")
  @@index([locations_id], map: "locations_id")
  @@index([otherserial], map: "otherserial")
  @@index([serial], map: "serial")
  @@index([states_id], map: "states_id")
}

model glpi_items_disks {
  id                   Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id          Int       @default(0) @db.UnsignedInt
  itemtype             String?   @db.VarChar(255)
  items_id             Int       @default(0) @db.UnsignedInt
  name                 String?   @db.VarChar(255)
  device               String?   @db.VarChar(255)
  mountpoint           String?   @db.VarChar(255)
  filesystems_id       Int       @default(0) @db.UnsignedInt
  totalsize            BigInt    @default(0)
  freesize             BigInt    @default(0)
  is_deleted           Int       @default(0) @db.TinyInt
  is_dynamic           Int       @default(0) @db.TinyInt
  encryption_status    Int       @default(0)
  encryption_tool      String?   @db.VarChar(255)
  encryption_algorithm String?   @db.VarChar(255)
  encryption_type      String?   @db.VarChar(255)
  date_mod             DateTime? @db.Timestamp(0)
  date_creation        DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([device], map: "device")
  @@index([entities_id], map: "entities_id")
  @@index([filesystems_id], map: "filesystems_id")
  @@index([freesize], map: "freesize")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_dynamic], map: "is_dynamic")
  @@index([itemtype, items_id], map: "item")
  @@index([mountpoint], map: "mountpoint")
  @@index([name], map: "name")
  @@index([totalsize], map: "totalsize")
}

model glpi_items_enclosures {
  id            Int    @id @default(autoincrement()) @db.UnsignedInt
  enclosures_id Int    @db.UnsignedInt
  itemtype      String @db.VarChar(255)
  items_id      Int    @db.UnsignedInt
  position      Int

  @@unique([itemtype, items_id], map: "item")
  @@index([enclosures_id, itemtype, items_id], map: "relation")
}

model glpi_items_kanbans {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  itemtype      String    @db.VarChar(100)
  items_id      Int?      @db.UnsignedInt
  users_id      Int       @db.UnsignedInt
  state         String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@unique([itemtype, items_id, users_id], map: "unicity")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([users_id], map: "users_id")
}

model glpi_items_operatingsystems {
  id                               Int       @id @default(autoincrement()) @db.UnsignedInt
  items_id                         Int       @default(0) @db.UnsignedInt
  itemtype                         String?   @db.VarChar(255)
  operatingsystems_id              Int       @default(0) @db.UnsignedInt
  operatingsystemversions_id       Int       @default(0) @db.UnsignedInt
  operatingsystemservicepacks_id   Int       @default(0) @db.UnsignedInt
  operatingsystemarchitectures_id  Int       @default(0) @db.UnsignedInt
  operatingsystemkernelversions_id Int       @default(0) @db.UnsignedInt
  license_number                   String?   @db.VarChar(255)
  licenseid                        String?   @db.VarChar(255)
  operatingsystemeditions_id       Int       @default(0) @db.UnsignedInt
  date_mod                         DateTime? @db.Timestamp(0)
  date_creation                    DateTime? @db.Timestamp(0)
  is_deleted                       Int       @default(0) @db.TinyInt
  is_dynamic                       Int       @default(0) @db.TinyInt
  entities_id                      Int       @default(0) @db.UnsignedInt
  is_recursive                     Int       @default(0) @db.TinyInt
  install_date                     DateTime? @db.Date
  company                          String?   @db.VarChar(255)
  owner                            String?   @db.VarChar(255)
  hostid                           String?   @db.VarChar(255)

  @@unique([items_id, itemtype, operatingsystems_id, operatingsystemarchitectures_id], map: "unicity")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_dynamic], map: "is_dynamic")
  @@index([is_recursive], map: "is_recursive")
  @@index([itemtype, items_id], map: "item")
  @@index([operatingsystemarchitectures_id], map: "operatingsystemarchitectures_id")
  @@index([operatingsystemeditions_id], map: "operatingsystemeditions_id")
  @@index([operatingsystemkernelversions_id], map: "operatingsystemkernelversions_id")
  @@index([operatingsystems_id], map: "operatingsystems_id")
  @@index([operatingsystemservicepacks_id], map: "operatingsystemservicepacks_id")
  @@index([operatingsystemversions_id], map: "operatingsystemversions_id")
}

model glpi_items_problems {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  problems_id Int     @default(0) @db.UnsignedInt
  itemtype    String? @db.VarChar(100)
  items_id    Int     @default(0) @db.UnsignedInt

  @@unique([problems_id, itemtype, items_id], map: "unicity")
  @@index([itemtype, items_id], map: "item")
}

model glpi_items_projects {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  projects_id Int     @default(0) @db.UnsignedInt
  itemtype    String? @db.VarChar(100)
  items_id    Int     @default(0) @db.UnsignedInt

  @@unique([projects_id, itemtype, items_id], map: "unicity")
  @@index([itemtype, items_id], map: "item")
}

model glpi_items_racks {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  racks_id    Int     @db.UnsignedInt
  itemtype    String  @db.VarChar(255)
  items_id    Int     @db.UnsignedInt
  position    Int
  orientation Int?    @db.TinyInt
  bgcolor     String? @db.VarChar(7)
  hpos        Int     @default(0) @db.TinyInt
  is_reserved Int     @default(0) @db.TinyInt

  @@unique([itemtype, items_id, is_reserved], map: "item")
  @@index([racks_id, itemtype, items_id], map: "relation")
}

model glpi_items_remotemanagements {
  id         Int     @id @default(autoincrement()) @db.UnsignedInt
  itemtype   String? @db.VarChar(100)
  items_id   Int     @default(0) @db.UnsignedInt
  remoteid   String? @db.VarChar(255)
  type       String? @db.VarChar(255)
  is_dynamic Int     @default(0) @db.TinyInt
  is_deleted Int     @default(0) @db.TinyInt

  @@index([is_deleted], map: "is_deleted")
  @@index([is_dynamic], map: "is_dynamic")
  @@index([itemtype, items_id], map: "item")
}

model glpi_items_softwarelicenses {
  id                  Int    @id @default(autoincrement()) @db.UnsignedInt
  items_id            Int    @default(0) @db.UnsignedInt
  itemtype            String @db.VarChar(100)
  softwarelicenses_id Int    @default(0) @db.UnsignedInt
  is_deleted          Int    @default(0) @db.TinyInt
  is_dynamic          Int    @default(0) @db.TinyInt

  @@index([is_deleted], map: "is_deleted")
  @@index([is_dynamic], map: "is_dynamic")
  @@index([itemtype, items_id], map: "item")
  @@index([softwarelicenses_id], map: "softwarelicenses_id")
}

model glpi_items_softwareversions {
  id                  Int       @id @default(autoincrement()) @db.UnsignedInt
  items_id            Int       @default(0) @db.UnsignedInt
  itemtype            String    @db.VarChar(100)
  softwareversions_id Int       @default(0) @db.UnsignedInt
  is_deleted_item     Int       @default(0) @db.TinyInt
  is_template_item    Int       @default(0) @db.TinyInt
  entities_id         Int       @default(0) @db.UnsignedInt
  is_deleted          Int       @default(0) @db.TinyInt
  is_dynamic          Int       @default(0) @db.TinyInt
  date_install        DateTime? @db.Date

  @@unique([itemtype, items_id, softwareversions_id], map: "unicity")
  @@index([entities_id, is_template_item, is_deleted_item], map: "computers_info")
  @@index([date_install], map: "date_install")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_deleted_item], map: "is_deleted_item")
  @@index([is_dynamic], map: "is_dynamic")
  @@index([is_template_item], map: "is_template_item")
  @@index([softwareversions_id], map: "softwareversions_id")
}

model glpi_items_tickets {
  id         Int     @id @default(autoincrement()) @db.UnsignedInt
  itemtype   String? @db.VarChar(255)
  items_id   Int     @default(0) @db.UnsignedInt
  tickets_id Int     @default(0) @db.UnsignedInt

  @@unique([itemtype, items_id, tickets_id], map: "unicity")
  @@index([tickets_id], map: "tickets_id")
}

model glpi_itilcategories {
  id                          Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id                 Int       @default(0) @db.UnsignedInt
  is_recursive                Int       @default(0) @db.TinyInt
  itilcategories_id           Int       @default(0) @db.UnsignedInt
  name                        String?   @db.VarChar(255)
  completename                String?   @db.Text
  comment                     String?   @db.Text
  level                       Int       @default(0)
  knowbaseitemcategories_id   Int       @default(0) @db.UnsignedInt
  users_id                    Int       @default(0) @db.UnsignedInt
  groups_id                   Int       @default(0) @db.UnsignedInt
  code                        String?   @db.VarChar(255)
  ancestors_cache             String?   @db.LongText
  sons_cache                  String?   @db.LongText
  is_helpdeskvisible          Int       @default(1) @db.TinyInt
  tickettemplates_id_incident Int       @default(0) @db.UnsignedInt
  tickettemplates_id_demand   Int       @default(0) @db.UnsignedInt
  changetemplates_id          Int       @default(0) @db.UnsignedInt
  problemtemplates_id         Int       @default(0) @db.UnsignedInt
  is_incident                 Int       @default(1)
  is_request                  Int       @default(1)
  is_problem                  Int       @default(1)
  is_change                   Int       @default(1) @db.TinyInt
  date_mod                    DateTime? @db.Timestamp(0)
  date_creation               DateTime? @db.Timestamp(0)

  @@index([changetemplates_id], map: "changetemplates_id")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([groups_id], map: "groups_id")
  @@index([is_change], map: "is_change")
  @@index([is_helpdeskvisible], map: "is_helpdeskvisible")
  @@index([is_incident], map: "is_incident")
  @@index([is_problem], map: "is_problem")
  @@index([is_recursive], map: "is_recursive")
  @@index([is_request], map: "is_request")
  @@index([itilcategories_id], map: "itilcategories_id")
  @@index([knowbaseitemcategories_id], map: "knowbaseitemcategories_id")
  @@index([level], map: "level")
  @@index([name], map: "name")
  @@index([problemtemplates_id], map: "problemtemplates_id")
  @@index([tickettemplates_id_demand], map: "tickettemplates_id_demand")
  @@index([tickettemplates_id_incident], map: "tickettemplates_id_incident")
  @@index([users_id], map: "users_id")
}

model glpi_itilfollowups {
  id                Int       @id @default(autoincrement()) @db.UnsignedInt
  itemtype          String    @db.VarChar(100)
  items_id          Int       @default(0) @db.UnsignedInt
  date              DateTime? @db.Timestamp(0)
  users_id          Int       @default(0) @db.UnsignedInt
  users_id_editor   Int       @default(0) @db.UnsignedInt
  content           String?   @db.LongText
  is_private        Int       @default(0) @db.TinyInt
  requesttypes_id   Int       @default(0) @db.UnsignedInt
  date_mod          DateTime? @db.Timestamp(0)
  date_creation     DateTime? @db.Timestamp(0)
  timeline_position Int       @default(0) @db.TinyInt
  sourceitems_id    Int       @default(0) @db.UnsignedInt
  sourceof_items_id Int       @default(0) @db.UnsignedInt

  @@index([date], map: "date")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([is_private], map: "is_private")
  @@index([itemtype, items_id], map: "item")
  @@index([requesttypes_id], map: "requesttypes_id")
  @@index([sourceitems_id], map: "sourceitems_id")
  @@index([sourceof_items_id], map: "sourceof_items_id")
  @@index([users_id], map: "users_id")
  @@index([users_id_editor], map: "users_id_editor")
}

model glpi_itilfollowuptemplates {
  id              Int       @id @default(autoincrement()) @db.UnsignedInt
  date_creation   DateTime? @db.Timestamp(0)
  date_mod        DateTime? @db.Timestamp(0)
  entities_id     Int       @default(0) @db.UnsignedInt
  is_recursive    Int       @default(0) @db.TinyInt
  name            String?   @db.VarChar(255)
  content         String?   @db.MediumText
  requesttypes_id Int       @default(0) @db.UnsignedInt
  is_private      Int       @default(0) @db.TinyInt
  comment         String?   @db.Text

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([is_private], map: "is_private")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
  @@index([requesttypes_id], map: "requesttypes_id")
}

model glpi_itils_projects {
  id          Int    @id @default(autoincrement()) @db.UnsignedInt
  itemtype    String @default("") @db.VarChar(100)
  items_id    Int    @default(0) @db.UnsignedInt
  projects_id Int    @default(0) @db.UnsignedInt

  @@unique([itemtype, items_id, projects_id], map: "unicity")
  @@index([projects_id], map: "projects_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_itilsolutions {
  id                 Int       @id @default(autoincrement()) @db.UnsignedInt
  itemtype           String    @db.VarChar(100)
  items_id           Int       @default(0) @db.UnsignedInt
  solutiontypes_id   Int       @default(0) @db.UnsignedInt
  solutiontype_name  String?   @db.VarChar(255)
  content            String?   @db.LongText
  date_creation      DateTime? @db.Timestamp(0)
  date_mod           DateTime? @db.Timestamp(0)
  date_approval      DateTime? @db.Timestamp(0)
  users_id           Int       @default(0) @db.UnsignedInt
  user_name          String?   @db.VarChar(255)
  users_id_editor    Int       @default(0) @db.UnsignedInt
  users_id_approval  Int       @default(0) @db.UnsignedInt
  user_name_approval String?   @db.VarChar(255)
  status             Int       @default(1)
  itilfollowups_id   Int?      @db.UnsignedInt

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([itemtype, items_id], map: "item")
  @@index([itilfollowups_id], map: "itilfollowups_id")
  @@index([solutiontypes_id], map: "solutiontypes_id")
  @@index([status], map: "status")
  @@index([users_id], map: "users_id")
  @@index([users_id_approval], map: "users_id_approval")
  @@index([users_id_editor], map: "users_id_editor")
}

model glpi_knowbaseitemcategories {
  id                        Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id               Int       @default(0) @db.UnsignedInt
  is_recursive              Int       @default(0) @db.TinyInt
  knowbaseitemcategories_id Int       @default(0) @db.UnsignedInt
  name                      String?   @db.VarChar(255)
  completename              String?   @db.Text
  comment                   String?   @db.Text
  level                     Int       @default(0)
  sons_cache                String?   @db.LongText
  ancestors_cache           String?   @db.LongText
  date_mod                  DateTime? @db.Timestamp(0)
  date_creation             DateTime? @db.Timestamp(0)

  @@unique([entities_id, knowbaseitemcategories_id, name], map: "unicity")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([is_recursive], map: "is_recursive")
  @@index([knowbaseitemcategories_id], map: "knowbaseitemcategories_id")
  @@index([level], map: "level")
  @@index([name], map: "name")
}

model glpi_knowbaseitems {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.Text
  answer        String?   @db.LongText
  is_faq        Int       @default(0) @db.TinyInt
  users_id      Int       @default(0) @db.UnsignedInt
  view          Int       @default(0)
  date_creation DateTime? @db.Timestamp(0)
  date_mod      DateTime? @db.Timestamp(0)
  begin_date    DateTime? @db.Timestamp(0)
  end_date      DateTime? @db.Timestamp(0)

  @@index([begin_date], map: "begin_date")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([end_date], map: "end_date")
  @@index([is_faq], map: "is_faq")
  @@index([users_id], map: "users_id")
  @@fulltext([answer], map: "answer")
  @@fulltext([name, answer], map: "fulltext")
  @@fulltext([name], map: "name")
}

model glpi_knowbaseitems_comments {
  id                Int       @id @default(autoincrement()) @db.UnsignedInt
  knowbaseitems_id  Int       @db.UnsignedInt
  users_id          Int       @default(0) @db.UnsignedInt
  language          String?   @db.VarChar(10)
  comment           String?   @db.Text
  parent_comment_id Int?      @db.UnsignedInt
  date_creation     DateTime? @db.Timestamp(0)
  date_mod          DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([knowbaseitems_id], map: "knowbaseitems_id")
  @@index([parent_comment_id], map: "parent_comment_id")
  @@index([users_id], map: "users_id")
}

model glpi_knowbaseitems_items {
  id               Int       @id @default(autoincrement()) @db.UnsignedInt
  knowbaseitems_id Int       @db.UnsignedInt
  itemtype         String    @db.VarChar(100)
  items_id         Int       @default(0) @db.UnsignedInt
  date_creation    DateTime? @db.Timestamp(0)
  date_mod         DateTime? @db.Timestamp(0)

  @@unique([itemtype, items_id, knowbaseitems_id], map: "unicity")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([knowbaseitems_id], map: "knowbaseitems_id")
}

model glpi_knowbaseitems_knowbaseitemcategories {
  id                        Int @id @default(autoincrement()) @db.UnsignedInt
  knowbaseitems_id          Int @default(0) @db.UnsignedInt
  knowbaseitemcategories_id Int @default(0) @db.UnsignedInt

  @@index([knowbaseitemcategories_id], map: "knowbaseitemcategories_id")
  @@index([knowbaseitems_id], map: "knowbaseitems_id")
}

model glpi_knowbaseitems_profiles {
  id                    Int  @id @default(autoincrement()) @db.UnsignedInt
  knowbaseitems_id      Int  @default(0) @db.UnsignedInt
  profiles_id           Int  @default(0) @db.UnsignedInt
  entities_id           Int? @db.UnsignedInt
  is_recursive          Int  @default(0) @db.TinyInt
  no_entity_restriction Int  @default(0) @db.TinyInt

  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([knowbaseitems_id], map: "knowbaseitems_id")
  @@index([profiles_id], map: "profiles_id")
}

model glpi_knowbaseitems_revisions {
  id               Int       @id @default(autoincrement()) @db.UnsignedInt
  knowbaseitems_id Int       @db.UnsignedInt
  revision         Int
  name             String?   @db.Text
  answer           String?   @db.LongText
  language         String?   @db.VarChar(10)
  users_id         Int       @default(0) @db.UnsignedInt
  date             DateTime? @db.Timestamp(0)

  @@unique([knowbaseitems_id, revision, language], map: "unicity")
  @@index([date], map: "date")
  @@index([revision], map: "revision")
  @@index([users_id], map: "users_id")
}

model glpi_knowbaseitems_users {
  id               Int @id @default(autoincrement()) @db.UnsignedInt
  knowbaseitems_id Int @default(0) @db.UnsignedInt
  users_id         Int @default(0) @db.UnsignedInt

  @@index([knowbaseitems_id], map: "knowbaseitems_id")
  @@index([users_id], map: "users_id")
}

model glpi_knowbaseitemtranslations {
  id               Int       @id @default(autoincrement()) @db.UnsignedInt
  knowbaseitems_id Int       @default(0) @db.UnsignedInt
  language         String?   @db.VarChar(10)
  name             String?   @db.Text
  answer           String?   @db.LongText
  users_id         Int       @default(0) @db.UnsignedInt
  date_mod         DateTime? @db.Timestamp(0)
  date_creation    DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([knowbaseitems_id, language], map: "item")
  @@index([users_id], map: "users_id")
  @@fulltext([answer], map: "answer")
  @@fulltext([name, answer], map: "fulltext")
  @@fulltext([name], map: "name")
}

model glpi_lineoperators {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String    @default("") @db.VarChar(255)
  comment       String?   @db.Text
  mcc           Int?
  mnc           Int?
  entities_id   Int       @default(0) @db.UnsignedInt
  is_recursive  Int       @default(0) @db.TinyInt
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@unique([mcc, mnc], map: "unicity")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
}

model glpi_lines {
  id               Int       @id @default(autoincrement()) @db.UnsignedInt
  name             String    @default("") @db.VarChar(255)
  entities_id      Int       @default(0) @db.UnsignedInt
  is_recursive     Int       @default(0) @db.TinyInt
  is_deleted       Int       @default(0) @db.TinyInt
  caller_num       String    @default("") @db.VarChar(255)
  caller_name      String    @default("") @db.VarChar(255)
  users_id         Int       @default(0) @db.UnsignedInt
  groups_id        Int       @default(0) @db.UnsignedInt
  lineoperators_id Int       @default(0) @db.UnsignedInt
  locations_id     Int       @default(0) @db.UnsignedInt
  states_id        Int       @default(0) @db.UnsignedInt
  linetypes_id     Int       @default(0) @db.UnsignedInt
  date_creation    DateTime? @db.Timestamp(0)
  date_mod         DateTime? @db.Timestamp(0)
  comment          String?   @db.Text

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([groups_id], map: "groups_id")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_recursive], map: "is_recursive")
  @@index([lineoperators_id], map: "lineoperators_id")
  @@index([linetypes_id], map: "linetypes_id")
  @@index([locations_id], map: "locations_id")
  @@index([name], map: "name")
  @@index([states_id], map: "states_id")
  @@index([users_id], map: "users_id")
}

model glpi_linetypes {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_links {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id   Int       @default(0) @db.UnsignedInt
  is_recursive  Int       @default(1) @db.TinyInt
  name          String?   @db.VarChar(255)
  link          String?   @db.VarChar(255)
  data          String?   @db.Text
  open_window   Int       @default(1) @db.TinyInt
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
}

model glpi_links_itemtypes {
  id       Int    @id @default(autoincrement()) @db.UnsignedInt
  links_id Int    @default(0) @db.UnsignedInt
  itemtype String @db.VarChar(100)

  @@unique([itemtype, links_id], map: "unicity")
  @@index([links_id], map: "links_id")
}

model glpi_locations {
  id              Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id     Int       @default(0) @db.UnsignedInt
  is_recursive    Int       @default(0) @db.TinyInt
  name            String?   @db.VarChar(255)
  locations_id    Int       @default(0) @db.UnsignedInt
  completename    String?   @db.Text
  comment         String?   @db.Text
  level           Int       @default(0)
  ancestors_cache String?   @db.LongText
  sons_cache      String?   @db.LongText
  address         String?   @db.Text
  postcode        String?   @db.VarChar(255)
  town            String?   @db.VarChar(255)
  state           String?   @db.VarChar(255)
  country         String?   @db.VarChar(255)
  building        String?   @db.VarChar(255)
  room            String?   @db.VarChar(255)
  latitude        String?   @db.VarChar(255)
  longitude       String?   @db.VarChar(255)
  altitude        String?   @db.VarChar(255)
  date_mod        DateTime? @db.Timestamp(0)
  date_creation   DateTime? @db.Timestamp(0)

  @@unique([entities_id, locations_id, name], map: "unicity")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([is_recursive], map: "is_recursive")
  @@index([level], map: "level")
  @@index([locations_id], map: "locations_id")
  @@index([name], map: "name")
}

model glpi_lockedfields {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  itemtype      String?   @db.VarChar(100)
  items_id      Int       @default(0) @db.UnsignedInt
  field         String    @db.VarChar(50)
  value         String?   @db.VarChar(255)
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)
  is_global     Int       @default(0) @db.TinyInt

  @@unique([itemtype, items_id, field], map: "unicity")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([is_global], map: "is_global")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_logs {
  id               Int       @id @default(autoincrement()) @db.UnsignedInt
  itemtype         String    @default("") @db.VarChar(100)
  items_id         Int       @default(0) @db.UnsignedInt
  itemtype_link    String    @default("") @db.VarChar(100)
  linked_action    Int       @default(0)
  user_name        String?   @db.VarChar(255)
  date_mod         DateTime? @db.Timestamp(0)
  id_search_option Int       @default(0)
  old_value        String?   @db.VarChar(255)
  new_value        String?   @db.VarChar(255)

  @@index([date_mod], map: "date_mod")
  @@index([id_search_option], map: "id_search_option")
  @@index([itemtype, items_id], map: "item")
  @@index([itemtype_link], map: "itemtype_link")
}

model glpi_mailcollectors {
  id                  Int       @id @default(autoincrement()) @db.UnsignedInt
  name                String?   @db.VarChar(255)
  host                String?   @db.VarChar(255)
  login               String?   @db.VarChar(255)
  filesize_max        Int       @default(2097152)
  is_active           Int       @default(1) @db.TinyInt
  date_mod            DateTime? @db.Timestamp(0)
  comment             String?   @db.Text
  passwd              String?   @db.VarChar(255)
  accepted            String?   @db.VarChar(255)
  refused             String?   @db.VarChar(255)
  errors              Int       @default(0)
  use_mail_date       Int       @default(0) @db.TinyInt
  date_creation       DateTime? @db.Timestamp(0)
  requester_field     Int       @default(0)
  add_cc_to_observer  Int       @default(0) @db.TinyInt
  collect_only_unread Int       @default(0) @db.TinyInt
  last_collect_date   DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([is_active], map: "is_active")
  @@index([last_collect_date], map: "last_collect_date")
  @@index([name], map: "name")
}

model glpi_manuallinks {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  url           String    @db.VarChar(8096)
  open_window   Int       @default(1) @db.TinyInt
  icon          String?   @db.VarChar(255)
  comment       String?   @db.Text
  items_id      Int       @default(0) @db.UnsignedInt
  itemtype      String?   @db.VarChar(255)
  date_creation DateTime? @db.Timestamp(0)
  date_mod      DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([itemtype, items_id], map: "item")
  @@index([items_id], map: "items_id")
  @@index([name], map: "name")
}

model glpi_manufacturers {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_monitormodels {
  id                Int       @id @default(autoincrement()) @db.UnsignedInt
  name              String?   @db.VarChar(255)
  comment           String?   @db.Text
  product_number    String?   @db.VarChar(255)
  weight            Int       @default(0)
  required_units    Int       @default(1)
  depth             Float     @default(1) @db.Float
  power_connections Int       @default(0)
  power_consumption Int       @default(0)
  is_half_rack      Int       @default(0) @db.TinyInt
  picture_front     String?   @db.Text
  picture_rear      String?   @db.Text
  pictures          String?   @db.Text
  date_mod          DateTime? @db.Timestamp(0)
  date_creation     DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
  @@index([product_number], map: "product_number")
}

model glpi_monitors {
  id                   Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id          Int       @default(0) @db.UnsignedInt
  name                 String?   @db.VarChar(255)
  date_mod             DateTime? @db.Timestamp(0)
  contact              String?   @db.VarChar(255)
  contact_num          String?   @db.VarChar(255)
  users_id_tech        Int       @default(0) @db.UnsignedInt
  groups_id_tech       Int       @default(0) @db.UnsignedInt
  comment              String?   @db.Text
  serial               String?   @db.VarChar(255)
  otherserial          String?   @db.VarChar(255)
  size                 Decimal   @default(0.00) @db.Decimal(5, 2)
  have_micro           Int       @default(0) @db.TinyInt
  have_speaker         Int       @default(0) @db.TinyInt
  have_subd            Int       @default(0) @db.TinyInt
  have_bnc             Int       @default(0) @db.TinyInt
  have_dvi             Int       @default(0) @db.TinyInt
  have_pivot           Int       @default(0) @db.TinyInt
  have_hdmi            Int       @default(0) @db.TinyInt
  have_displayport     Int       @default(0) @db.TinyInt
  locations_id         Int       @default(0) @db.UnsignedInt
  monitortypes_id      Int       @default(0) @db.UnsignedInt
  monitormodels_id     Int       @default(0) @db.UnsignedInt
  manufacturers_id     Int       @default(0) @db.UnsignedInt
  is_global            Int       @default(0) @db.TinyInt
  is_deleted           Int       @default(0) @db.TinyInt
  is_template          Int       @default(0) @db.TinyInt
  template_name        String?   @db.VarChar(255)
  users_id             Int       @default(0) @db.UnsignedInt
  groups_id            Int       @default(0) @db.UnsignedInt
  states_id            Int       @default(0) @db.UnsignedInt
  ticket_tco           Decimal?  @default(0.0000) @db.Decimal(20, 4)
  is_dynamic           Int       @default(0) @db.TinyInt
  autoupdatesystems_id Int       @default(0) @db.UnsignedInt
  uuid                 String?   @db.VarChar(255)
  date_creation        DateTime? @db.Timestamp(0)
  is_recursive         Int       @default(0) @db.TinyInt

  @@index([autoupdatesystems_id], map: "autoupdatesystems_id")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([groups_id], map: "groups_id")
  @@index([groups_id_tech], map: "groups_id_tech")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_dynamic], map: "is_dynamic")
  @@index([is_global], map: "is_global")
  @@index([is_recursive], map: "is_recursive")
  @@index([is_template], map: "is_template")
  @@index([locations_id], map: "locations_id")
  @@index([manufacturers_id], map: "manufacturers_id")
  @@index([monitormodels_id], map: "monitormodels_id")
  @@index([monitortypes_id], map: "monitortypes_id")
  @@index([name], map: "name")
  @@index([otherserial], map: "otherserial")
  @@index([serial], map: "serial")
  @@index([states_id], map: "states_id")
  @@index([users_id], map: "users_id")
  @@index([users_id_tech], map: "users_id_tech")
  @@index([uuid], map: "uuid")
}

model glpi_monitortypes {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_networkaliases {
  id              Int     @id @default(autoincrement()) @db.UnsignedInt
  entities_id     Int     @default(0) @db.UnsignedInt
  networknames_id Int     @default(0) @db.UnsignedInt
  name            String? @db.VarChar(255)
  fqdns_id        Int     @default(0) @db.UnsignedInt
  comment         String? @db.Text

  @@index([entities_id], map: "entities_id")
  @@index([fqdns_id], map: "fqdns_id")
  @@index([name], map: "name")
  @@index([networknames_id], map: "networknames_id")
}

model glpi_networkequipmentmodels {
  id                Int       @id @default(autoincrement()) @db.UnsignedInt
  name              String?   @db.VarChar(255)
  comment           String?   @db.Text
  product_number    String?   @db.VarChar(255)
  weight            Int       @default(0)
  required_units    Int       @default(1)
  depth             Float     @default(1) @db.Float
  power_connections Int       @default(0)
  power_consumption Int       @default(0)
  is_half_rack      Int       @default(0) @db.TinyInt
  picture_front     String?   @db.Text
  picture_rear      String?   @db.Text
  pictures          String?   @db.Text
  date_mod          DateTime? @db.Timestamp(0)
  date_creation     DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
  @@index([product_number], map: "product_number")
}

model glpi_networkequipments {
  id                        Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id               Int       @default(0) @db.UnsignedInt
  is_recursive              Int       @default(0) @db.TinyInt
  name                      String?   @db.VarChar(255)
  ram                       Int?      @db.UnsignedInt
  serial                    String?   @db.VarChar(255)
  otherserial               String?   @db.VarChar(255)
  contact                   String?   @db.VarChar(255)
  contact_num               String?   @db.VarChar(255)
  users_id_tech             Int       @default(0) @db.UnsignedInt
  groups_id_tech            Int       @default(0) @db.UnsignedInt
  date_mod                  DateTime? @db.Timestamp(0)
  comment                   String?   @db.Text
  locations_id              Int       @default(0) @db.UnsignedInt
  networks_id               Int       @default(0) @db.UnsignedInt
  networkequipmenttypes_id  Int       @default(0) @db.UnsignedInt
  networkequipmentmodels_id Int       @default(0) @db.UnsignedInt
  manufacturers_id          Int       @default(0) @db.UnsignedInt
  is_deleted                Int       @default(0) @db.TinyInt
  is_template               Int       @default(0) @db.TinyInt
  template_name             String?   @db.VarChar(255)
  users_id                  Int       @default(0) @db.UnsignedInt
  groups_id                 Int       @default(0) @db.UnsignedInt
  states_id                 Int       @default(0) @db.UnsignedInt
  ticket_tco                Decimal?  @default(0.0000) @db.Decimal(20, 4)
  is_dynamic                Int       @default(0) @db.TinyInt
  uuid                      String?   @db.VarChar(255)
  date_creation             DateTime? @db.Timestamp(0)
  autoupdatesystems_id      Int       @default(0) @db.UnsignedInt
  sysdescr                  String?   @db.Text
  cpu                       Int       @default(0)
  uptime                    String    @default("0") @db.VarChar(255)
  last_inventory_update     DateTime? @db.Timestamp(0)
  snmpcredentials_id        Int       @default(0) @db.UnsignedInt

  @@index([autoupdatesystems_id], map: "autoupdatesystems_id")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([groups_id], map: "groups_id")
  @@index([groups_id_tech], map: "groups_id_tech")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_dynamic], map: "is_dynamic")
  @@index([is_recursive], map: "is_recursive")
  @@index([is_template], map: "is_template")
  @@index([locations_id], map: "locations_id")
  @@index([manufacturers_id], map: "manufacturers_id")
  @@index([name], map: "name")
  @@index([networkequipmentmodels_id], map: "networkequipmentmodels_id")
  @@index([networkequipmenttypes_id], map: "networkequipmenttypes_id")
  @@index([networks_id], map: "networks_id")
  @@index([otherserial], map: "otherserial")
  @@index([serial], map: "serial")
  @@index([snmpcredentials_id], map: "snmpcredentials_id")
  @@index([states_id], map: "states_id")
  @@index([users_id], map: "users_id")
  @@index([users_id_tech], map: "users_id_tech")
  @@index([uuid], map: "uuid")
}

model glpi_networkequipmenttypes {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_networkinterfaces {
  id      Int     @id @default(autoincrement()) @db.UnsignedInt
  name    String? @db.VarChar(255)
  comment String? @db.Text

  @@index([name], map: "name")
}

model glpi_networknames {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id   Int       @default(0) @db.UnsignedInt
  items_id      Int       @default(0) @db.UnsignedInt
  itemtype      String    @db.VarChar(100)
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  fqdns_id      Int       @default(0) @db.UnsignedInt
  ipnetworks_id Int       @default(0) @db.UnsignedInt
  is_deleted    Int       @default(0) @db.TinyInt
  is_dynamic    Int       @default(0) @db.TinyInt
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([name, fqdns_id], map: "FQDN")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([fqdns_id], map: "fqdns_id")
  @@index([ipnetworks_id], map: "ipnetworks_id")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_dynamic], map: "is_dynamic")
  @@index([itemtype, items_id, is_deleted], map: "item")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_networkportaggregates {
  id                   Int       @id @default(autoincrement()) @db.UnsignedInt
  networkports_id      Int       @unique(map: "networkports_id") @default(0) @db.UnsignedInt
  networkports_id_list String?   @db.Text
  date_mod             DateTime? @db.Timestamp(0)
  date_creation        DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
}

model glpi_networkportaliases {
  id                    Int       @id @default(autoincrement()) @db.UnsignedInt
  networkports_id       Int       @unique(map: "networkports_id") @default(0) @db.UnsignedInt
  networkports_id_alias Int       @default(0) @db.UnsignedInt
  date_mod              DateTime? @db.Timestamp(0)
  date_creation         DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([networkports_id_alias], map: "networkports_id_alias")
}

model glpi_networkportconnectionlogs {
  id                          Int       @id @default(autoincrement()) @db.UnsignedInt
  date                        DateTime? @db.Timestamp(0)
  connected                   Int       @default(0) @db.TinyInt
  networkports_id_source      Int       @default(0) @db.UnsignedInt
  networkports_id_destination Int       @default(0) @db.UnsignedInt

  @@index([date], map: "date")
  @@index([networkports_id_destination], map: "networkports_id_destination")
  @@index([networkports_id_source], map: "networkports_id_source")
}

model glpi_networkportdialups {
  id              Int       @id @default(autoincrement()) @db.UnsignedInt
  networkports_id Int       @unique(map: "networkports_id") @default(0) @db.UnsignedInt
  date_mod        DateTime? @db.Timestamp(0)
  date_creation   DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_networkportethernets {
  id                          Int       @id @default(autoincrement()) @db.UnsignedInt
  networkports_id             Int       @unique(map: "networkports_id") @default(0) @db.UnsignedInt
  items_devicenetworkcards_id Int       @default(0) @db.UnsignedInt
  type                        String?   @default("") @db.VarChar(10)
  speed                       Int       @default(10)
  date_mod                    DateTime? @db.Timestamp(0)
  date_creation               DateTime? @db.Timestamp(0)

  @@index([items_devicenetworkcards_id], map: "card")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([speed], map: "speed")
  @@index([type], map: "type")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_networkportfiberchannels {
  id                              Int       @id @default(autoincrement()) @db.UnsignedInt
  networkports_id                 Int       @unique(map: "networkports_id") @default(0) @db.UnsignedInt
  items_devicenetworkcards_id     Int       @default(0) @db.UnsignedInt
  networkportfiberchanneltypes_id Int       @default(0) @db.UnsignedInt
  wwn                             String?   @default("") @db.VarChar(50)
  speed                           Int       @default(10)
  date_mod                        DateTime? @db.Timestamp(0)
  date_creation                   DateTime? @db.Timestamp(0)

  @@index([items_devicenetworkcards_id], map: "card")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([speed], map: "speed")
  @@index([networkportfiberchanneltypes_id], map: "type")
  @@index([wwn], map: "wwn")
}

model glpi_networkportfiberchanneltypes {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_networkportlocals {
  id              Int       @id @default(autoincrement()) @db.UnsignedInt
  networkports_id Int       @unique(map: "networkports_id") @default(0) @db.UnsignedInt
  date_mod        DateTime? @db.Timestamp(0)
  date_creation   DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
}

model glpi_networkportmetrics {
  id              Int       @id @default(autoincrement()) @db.UnsignedInt
  date            DateTime? @db.Date
  ifinbytes       BigInt    @default(0)
  ifinerrors      BigInt    @default(0)
  ifoutbytes      BigInt    @default(0)
  ifouterrors     BigInt    @default(0)
  networkports_id Int       @default(0) @db.UnsignedInt
  date_creation   DateTime? @db.Timestamp(0)
  date_mod        DateTime? @db.Timestamp(0)

  @@unique([networkports_id, date], map: "unicity")
  @@index([date], map: "date")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
}

model glpi_networkports {
  id                 Int       @id @default(autoincrement()) @db.UnsignedInt
  items_id           Int       @default(0) @db.UnsignedInt
  itemtype           String    @db.VarChar(100)
  entities_id        Int       @default(0) @db.UnsignedInt
  is_recursive       Int       @default(0) @db.TinyInt
  logical_number     Int       @default(0)
  name               String?   @db.VarChar(255)
  instantiation_type String?   @db.VarChar(255)
  mac                String?   @db.VarChar(255)
  comment            String?   @db.Text
  is_deleted         Int       @default(0) @db.TinyInt
  is_dynamic         Int       @default(0) @db.TinyInt
  date_mod           DateTime? @db.Timestamp(0)
  date_creation      DateTime? @db.Timestamp(0)
  ifmtu              Int       @default(0)
  ifspeed            BigInt    @default(0)
  ifinternalstatus   String?   @db.VarChar(255)
  ifconnectionstatus Int       @default(0)
  iflastchange       String?   @db.VarChar(255)
  ifinbytes          BigInt    @default(0)
  ifinerrors         BigInt    @default(0)
  ifoutbytes         BigInt    @default(0)
  ifouterrors        BigInt    @default(0)
  ifstatus           String?   @db.VarChar(255)
  ifdescr            String?   @db.VarChar(255)
  ifalias            String?   @db.VarChar(255)
  portduplex         String?   @db.VarChar(255)
  trunk              Int       @default(0) @db.TinyInt
  lastup             DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_dynamic], map: "is_dynamic")
  @@index([is_recursive], map: "is_recursive")
  @@index([itemtype, items_id], map: "item")
  @@index([mac], map: "mac")
  @@index([name], map: "name")
}

model glpi_networkports_networkports {
  id                Int @id @default(autoincrement()) @db.UnsignedInt
  networkports_id_1 Int @default(0) @db.UnsignedInt
  networkports_id_2 Int @default(0) @db.UnsignedInt

  @@unique([networkports_id_1, networkports_id_2], map: "unicity")
  @@index([networkports_id_2], map: "networkports_id_2")
}

model glpi_networkports_vlans {
  id              Int @id @default(autoincrement()) @db.UnsignedInt
  networkports_id Int @default(0) @db.UnsignedInt
  vlans_id        Int @default(0) @db.UnsignedInt
  tagged          Int @default(0) @db.TinyInt

  @@unique([networkports_id, vlans_id], map: "unicity")
  @@index([vlans_id], map: "vlans_id")
}

model glpi_networkporttypes {
  id                 Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id        Int       @default(0) @db.UnsignedInt
  is_recursive       Int       @default(0) @db.TinyInt
  value_decimal      Int
  name               String?   @db.VarChar(255)
  comment            String?   @db.Text
  is_importable      Int       @default(0) @db.TinyInt
  instantiation_type String?   @db.VarChar(255)
  date_creation      DateTime? @db.Timestamp(0)
  date_mod           DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([is_importable], map: "is_importable")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
  @@index([value_decimal], map: "value_decimal")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_networkportwifis {
  id                          Int       @id @default(autoincrement()) @db.UnsignedInt
  networkports_id             Int       @unique(map: "networkports_id") @default(0) @db.UnsignedInt
  items_devicenetworkcards_id Int       @default(0) @db.UnsignedInt
  wifinetworks_id             Int       @default(0) @db.UnsignedInt
  networkportwifis_id         Int       @default(0) @db.UnsignedInt
  version                     String?   @db.VarChar(20)
  mode                        String?   @db.VarChar(20)
  date_mod                    DateTime? @db.Timestamp(0)
  date_creation               DateTime? @db.Timestamp(0)

  @@index([items_devicenetworkcards_id], map: "card")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([wifinetworks_id], map: "essid")
  @@index([mode], map: "mode")
  @@index([networkportwifis_id], map: "networkportwifis_id")
  @@index([version], map: "version")
}

model glpi_networks {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_notepads {
  id                   Int       @id @default(autoincrement()) @db.UnsignedInt
  itemtype             String?   @db.VarChar(100)
  items_id             Int       @default(0) @db.UnsignedInt
  date_creation        DateTime? @db.Timestamp(0)
  date_mod             DateTime? @db.Timestamp(0)
  users_id             Int       @default(0) @db.UnsignedInt
  users_id_lastupdater Int       @default(0) @db.UnsignedInt
  content              String?   @db.LongText

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([itemtype, items_id], map: "item")
  @@index([users_id], map: "users_id")
  @@index([users_id_lastupdater], map: "users_id_lastupdater")
}

model glpi_notifications {
  id             Int       @id @default(autoincrement()) @db.UnsignedInt
  name           String?   @db.VarChar(255)
  entities_id    Int       @default(0) @db.UnsignedInt
  itemtype       String    @db.VarChar(100)
  event          String    @db.VarChar(255)
  comment        String?   @db.Text
  is_recursive   Int       @default(0) @db.TinyInt
  is_active      Int       @default(0) @db.TinyInt
  date_mod       DateTime? @db.Timestamp(0)
  date_creation  DateTime? @db.Timestamp(0)
  allow_response Int       @default(1) @db.TinyInt

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([is_active], map: "is_active")
  @@index([is_recursive], map: "is_recursive")
  @@index([itemtype], map: "itemtype")
  @@index([name], map: "name")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_notifications_notificationtemplates {
  id                       Int    @id @default(autoincrement()) @db.UnsignedInt
  notifications_id         Int    @default(0) @db.UnsignedInt
  mode                     String @db.VarChar(20)
  notificationtemplates_id Int    @default(0) @db.UnsignedInt

  @@unique([notifications_id, mode, notificationtemplates_id], map: "unicity")
  @@index([mode], map: "mode")
  @@index([notificationtemplates_id], map: "notificationtemplates_id")
}

model glpi_notificationtargets {
  id               Int @id @default(autoincrement()) @db.UnsignedInt
  items_id         Int @default(0) @db.UnsignedInt
  type             Int @default(0)
  notifications_id Int @default(0) @db.UnsignedInt

  @@index([type, items_id], map: "items")
  @@index([notifications_id], map: "notifications_id")
}

model glpi_notificationtemplates {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  itemtype      String    @db.VarChar(100)
  date_mod      DateTime? @db.Timestamp(0)
  comment       String?   @db.Text
  css           String?   @db.Text
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([itemtype], map: "itemtype")
  @@index([name], map: "name")
}

model glpi_notificationtemplatetranslations {
  id                       Int     @id @default(autoincrement()) @db.UnsignedInt
  notificationtemplates_id Int     @default(0) @db.UnsignedInt
  language                 String  @default("") @db.VarChar(10)
  subject                  String  @db.VarChar(255)
  content_text             String? @db.Text
  content_html             String? @db.Text

  @@index([notificationtemplates_id], map: "notificationtemplates_id")
}

model glpi_notimportedemails {
  id                Int      @id @default(autoincrement()) @db.UnsignedInt
  from              String   @db.VarChar(255)
  to                String   @db.VarChar(255)
  mailcollectors_id Int      @default(0) @db.UnsignedInt
  date              DateTime @default(now()) @db.Timestamp(0)
  subject           String?  @db.Text
  messageid         String   @db.VarChar(255)
  reason            Int      @default(0)
  users_id          Int      @default(0) @db.UnsignedInt

  @@index([mailcollectors_id], map: "mailcollectors_id")
  @@index([users_id], map: "users_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_objectlocks {
  id       Int      @id @default(autoincrement()) @db.UnsignedInt
  itemtype String   @db.VarChar(100)
  items_id Int      @db.UnsignedInt
  users_id Int      @db.UnsignedInt
  date     DateTime @default(now()) @db.Timestamp(0)

  @@unique([itemtype, items_id], map: "item")
  @@index([date], map: "date")
  @@index([users_id], map: "users_id")
}

model glpi_olalevelactions {
  id           Int     @id @default(autoincrement()) @db.UnsignedInt
  olalevels_id Int     @default(0) @db.UnsignedInt
  action_type  String? @db.VarChar(255)
  field        String? @db.VarChar(255)
  value        String? @db.VarChar(255)

  @@index([olalevels_id], map: "olalevels_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_olalevelcriterias {
  id           Int     @id @default(autoincrement()) @db.UnsignedInt
  olalevels_id Int     @default(0) @db.UnsignedInt
  criteria     String? @db.VarChar(255)
  condition    Int     @default(0)
  pattern      String? @db.VarChar(255)

  @@index([condition], map: "condition")
  @@index([olalevels_id], map: "olalevels_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_olalevels {
  id             Int     @id @default(autoincrement()) @db.UnsignedInt
  name           String? @db.VarChar(255)
  olas_id        Int     @default(0) @db.UnsignedInt
  execution_time Int
  is_active      Int     @default(1) @db.TinyInt
  entities_id    Int     @default(0) @db.UnsignedInt
  is_recursive   Int     @default(0) @db.TinyInt
  match          String? @db.Char(10)
  uuid           String? @db.VarChar(255)

  @@index([entities_id], map: "entities_id")
  @@index([is_active], map: "is_active")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
  @@index([olas_id], map: "olas_id")
}

model glpi_olalevels_tickets {
  id           Int       @id @default(autoincrement()) @db.UnsignedInt
  tickets_id   Int       @default(0) @db.UnsignedInt
  olalevels_id Int       @default(0) @db.UnsignedInt
  date         DateTime? @db.Timestamp(0)

  @@unique([tickets_id, olalevels_id], map: "unicity")
  @@index([olalevels_id], map: "olalevels_id")
}

model glpi_olas {
  id                  Int       @id @default(autoincrement()) @db.UnsignedInt
  name                String?   @db.VarChar(255)
  entities_id         Int       @default(0) @db.UnsignedInt
  is_recursive        Int       @default(0) @db.TinyInt
  type                Int       @default(0)
  comment             String?   @db.Text
  number_time         Int
  use_ticket_calendar Int       @default(0) @db.TinyInt
  calendars_id        Int       @default(0) @db.UnsignedInt
  date_mod            DateTime? @db.Timestamp(0)
  definition_time     String?   @db.VarChar(255)
  end_of_working_day  Int       @default(0) @db.TinyInt
  date_creation       DateTime? @db.Timestamp(0)
  slms_id             Int       @default(0) @db.UnsignedInt

  @@index([calendars_id], map: "calendars_id")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
  @@index([slms_id], map: "slms_id")
}

model glpi_operatingsystemarchitectures {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_operatingsystemeditions {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_operatingsystemkernels {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_operatingsystemkernelversions {
  id                        Int       @id @default(autoincrement()) @db.UnsignedInt
  operatingsystemkernels_id Int       @default(0) @db.UnsignedInt
  name                      String?   @db.VarChar(255)
  comment                   String?   @db.Text
  date_mod                  DateTime? @db.Timestamp(0)
  date_creation             DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
  @@index([operatingsystemkernels_id], map: "operatingsystemkernels_id")
}

model glpi_operatingsystems {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_operatingsystemservicepacks {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_operatingsystemversions {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_passivedcequipmentmodels {
  id                Int       @id @default(autoincrement()) @db.UnsignedInt
  name              String?   @db.VarChar(255)
  comment           String?   @db.Text
  product_number    String?   @db.VarChar(255)
  weight            Int       @default(0)
  required_units    Int       @default(1)
  depth             Float     @default(1) @db.Float
  power_connections Int       @default(0)
  power_consumption Int       @default(0)
  is_half_rack      Int       @default(0) @db.TinyInt
  picture_front     String?   @db.Text
  picture_rear      String?   @db.Text
  pictures          String?   @db.Text
  date_mod          DateTime? @db.Timestamp(0)
  date_creation     DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
  @@index([product_number], map: "product_number")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_passivedcequipments {
  id                          Int       @id @default(autoincrement()) @db.UnsignedInt
  name                        String?   @db.VarChar(255)
  entities_id                 Int       @default(0) @db.UnsignedInt
  is_recursive                Int       @default(0) @db.TinyInt
  locations_id                Int       @default(0) @db.UnsignedInt
  serial                      String?   @db.VarChar(255)
  otherserial                 String?   @db.VarChar(255)
  passivedcequipmentmodels_id Int?      @db.UnsignedInt
  passivedcequipmenttypes_id  Int       @default(0) @db.UnsignedInt
  users_id_tech               Int       @default(0) @db.UnsignedInt
  groups_id_tech              Int       @default(0) @db.UnsignedInt
  is_template                 Int       @default(0) @db.TinyInt
  template_name               String?   @db.VarChar(255)
  is_deleted                  Int       @default(0) @db.TinyInt
  states_id                   Int       @default(0) @db.UnsignedInt
  comment                     String?   @db.Text
  manufacturers_id            Int       @default(0) @db.UnsignedInt
  date_mod                    DateTime? @db.Timestamp(0)
  date_creation               DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([groups_id_tech], map: "group_id_tech")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_recursive], map: "is_recursive")
  @@index([is_template], map: "is_template")
  @@index([locations_id], map: "locations_id")
  @@index([manufacturers_id], map: "manufacturers_id")
  @@index([name], map: "name")
  @@index([passivedcequipmentmodels_id], map: "passivedcequipmentmodels_id")
  @@index([passivedcequipmenttypes_id], map: "passivedcequipmenttypes_id")
  @@index([states_id], map: "states_id")
  @@index([users_id_tech], map: "users_id_tech")
}

model glpi_passivedcequipmenttypes {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_pcivendors {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id   Int       @default(0) @db.UnsignedInt
  is_recursive  Int       @default(0) @db.TinyInt
  vendorid      String    @db.VarChar(4)
  deviceid      String?   @db.VarChar(4)
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_creation DateTime? @db.Timestamp(0)
  date_mod      DateTime? @db.Timestamp(0)

  @@unique([vendorid, deviceid], map: "unicity")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([deviceid], map: "deviceid")
  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
}

model glpi_pdumodels {
  id                Int       @id @default(autoincrement()) @db.UnsignedInt
  name              String?   @db.VarChar(255)
  comment           String?   @db.Text
  product_number    String?   @db.VarChar(255)
  weight            Int       @default(0)
  required_units    Int       @default(1)
  depth             Float     @default(1) @db.Float
  power_connections Int       @default(0)
  max_power         Int       @default(0)
  is_half_rack      Int       @default(0) @db.TinyInt
  picture_front     String?   @db.Text
  picture_rear      String?   @db.Text
  pictures          String?   @db.Text
  is_rackable       Int       @default(0) @db.TinyInt
  date_mod          DateTime? @db.Timestamp(0)
  date_creation     DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([is_rackable], map: "is_rackable")
  @@index([name], map: "name")
  @@index([product_number], map: "product_number")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_pdus {
  id               Int       @id @default(autoincrement()) @db.UnsignedInt
  name             String?   @db.VarChar(255)
  entities_id      Int       @default(0) @db.UnsignedInt
  is_recursive     Int       @default(0) @db.TinyInt
  locations_id     Int       @default(0) @db.UnsignedInt
  serial           String?   @db.VarChar(255)
  otherserial      String?   @db.VarChar(255)
  pdumodels_id     Int?      @db.UnsignedInt
  users_id_tech    Int       @default(0) @db.UnsignedInt
  groups_id_tech   Int       @default(0) @db.UnsignedInt
  is_template      Int       @default(0) @db.TinyInt
  template_name    String?   @db.VarChar(255)
  is_deleted       Int       @default(0) @db.TinyInt
  states_id        Int       @default(0) @db.UnsignedInt
  comment          String?   @db.Text
  manufacturers_id Int       @default(0) @db.UnsignedInt
  pdutypes_id      Int       @default(0) @db.UnsignedInt
  date_mod         DateTime? @db.Timestamp(0)
  date_creation    DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([groups_id_tech], map: "group_id_tech")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_recursive], map: "is_recursive")
  @@index([is_template], map: "is_template")
  @@index([locations_id], map: "locations_id")
  @@index([manufacturers_id], map: "manufacturers_id")
  @@index([name], map: "name")
  @@index([pdumodels_id], map: "pdumodels_id")
  @@index([pdutypes_id], map: "pdutypes_id")
  @@index([states_id], map: "states_id")
  @@index([users_id_tech], map: "users_id_tech")
}

model glpi_pdus_plugs {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  plugs_id      Int       @default(0) @db.UnsignedInt
  pdus_id       Int       @default(0) @db.UnsignedInt
  number_plugs  Int?      @default(0)
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([pdus_id], map: "pdus_id")
  @@index([plugs_id], map: "plugs_id")
}

model glpi_pdus_racks {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  racks_id      Int       @default(0) @db.UnsignedInt
  pdus_id       Int       @default(0) @db.UnsignedInt
  side          Int?      @default(0)
  position      Int
  bgcolor       String?   @db.VarChar(7)
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([pdus_id], map: "pdus_id")
  @@index([racks_id], map: "racks_id")
}

model glpi_pdutypes {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id   Int       @default(0) @db.UnsignedInt
  is_recursive  Int       @default(0) @db.TinyInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_creation DateTime? @db.Timestamp(0)
  date_mod      DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
}

model glpi_pendingreasons {
  id                          Int     @id @default(autoincrement()) @db.UnsignedInt
  entities_id                 Int     @default(0) @db.UnsignedInt
  is_recursive                Int     @default(0) @db.TinyInt
  name                        String? @db.VarChar(255)
  followup_frequency          Int     @default(0)
  followups_before_resolution Int     @default(0)
  itilfollowuptemplates_id    Int     @default(0) @db.UnsignedInt
  solutiontemplates_id        Int     @default(0) @db.UnsignedInt
  comment                     String? @db.Text

  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([itilfollowuptemplates_id], map: "itilfollowuptemplates_id")
  @@index([name], map: "name")
  @@index([solutiontemplates_id], map: "solutiontemplates_id")
}

model glpi_pendingreasons_items {
  id                          Int       @id @default(autoincrement()) @db.UnsignedInt
  pendingreasons_id           Int       @default(0) @db.UnsignedInt
  items_id                    Int       @default(0) @db.UnsignedInt
  itemtype                    String    @default("") @db.VarChar(100)
  followup_frequency          Int       @default(0)
  followups_before_resolution Int       @default(0)
  bump_count                  Int       @default(0)
  last_bump_date              DateTime? @db.Timestamp(0)
  previous_status             Int?

  @@unique([items_id, itemtype], map: "unicity")
  @@index([itemtype, items_id], map: "item")
  @@index([pendingreasons_id], map: "pendingreasons_id")
}

model glpi_peripheralmodels {
  id                Int       @id @default(autoincrement()) @db.UnsignedInt
  name              String?   @db.VarChar(255)
  comment           String?   @db.Text
  product_number    String?   @db.VarChar(255)
  weight            Int       @default(0)
  required_units    Int       @default(1)
  depth             Float     @default(1) @db.Float
  power_connections Int       @default(0)
  power_consumption Int       @default(0)
  is_half_rack      Int       @default(0) @db.TinyInt
  picture_front     String?   @db.Text
  picture_rear      String?   @db.Text
  pictures          String?   @db.Text
  date_mod          DateTime? @db.Timestamp(0)
  date_creation     DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
  @@index([product_number], map: "product_number")
}

model glpi_peripherals {
  id                   Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id          Int       @default(0) @db.UnsignedInt
  name                 String?   @db.VarChar(255)
  date_mod             DateTime? @db.Timestamp(0)
  contact              String?   @db.VarChar(255)
  contact_num          String?   @db.VarChar(255)
  users_id_tech        Int       @default(0) @db.UnsignedInt
  groups_id_tech       Int       @default(0) @db.UnsignedInt
  comment              String?   @db.Text
  serial               String?   @db.VarChar(255)
  otherserial          String?   @db.VarChar(255)
  locations_id         Int       @default(0) @db.UnsignedInt
  peripheraltypes_id   Int       @default(0) @db.UnsignedInt
  peripheralmodels_id  Int       @default(0) @db.UnsignedInt
  brand                String?   @db.VarChar(255)
  manufacturers_id     Int       @default(0) @db.UnsignedInt
  is_global            Int       @default(0) @db.TinyInt
  is_deleted           Int       @default(0) @db.TinyInt
  is_template          Int       @default(0) @db.TinyInt
  template_name        String?   @db.VarChar(255)
  users_id             Int       @default(0) @db.UnsignedInt
  groups_id            Int       @default(0) @db.UnsignedInt
  states_id            Int       @default(0) @db.UnsignedInt
  ticket_tco           Decimal?  @default(0.0000) @db.Decimal(20, 4)
  is_dynamic           Int       @default(0) @db.TinyInt
  autoupdatesystems_id Int       @default(0) @db.UnsignedInt
  uuid                 String?   @db.VarChar(255)
  date_creation        DateTime? @db.Timestamp(0)
  is_recursive         Int       @default(0) @db.TinyInt

  @@index([autoupdatesystems_id], map: "autoupdatesystems_id")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([groups_id], map: "groups_id")
  @@index([groups_id_tech], map: "groups_id_tech")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_dynamic], map: "is_dynamic")
  @@index([is_global], map: "is_global")
  @@index([is_recursive], map: "is_recursive")
  @@index([is_template], map: "is_template")
  @@index([locations_id], map: "locations_id")
  @@index([manufacturers_id], map: "manufacturers_id")
  @@index([name], map: "name")
  @@index([otherserial], map: "otherserial")
  @@index([peripheralmodels_id], map: "peripheralmodels_id")
  @@index([peripheraltypes_id], map: "peripheraltypes_id")
  @@index([serial], map: "serial")
  @@index([states_id], map: "states_id")
  @@index([users_id], map: "users_id")
  @@index([users_id_tech], map: "users_id_tech")
  @@index([uuid], map: "uuid")
}

model glpi_peripheraltypes {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_phonemodels {
  id             Int       @id @default(autoincrement()) @db.UnsignedInt
  name           String?   @db.VarChar(255)
  comment        String?   @db.Text
  product_number String?   @db.VarChar(255)
  date_mod       DateTime? @db.Timestamp(0)
  date_creation  DateTime? @db.Timestamp(0)
  picture_front  String?   @db.Text
  picture_rear   String?   @db.Text
  pictures       String?   @db.Text

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
  @@index([product_number], map: "product_number")
}

model glpi_phonepowersupplies {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_phones {
  id                    Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id           Int       @default(0) @db.UnsignedInt
  name                  String?   @db.VarChar(255)
  date_mod              DateTime? @db.Timestamp(0)
  contact               String?   @db.VarChar(255)
  contact_num           String?   @db.VarChar(255)
  users_id_tech         Int       @default(0) @db.UnsignedInt
  groups_id_tech        Int       @default(0) @db.UnsignedInt
  comment               String?   @db.Text
  serial                String?   @db.VarChar(255)
  otherserial           String?   @db.VarChar(255)
  locations_id          Int       @default(0) @db.UnsignedInt
  phonetypes_id         Int       @default(0) @db.UnsignedInt
  phonemodels_id        Int       @default(0) @db.UnsignedInt
  brand                 String?   @db.VarChar(255)
  phonepowersupplies_id Int       @default(0) @db.UnsignedInt
  number_line           String?   @db.VarChar(255)
  have_headset          Int       @default(0) @db.TinyInt
  have_hp               Int       @default(0) @db.TinyInt
  manufacturers_id      Int       @default(0) @db.UnsignedInt
  is_global             Int       @default(0) @db.TinyInt
  is_deleted            Int       @default(0) @db.TinyInt
  is_template           Int       @default(0) @db.TinyInt
  template_name         String?   @db.VarChar(255)
  users_id              Int       @default(0) @db.UnsignedInt
  groups_id             Int       @default(0) @db.UnsignedInt
  states_id             Int       @default(0) @db.UnsignedInt
  ticket_tco            Decimal?  @default(0.0000) @db.Decimal(20, 4)
  is_dynamic            Int       @default(0) @db.TinyInt
  autoupdatesystems_id  Int       @default(0) @db.UnsignedInt
  uuid                  String?   @db.VarChar(255)
  date_creation         DateTime? @db.Timestamp(0)
  is_recursive          Int       @default(0) @db.TinyInt
  last_inventory_update DateTime? @db.Timestamp(0)

  @@index([autoupdatesystems_id], map: "autoupdatesystems_id")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([groups_id], map: "groups_id")
  @@index([groups_id_tech], map: "groups_id_tech")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_dynamic], map: "is_dynamic")
  @@index([is_global], map: "is_global")
  @@index([is_recursive], map: "is_recursive")
  @@index([is_template], map: "is_template")
  @@index([locations_id], map: "locations_id")
  @@index([manufacturers_id], map: "manufacturers_id")
  @@index([name], map: "name")
  @@index([otherserial], map: "otherserial")
  @@index([phonemodels_id], map: "phonemodels_id")
  @@index([phonepowersupplies_id], map: "phonepowersupplies_id")
  @@index([phonetypes_id], map: "phonetypes_id")
  @@index([serial], map: "serial")
  @@index([states_id], map: "states_id")
  @@index([users_id], map: "users_id")
  @@index([users_id_tech], map: "users_id_tech")
  @@index([uuid], map: "uuid")
}

model glpi_phonetypes {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_planningeventcategories {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  color         String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_planningexternalevents {
  id                                Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid                              String?   @unique(map: "uuid") @db.VarChar(255)
  planningexternaleventtemplates_id Int       @default(0) @db.UnsignedInt
  entities_id                       Int       @default(0) @db.UnsignedInt
  is_recursive                      Int       @default(1) @db.TinyInt
  date                              DateTime? @db.Timestamp(0)
  users_id                          Int       @default(0) @db.UnsignedInt
  users_id_guests                   String?   @db.Text
  groups_id                         Int       @default(0) @db.UnsignedInt
  name                              String?   @db.VarChar(255)
  text                              String?   @db.Text
  begin                             DateTime? @db.Timestamp(0)
  end                               DateTime? @db.Timestamp(0)
  rrule                             String?   @db.Text
  state                             Int       @default(0)
  planningeventcategories_id        Int       @default(0) @db.UnsignedInt
  background                        Int       @default(0) @db.TinyInt
  date_mod                          DateTime? @db.Timestamp(0)
  date_creation                     DateTime? @db.Timestamp(0)

  @@index([begin], map: "begin")
  @@index([date], map: "date")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([end], map: "end")
  @@index([entities_id], map: "entities_id")
  @@index([groups_id], map: "groups_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
  @@index([planningeventcategories_id], map: "planningeventcategories_id")
  @@index([planningexternaleventtemplates_id], map: "planningexternaleventtemplates_id")
  @@index([state], map: "state")
  @@index([users_id], map: "users_id")
}

model glpi_planningexternaleventtemplates {
  id                         Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id                Int       @default(0) @db.UnsignedInt
  name                       String?   @db.VarChar(255)
  text                       String?   @db.MediumText
  comment                    String?   @db.Text
  duration                   Int       @default(0)
  before_time                Int       @default(0)
  rrule                      String?   @db.Text
  state                      Int       @default(0)
  planningeventcategories_id Int       @default(0) @db.UnsignedInt
  background                 Int       @default(0) @db.TinyInt
  date_mod                   DateTime? @db.Timestamp(0)
  date_creation              DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([name], map: "name")
  @@index([planningeventcategories_id], map: "planningeventcategories_id")
  @@index([state], map: "state")
}

model glpi_planningrecalls {
  id          Int       @id @default(autoincrement()) @db.UnsignedInt
  items_id    Int       @default(0) @db.UnsignedInt
  itemtype    String    @db.VarChar(100)
  users_id    Int       @default(0) @db.UnsignedInt
  before_time Int       @default(-10)
  when        DateTime? @db.Timestamp(0)

  @@unique([itemtype, items_id, users_id], map: "unicity")
  @@index([before_time], map: "before_time")
  @@index([users_id], map: "users_id")
  @@index([when], map: "when")
}

model glpi_plugin_barcode_configs {
  id   Int     @id @default(autoincrement())
  type String? @db.VarChar(20)
}

model glpi_plugin_barcode_configs_types {
  id               Int     @id @default(autoincrement())
  type             String? @unique(map: "type") @db.VarChar(20)
  size             String? @db.VarChar(20)
  orientation      String? @db.VarChar(9)
  marginTop        Int?
  marginBottom     Int?
  marginLeft       Int?
  marginRight      Int?
  marginHorizontal Int?
  marginVertical   Int?
  maxCodeWidth     Int?
  maxCodeHeight    Int?
  txtSize          Int?
  txtSpacing       Int?
}

model glpi_plugin_datainjection_infos {
  id           Int    @id @default(autoincrement()) @db.UnsignedInt
  models_id    Int    @db.UnsignedInt
  itemtype     String @default("") @db.VarChar(255)
  value        String @db.VarChar(255)
  is_mandatory Int    @default(0) @db.TinyInt
}

model glpi_plugin_datainjection_mappings {
  id           Int    @id @default(autoincrement()) @db.UnsignedInt
  models_id    Int    @db.UnsignedInt
  itemtype     String @default("") @db.VarChar(255)
  rank         Int
  name         String @db.VarChar(255)
  value        String @db.VarChar(255)
  is_mandatory Int    @default(0) @db.TinyInt
}

model glpi_plugin_datainjection_modelcsvs {
  id                Int    @id @default(autoincrement()) @db.UnsignedInt
  models_id         Int    @db.UnsignedInt
  itemtype          String @default("") @db.VarChar(255)
  delimiter         String @default(";") @db.VarChar(1)
  is_header_present Int    @default(1) @db.TinyInt
}

model glpi_plugin_datainjection_models {
  id                         Int       @id @default(autoincrement()) @db.UnsignedInt
  name                       String    @db.VarChar(255)
  comment                    String?   @db.Text
  date_mod                   DateTime? @db.Timestamp(0)
  date_creation              DateTime? @db.Timestamp(0)
  filetype                   String    @default("csv") @db.VarChar(255)
  itemtype                   String    @default("") @db.VarChar(255)
  entities_id                Int       @default(0) @db.UnsignedInt
  behavior_add               Int       @default(1) @db.TinyInt
  behavior_update            Int       @default(0) @db.TinyInt
  can_add_dropdown           Int       @default(0) @db.TinyInt
  can_overwrite_if_not_empty Int       @default(1)
  is_private                 Int       @default(1) @db.TinyInt
  is_recursive               Int       @default(0) @db.TinyInt
  users_id                   Int       @db.UnsignedInt
  date_format                String    @default("yyyy-mm-dd") @db.VarChar(11)
  float_format               Int       @default(0) @db.TinyInt
  port_unicity               Int       @default(0) @db.TinyInt
  step                       Int       @default(0)
}

model glpi_plugin_fields_containerdisplayconditions {
  id                          Int     @id @default(autoincrement()) @db.UnsignedInt
  plugin_fields_containers_id Int     @default(0) @db.UnsignedInt
  itemtype                    String? @db.VarChar(100)
  search_option               String? @db.VarChar(255)
  condition                   String? @db.VarChar(255)
  value                       String? @db.VarChar(255)
  is_visible                  Int     @default(0) @db.TinyInt

  @@index([plugin_fields_containers_id, itemtype], map: "plugin_fields_containers_id_itemtype")
}

model glpi_plugin_fields_containers {
  id           Int     @id @default(autoincrement()) @db.UnsignedInt
  name         String? @db.VarChar(255)
  label        String? @db.VarChar(255)
  itemtypes    String? @db.LongText
  type         String? @db.VarChar(255)
  subtype      String? @db.VarChar(255)
  entities_id  Int     @default(0) @db.UnsignedInt
  is_recursive Int     @default(0) @db.TinyInt
  is_active    Int     @default(0) @db.TinyInt

  @@index([entities_id], map: "entities_id")
}

model glpi_plugin_fields_fields {
  id                          Int     @id @default(autoincrement()) @db.UnsignedInt
  name                        String? @db.VarChar(255)
  label                       String? @db.VarChar(255)
  type                        String? @db.VarChar(255)
  plugin_fields_containers_id Int     @default(0) @db.UnsignedInt
  ranking                     Int     @default(0)
  default_value               String? @db.LongText
  is_active                   Int     @default(1) @db.TinyInt
  is_readonly                 Int     @default(1) @db.TinyInt
  mandatory                   Int     @default(0) @db.TinyInt
  multiple                    Int     @default(0) @db.TinyInt
  allowed_values              String? @db.Text

  @@index([is_active], map: "is_active")
  @@index([is_readonly], map: "is_readonly")
  @@index([plugin_fields_containers_id], map: "plugin_fields_containers_id")
}

model glpi_plugin_fields_labeltranslations {
  id       Int     @id @default(autoincrement()) @db.UnsignedInt
  itemtype String  @db.VarChar(30)
  items_id Int     @db.UnsignedInt
  language String  @db.VarChar(5)
  label    String? @db.VarChar(255)

  @@unique([itemtype, items_id, language], map: "unicity")
  @@index([items_id], map: "items_id")
  @@index([itemtype], map: "itemtype")
  @@index([language], map: "language")
}

model glpi_plugin_fields_pluginformcreatorcategoryasdasdasdasds {
  id                          Int     @id @default(autoincrement()) @db.UnsignedInt
  items_id                    Int     @db.UnsignedInt
  itemtype                    String? @default("PluginFormcreatorCategory") @db.VarChar(255)
  plugin_fields_containers_id Int     @default(1) @db.UnsignedInt

  @@unique([itemtype, items_id, plugin_fields_containers_id], map: "itemtype_item_container")
}

model glpi_plugin_fields_profiles {
  id                          Int     @id @default(autoincrement()) @db.UnsignedInt
  profiles_id                 Int     @default(0) @db.UnsignedInt
  plugin_fields_containers_id Int     @default(0) @db.UnsignedInt
  right                       String? @db.Char(1)

  @@index([plugin_fields_containers_id], map: "plugin_fields_containers_id")
  @@index([profiles_id], map: "profiles_id")
}

model glpi_plugin_fields_statusoverrides {
  id                      Int     @id @default(autoincrement()) @db.UnsignedInt
  plugin_fields_fields_id Int     @default(0) @db.UnsignedInt
  itemtype                String? @db.VarChar(100)
  states                  String? @db.VarChar(255)
  is_readonly             Int     @default(1) @db.TinyInt
  mandatory               Int     @default(0) @db.TinyInt

  @@index([plugin_fields_fields_id], map: "plugin_fields_fields_id")
}

model glpi_plugin_formcreator_answers {
  id                                Int     @id @default(autoincrement()) @db.UnsignedInt
  plugin_formcreator_formanswers_id Int     @default(0) @db.UnsignedInt
  plugin_formcreator_questions_id   Int     @default(0) @db.UnsignedInt
  answer                            String? @db.LongText

  @@index([plugin_formcreator_formanswers_id], map: "plugin_formcreator_formanswers_id")
  @@index([plugin_formcreator_questions_id], map: "plugin_formcreator_questions_id")
}

model glpi_plugin_formcreator_categories {
  id                               Int     @id @default(autoincrement()) @db.UnsignedInt
  name                             String  @default("") @db.VarChar(255)
  comment                          String? @db.MediumText
  completename                     String? @db.VarChar(255)
  plugin_formcreator_categories_id Int     @default(0) @db.UnsignedInt
  level                            Int     @default(1)
  sons_cache                       String? @db.LongText
  ancestors_cache                  String? @db.LongText
  knowbaseitemcategories_id        Int     @default(0) @db.UnsignedInt

  @@index([knowbaseitemcategories_id], map: "knowbaseitemcategories_id")
  @@index([name], map: "name")
  @@index([plugin_formcreator_categories_id], map: "plugin_formcreator_categories_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_plugin_formcreator_conditions {
  id                              Int     @id @default(autoincrement()) @db.UnsignedInt
  itemtype                        String  @default("") @db.VarChar(255)
  items_id                        Int     @default(0) @db.UnsignedInt
  plugin_formcreator_questions_id Int     @default(0) @db.UnsignedInt
  show_condition                  Int     @default(0)
  show_value                      String? @db.MediumText
  show_logic                      Int     @default(1)
  order                           Int     @default(1)
  uuid                            String? @db.VarChar(255)

  @@index([itemtype, items_id], map: "item")
  @@index([plugin_formcreator_questions_id], map: "plugin_formcreator_questions_id")
}

model glpi_plugin_formcreator_entityconfigs {
  id                      Int     @id @default(autoincrement()) @db.UnsignedInt
  entities_id             Int     @unique(map: "unicity") @default(0) @db.UnsignedInt
  replace_helpdesk        Int     @default(-2)
  default_form_list_mode  Int     @default(-2)
  sort_order              Int     @default(-2)
  is_kb_separated         Int     @default(-2)
  is_search_visible       Int     @default(-2)
  is_dashboard_visible    Int     @default(-2)
  is_header_visible       Int     @default(-2)
  is_search_issue_visible Int     @default(-2)
  tile_design             Int     @default(-2)
  header                  String? @db.Text
  service_catalog_home    Int     @default(-2)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_plugin_formcreator_formanswers {
  id                          Int       @id @default(autoincrement()) @db.UnsignedInt
  name                        String    @default("") @db.VarChar(255)
  entities_id                 Int       @default(0) @db.UnsignedInt
  is_recursive                Boolean   @default(false)
  plugin_formcreator_forms_id Int       @default(0) @db.UnsignedInt
  requester_id                Int       @default(0) @db.UnsignedInt
  users_id_validator          Int       @default(0) @db.UnsignedInt
  groups_id_validator         Int       @default(0) @db.UnsignedInt
  request_date                DateTime? @db.Timestamp(0)
  status                      Int       @default(101)
  comment                     String?   @db.MediumText

  @@index([entities_id, is_recursive], map: "entities_id_is_recursive")
  @@index([groups_id_validator], map: "groups_id_validator")
  @@index([plugin_formcreator_forms_id], map: "plugin_formcreator_forms_id")
  @@index([requester_id], map: "requester_id")
  @@index([users_id_validator], map: "users_id_validator")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_plugin_formcreator_forms {
  id                               Int     @id @default(autoincrement()) @db.UnsignedInt
  name                             String  @default("") @db.VarChar(255)
  entities_id                      Int     @default(0) @db.UnsignedInt
  is_recursive                     Boolean @default(false)
  icon                             String  @default("") @db.VarChar(255)
  icon_color                       String  @default("") @db.VarChar(255)
  background_color                 String  @default("") @db.VarChar(255)
  access_rights                    Boolean @default(true)
  description                      String? @db.VarChar(255)
  content                          String? @db.LongText
  plugin_formcreator_categories_id Int     @default(0) @db.UnsignedInt
  is_active                        Boolean @default(false)
  language                         String  @default("") @db.VarChar(255)
  helpdesk_home                    Boolean @default(false)
  is_deleted                       Boolean @default(false)
  validation_required              Boolean @default(false)
  usage_count                      Int     @default(0)
  is_default                       Boolean @default(false)
  is_captcha_enabled               Boolean @default(false)
  show_rule                        Int     @default(1)
  formanswer_name                  String  @default("") @db.VarChar(255)
  is_visible                       Int     @default(1) @db.TinyInt
  uuid                             String? @db.VarChar(255)

  @@index([entities_id], map: "entities_id")
  @@index([plugin_formcreator_categories_id], map: "plugin_formcreator_categories_id")
  @@fulltext([name, description], map: "Search")
}

model glpi_plugin_formcreator_forms_groups {
  id                          Int     @id @default(autoincrement()) @db.UnsignedInt
  plugin_formcreator_forms_id Int     @db.UnsignedInt
  groups_id                   Int     @db.UnsignedInt
  uuid                        String? @db.VarChar(255)

  @@unique([plugin_formcreator_forms_id, groups_id], map: "unicity")
}

model glpi_plugin_formcreator_forms_languages {
  id                          Int     @id @default(autoincrement()) @db.UnsignedInt
  plugin_formcreator_forms_id Int     @default(0) @db.UnsignedInt
  name                        String? @db.VarChar(255)
  comment                     String? @db.Text
  uuid                        String? @db.VarChar(255)
}

model glpi_plugin_formcreator_forms_profiles {
  id                          Int     @id @default(autoincrement()) @db.UnsignedInt
  plugin_formcreator_forms_id Int     @default(0) @db.UnsignedInt
  profiles_id                 Int     @default(0) @db.UnsignedInt
  uuid                        String? @db.VarChar(255)

  @@unique([plugin_formcreator_forms_id, profiles_id], map: "unicity")
}

model glpi_plugin_formcreator_forms_users {
  id                          Int     @id @default(autoincrement()) @db.UnsignedInt
  plugin_formcreator_forms_id Int     @db.UnsignedInt
  users_id                    Int     @db.UnsignedInt
  uuid                        String? @db.VarChar(255)

  @@unique([plugin_formcreator_forms_id, users_id], map: "unicity")
}

model glpi_plugin_formcreator_forms_validators {
  id                          Int     @id @default(autoincrement()) @db.UnsignedInt
  plugin_formcreator_forms_id Int     @default(0) @db.UnsignedInt
  itemtype                    String  @default("") @db.VarChar(255)
  items_id                    Int     @default(0) @db.UnsignedInt
  uuid                        String? @db.VarChar(255)

  @@unique([plugin_formcreator_forms_id, itemtype, items_id], map: "unicity")
}

model glpi_plugin_formcreator_issues {
  id                 Int       @id @default(autoincrement()) @db.UnsignedInt
  name               String?   @db.VarChar(255)
  display_id         String    @db.VarChar(255)
  items_id           Int       @default(0) @db.UnsignedInt
  itemtype           String    @default("") @db.VarChar(255)
  status             String    @default("") @db.VarChar(255)
  date_creation      DateTime? @db.Timestamp(0)
  date_mod           DateTime? @db.Timestamp(0)
  entities_id        Int       @default(0) @db.UnsignedInt
  is_recursive       Boolean   @default(false)
  requester_id       Int       @default(0) @db.UnsignedInt
  comment            String?   @db.LongText
  users_id_recipient Int       @default(0) @db.UnsignedInt

  @@index([entities_id], map: "entities_id")
  @@index([itemtype, items_id], map: "item")
  @@index([requester_id], map: "requester_id")
}

model glpi_plugin_formcreator_items_targettickets {
  id                                  Int     @id @default(autoincrement()) @db.UnsignedInt
  plugin_formcreator_targettickets_id Int     @default(0) @db.UnsignedInt
  link                                Int     @default(0)
  itemtype                            String  @default("") @db.VarChar(255)
  items_id                            Int     @default(0) @db.UnsignedInt
  uuid                                String? @db.VarChar(255)

  @@index([itemtype, items_id], map: "item")
  @@index([plugin_formcreator_targettickets_id], map: "plugin_formcreator_targettickets_id")
}

model glpi_plugin_formcreator_questiondependencies {
  id                                Int     @id @default(autoincrement()) @db.UnsignedInt
  plugin_formcreator_questions_id   Int     @default(0) @db.UnsignedInt
  plugin_formcreator_questions_id_2 Int     @default(0) @db.UnsignedInt
  fieldname                         String? @db.VarChar(255)
  uuid                              String? @db.VarChar(255)

  @@index([plugin_formcreator_questions_id], map: "plugin_formcreator_questions_id")
  @@index([plugin_formcreator_questions_id_2], map: "plugin_formcreator_questions_id_2")
}

model glpi_plugin_formcreator_questionranges {
  id                              Int     @id @default(autoincrement()) @db.UnsignedInt
  plugin_formcreator_questions_id Int     @default(0) @db.UnsignedInt
  range_min                       String? @db.VarChar(255)
  range_max                       String? @db.VarChar(255)
  fieldname                       String? @db.VarChar(255)
  uuid                            String? @db.VarChar(255)

  @@index([plugin_formcreator_questions_id], map: "plugin_formcreator_questions_id")
}

model glpi_plugin_formcreator_questionregexes {
  id                              Int     @id @default(autoincrement()) @db.UnsignedInt
  plugin_formcreator_questions_id Int     @default(0) @db.UnsignedInt
  regex                           String? @db.MediumText
  fieldname                       String? @db.VarChar(255)
  uuid                            String? @db.VarChar(255)

  @@index([plugin_formcreator_questions_id], map: "plugin_formcreator_questions_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_plugin_formcreator_questions {
  id                             Int     @id @default(autoincrement()) @db.UnsignedInt
  name                           String  @default("") @db.VarChar(255)
  plugin_formcreator_sections_id Int     @default(0) @db.UnsignedInt
  fieldtype                      String  @default("text") @db.VarChar(30)
  required                       Boolean @default(false)
  show_empty                     Boolean @default(false)
  default_values                 String? @db.MediumText
  itemtype                       String  @default("") @db.VarChar(255)
  values                         String? @db.MediumText
  description                    String? @db.MediumText
  row                            Int     @default(0)
  col                            Int     @default(0)
  width                          Int     @default(0)
  show_rule                      Int     @default(1)
  uuid                           String? @db.VarChar(255)

  @@index([plugin_formcreator_sections_id], map: "plugin_formcreator_sections_id")
  @@fulltext([name, description], map: "Search")
}

model glpi_plugin_formcreator_sections {
  id                          Int     @id @default(autoincrement()) @db.UnsignedInt
  name                        String  @default("") @db.VarChar(255)
  plugin_formcreator_forms_id Int     @default(0) @db.UnsignedInt
  order                       Int     @default(0)
  show_rule                   Int     @default(1)
  uuid                        String? @db.VarChar(255)

  @@index([plugin_formcreator_forms_id], map: "plugin_formcreator_forms_id")
}

model glpi_plugin_formcreator_targetchanges {
  id                             Int     @id @default(autoincrement()) @db.UnsignedInt
  name                           String  @default("") @db.VarChar(255)
  plugin_formcreator_forms_id    Int     @default(0) @db.UnsignedInt
  target_name                    String  @default("") @db.VarChar(255)
  changetemplates_id             Int     @default(0) @db.UnsignedInt
  content                        String? @db.LongText
  impactcontent                  String? @db.LongText
  controlistcontent              String? @db.LongText
  rolloutplancontent             String? @db.LongText
  backoutplancontent             String? @db.LongText
  checklistcontent               String? @db.LongText
  due_date_rule                  Int     @default(1)
  due_date_question              Int     @default(0) @db.UnsignedInt
  due_date_value                 Int?    @db.TinyInt
  due_date_period                Int     @default(0)
  urgency_rule                   Int     @default(1)
  urgency_question               Int     @default(0) @db.UnsignedInt
  validation_followup            Boolean @default(true)
  destination_entity             Int     @default(1)
  destination_entity_value       Int     @default(0) @db.UnsignedInt
  tag_type                       Int     @default(1)
  tag_questions                  String  @default("") @db.VarChar(255)
  tag_specifics                  String  @default("") @db.VarChar(255)
  category_rule                  Int     @default(1)
  category_question              Int     @default(0) @db.UnsignedInt
  commonitil_validation_rule     Int     @default(1)
  commonitil_validation_question String? @db.VarChar(255)
  show_rule                      Int     @default(1)
  sla_rule                       Int     @default(1)
  sla_question_tto               Int     @default(0) @db.UnsignedInt
  sla_question_ttr               Int     @default(0) @db.UnsignedInt
  ola_rule                       Int     @default(1)
  ola_question_tto               Int     @default(0) @db.UnsignedInt
  ola_question_ttr               Int     @default(0) @db.UnsignedInt
  uuid                           String? @db.VarChar(255)
}

model glpi_plugin_formcreator_targetproblems {
  id                          Int     @id @default(autoincrement()) @db.UnsignedInt
  name                        String  @default("") @db.VarChar(255)
  plugin_formcreator_forms_id Int     @default(0) @db.UnsignedInt
  target_name                 String  @default("") @db.VarChar(255)
  problemtemplates_id         Int     @default(0) @db.UnsignedInt
  content                     String? @db.LongText
  impactcontent               String? @db.LongText
  causecontent                String? @db.LongText
  symptomcontent              String? @db.LongText
  urgency_rule                Int     @default(1)
  urgency_question            Int     @default(0) @db.UnsignedInt
  destination_entity          Int     @default(1)
  destination_entity_value    Int     @default(0) @db.UnsignedInt
  tag_type                    Int     @default(1)
  tag_questions               String  @default("") @db.VarChar(255)
  tag_specifics               String  @default("") @db.VarChar(255)
  category_rule               Int     @default(1)
  category_question           Int     @default(0) @db.UnsignedInt
  show_rule                   Int     @default(1)
  uuid                        String? @db.VarChar(255)

  @@index([problemtemplates_id], map: "problemtemplates_id")
}

model glpi_plugin_formcreator_targets_actors {
  id               Int     @id @default(autoincrement()) @db.UnsignedInt
  itemtype         String? @db.VarChar(255)
  items_id         Int     @default(0) @db.UnsignedInt
  actor_role       Int     @default(1)
  actor_type       Int     @default(1)
  actor_value      Int     @default(0) @db.UnsignedInt
  use_notification Boolean @default(true)
  uuid             String? @db.VarChar(255)

  @@index([itemtype, items_id], map: "item")
}

model glpi_plugin_formcreator_targettickets {
  id                             Int     @id @default(autoincrement()) @db.UnsignedInt
  name                           String  @default("") @db.VarChar(255)
  plugin_formcreator_forms_id    Int     @default(0) @db.UnsignedInt
  target_name                    String  @default("") @db.VarChar(255)
  source_rule                    Int     @default(0)
  source_question                Int     @default(0)
  type_rule                      Int     @default(0)
  type_question                  Int     @default(0) @db.UnsignedInt
  tickettemplates_id             Int     @default(0) @db.UnsignedInt
  content                        String? @db.LongText
  due_date_rule                  Int     @default(1)
  due_date_question              Int     @default(0) @db.UnsignedInt
  due_date_value                 Int?    @db.TinyInt
  due_date_period                Int     @default(0)
  urgency_rule                   Int     @default(1)
  urgency_question               Int     @default(0) @db.UnsignedInt
  validation_followup            Boolean @default(true)
  destination_entity             Int     @default(1)
  destination_entity_value       Int     @default(0) @db.UnsignedInt
  tag_type                       Int     @default(1)
  tag_questions                  String  @default("") @db.VarChar(255)
  tag_specifics                  String  @default("") @db.VarChar(255)
  category_rule                  Int     @default(1)
  category_question              Int     @default(0) @db.UnsignedInt
  associate_rule                 Int     @default(1)
  associate_question             Int     @default(0) @db.UnsignedInt
  location_rule                  Int     @default(1)
  location_question              Int     @default(0) @db.UnsignedInt
  commonitil_validation_rule     Int     @default(1)
  commonitil_validation_question String? @db.VarChar(255)
  show_rule                      Int     @default(1)
  sla_rule                       Int     @default(1)
  sla_question_tto               Int     @default(0) @db.UnsignedInt
  sla_question_ttr               Int     @default(0) @db.UnsignedInt
  ola_rule                       Int     @default(1)
  ola_question_tto               Int     @default(0) @db.UnsignedInt
  ola_question_ttr               Int     @default(0) @db.UnsignedInt
  uuid                           String? @db.VarChar(255)

  @@index([tickettemplates_id], map: "tickettemplates_id")
}

model glpi_plugin_itilcategorygroups_categories {
  id                    Int       @id @default(autoincrement()) @db.UnsignedInt
  is_active             Int       @default(0) @db.TinyInt
  name                  String?   @default("") @db.VarChar(255)
  comment               String?   @db.Text
  date_mod              DateTime? @db.Date
  itilcategories_id     Int       @default(0) @db.UnsignedInt
  view_all_lvl1         Int       @default(0) @db.TinyInt
  view_all_lvl2         Int       @default(0) @db.TinyInt
  view_all_lvl3         Int       @default(0) @db.TinyInt
  view_all_lvl4         Int       @default(0) @db.TinyInt
  entities_id           Int       @default(0) @db.UnsignedInt
  is_recursive          Int       @default(1) @db.TinyInt
  is_incident           Int       @default(1) @db.TinyInt
  is_request            Int       @default(1) @db.TinyInt
  is_groups_restriction Int       @default(0) @db.TinyInt

  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([is_incident], map: "is_incident")
  @@index([is_recursive], map: "is_recursive")
  @@index([is_request], map: "is_request")
  @@index([itilcategories_id], map: "itilcategories_id")
}

model glpi_plugin_itilcategorygroups_categories_groups {
  id                                      Int @id @default(autoincrement()) @db.UnsignedInt
  plugin_itilcategorygroups_categories_id Int @default(0) @db.UnsignedInt
  level                                   Int @default(0) @db.TinyInt
  itilcategories_id                       Int @default(0) @db.UnsignedInt
  groups_id                               Int @default(0) @db.UnsignedInt

  @@unique([plugin_itilcategorygroups_categories_id, level, groups_id], map: "group_lvl_unicity")
  @@index([groups_id], map: "groups_id")
  @@index([itilcategories_id], map: "itilcategories_id")
  @@index([level], map: "level")
  @@index([plugin_itilcategorygroups_categories_id], map: "plugin_itilcategorygroups_categories_id")
}

model glpi_plugin_itilcategorygroups_groups_levels {
  id        Int  @id @default(autoincrement()) @db.UnsignedInt
  groups_id Int  @db.UnsignedInt
  lvl       Int?

  @@index([groups_id], map: "groups_id")
  @@index([lvl], map: "lvl")
}

model glpi_plugin_metademands_basketlines {
  id                                Int     @id @default(autoincrement()) @db.UnsignedInt
  users_id                          Int     @default(0) @db.UnsignedInt
  plugin_metademands_metademands_id Int     @default(0) @db.UnsignedInt
  plugin_metademands_fields_id      Int     @default(0) @db.UnsignedInt
  line                              Int     @default(0)
  name                              String? @db.VarChar(150)
  value                             String? @db.Text
  value2                            String? @db.Text

  @@unique([plugin_metademands_metademands_id, plugin_metademands_fields_id, line, name, users_id], map: "unicity")
  @@index([plugin_metademands_fields_id], map: "plugin_metademands_fields_id")
  @@index([plugin_metademands_metademands_id], map: "plugin_metademands_metademands_id")
  @@index([users_id], map: "users_id")
}

model glpi_plugin_metademands_basketobjects {
  id                                      Int     @id @default(autoincrement()) @db.UnsignedInt
  name                                    String? @db.VarChar(255)
  description                             String? @db.LongText
  reference                               String? @db.VarChar(255)
  plugin_metademands_basketobjecttypes_id Int     @default(0) @db.UnsignedInt

  @@index([plugin_metademands_basketobjecttypes_id], map: "plugin_metademands_basketobjecttypes_id")
}

model glpi_plugin_metademands_basketobjecttranslations {
  id       Int     @id @default(autoincrement()) @db.UnsignedInt
  items_id Int     @default(0) @db.UnsignedInt
  itemtype String? @db.VarChar(100)
  field    String? @db.VarChar(100)
  language String? @db.VarChar(5)
  value    String? @db.Text
}

model glpi_plugin_metademands_basketobjecttypes {
  id   Int     @id @default(autoincrement()) @db.UnsignedInt
  name String? @db.VarChar(255)
}

model glpi_plugin_metademands_basketobjecttypetranslations {
  id       Int     @id @default(autoincrement()) @db.UnsignedInt
  items_id Int     @default(0) @db.UnsignedInt
  itemtype String? @db.VarChar(100)
  language String? @db.VarChar(5)
  field    String? @db.VarChar(100)
  value    String? @db.Text
}

model glpi_plugin_metademands_conditions {
  id                                Int     @id @default(autoincrement()) @db.UnsignedInt
  plugin_metademands_fields_id      Int     @default(0) @db.UnsignedInt
  plugin_metademands_metademands_id Int     @default(0) @db.UnsignedInt
  items_id                          Int     @default(0) @db.UnsignedInt
  item                              String? @db.VarChar(255)
  check_value                       String? @db.VarChar(255)
  show_logic                        Int     @default(1)
  show_condition                    Int     @default(0)
  order                             Int     @default(0)
  type                              String? @db.VarChar(255)

  @@index([plugin_metademands_fields_id], map: "plugin_metademands_fields_id")
  @@index([plugin_metademands_metademands_id], map: "plugin_metademands_metademands_id")
}

model glpi_plugin_metademands_configs {
  id                                Int     @id @default(autoincrement()) @db.UnsignedInt
  simpleticket_to_metademand        Int?    @default(0) @db.TinyInt
  parent_ticket_tag                 String? @db.VarChar(255)
  son_ticket_tag                    String? @db.VarChar(255)
  create_pdf                        Int?    @default(0) @db.TinyInt
  show_requester_informations       Int?    @default(0) @db.TinyInt
  childs_parent_content             Int?    @default(0) @db.TinyInt
  display_type                      Int?    @default(1) @db.TinyInt
  display_buttonlist_servicecatalog Int?    @default(1) @db.TinyInt
  title_servicecatalog              String? @db.VarChar(255)
  comment_servicecatalog            String? @db.Text
  fa_servicecatalog                 String  @default("fas fa-share-alt") @db.VarChar(100)
  languageTech                      String? @db.VarChar(100)
  use_draft                         Int?    @default(0) @db.TinyInt
  show_form_changes                 Int     @default(0) @db.TinyInt
  add_groups_with_regex             Int     @default(0) @db.TinyInt
}

model glpi_plugin_metademands_configsteps {
  id                                Int @id @default(autoincrement()) @db.UnsignedInt
  plugin_metademands_metademands_id Int @default(0) @db.UnsignedInt
  link_user_block                   Int @default(0) @db.TinyInt
  multiple_link_groups_blocks       Int @default(0) @db.TinyInt
  add_user_as_requester             Int @default(0) @db.TinyInt
  step_by_step_interface            Int @default(0) @db.TinyInt

  @@index([plugin_metademands_metademands_id], map: "plugin_metademands_metademands_id")
}

model glpi_plugin_metademands_drafts {
  id                                Int      @id @default(autoincrement()) @db.UnsignedInt
  name                              String   @default("0") @db.VarChar(255)
  plugin_metademands_metademands_id Int      @default(0) @db.UnsignedInt
  users_id                          Int      @default(0) @db.UnsignedInt
  date                              DateTime @default(now()) @db.Timestamp(0)

  @@index([plugin_metademands_metademands_id], map: "plugin_metademands_metademands_id")
}

model glpi_plugin_metademands_drafts_values {
  id                           Int    @id @default(autoincrement()) @db.UnsignedInt
  plugin_metademands_drafts_id Int    @default(0) @db.UnsignedInt
  plugin_metademands_fields_id Int    @default(0) @db.UnsignedInt
  value                        String @db.Text
  value2                       String @db.Text

  @@index([plugin_metademands_drafts_id], map: "plugin_metademands_drafts_id")
  @@index([plugin_metademands_fields_id], map: "plugin_metademands_fields_id")
}

model glpi_plugin_metademands_fieldcustomvalues {
  id                           Int     @id @default(autoincrement()) @db.UnsignedInt
  plugin_metademands_fields_id Int     @default(0) @db.UnsignedInt
  name                         String  @default("0") @db.VarChar(255)
  is_default                   Int     @default(0)
  comment                      String? @db.Text
  rank                         Int     @default(0)

  @@index([plugin_metademands_fields_id], map: "plugin_metademands_fields_id")
}

model glpi_plugin_metademands_fieldoptions {
  id                           Int    @id @default(autoincrement()) @db.UnsignedInt
  plugin_metademands_fields_id Int    @default(0) @db.UnsignedInt
  check_value                  Int    @default(0)
  plugin_metademands_tasks_id  Int    @default(0) @db.UnsignedInt
  fields_link                  Int    @default(0) @db.UnsignedInt
  hidden_link                  Int    @default(0) @db.UnsignedInt
  hidden_block                 Int    @default(0) @db.UnsignedInt
  users_id_validate            Int    @default(0) @db.UnsignedInt
  childs_blocks                String @default("[]") @db.VarChar(255)
  checkbox_value               Int    @default(0) @db.UnsignedInt
  checkbox_id                  Int    @default(0) @db.UnsignedInt
  parent_field_id              Int    @default(0) @db.UnsignedInt

  @@index([plugin_metademands_fields_id], map: "plugin_metademands_fields_id")
  @@index([plugin_metademands_tasks_id], map: "plugin_metademands_tasks_id")
}

model glpi_plugin_metademands_fieldparameters {
  id                                  Int     @id @default(autoincrement()) @db.UnsignedInt
  plugin_metademands_fields_id        Int     @default(0) @db.UnsignedInt
  custom                              String? @db.Text
  default                             String? @db.Text
  hide_title                          Int     @default(0) @db.TinyInt
  is_mandatory                        Int     @default(0)
  max_upload                          Int     @default(0)
  regex                               String  @default("") @db.VarChar(255)
  color                               String? @db.VarChar(255)
  row_display                         Int?    @default(0) @db.TinyInt
  is_basket                           Int?    @default(0) @db.TinyInt
  display_type                        Int?    @default(0)
  used_by_ticket                      Int     @default(0)
  used_by_child                       Int?    @default(0) @db.TinyInt
  link_to_user                        Int?    @default(0)
  default_use_id_requester            Int?    @default(0) @db.UnsignedInt
  default_use_id_requester_supervisor Int?    @default(0) @db.UnsignedInt
  use_future_date                     Int?    @default(0) @db.TinyInt
  use_date_now                        Int?    @default(0) @db.TinyInt
  additional_number_day               Int?    @default(0)
  informations_to_display             String  @default("[]") @db.VarChar(255)
  use_richtext                        Int     @default(1) @db.TinyInt
  icon                                String? @db.VarChar(255)
  readonly                            Int?    @default(0) @db.TinyInt
  hidden                              Int?    @default(0) @db.TinyInt

  @@index([plugin_metademands_fields_id], map: "plugin_metademands_fields_id")
}

model glpi_plugin_metademands_fields {
  id                                Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id                       Int       @default(0) @db.UnsignedInt
  is_recursive                      Int       @default(0)
  comment                           String?   @db.Text
  rank                              Int       @default(0)
  order                             Int       @default(0)
  name                              String?   @db.VarChar(255)
  label2                            String?   @db.Text
  type                              String?   @db.VarChar(255)
  item                              String?   @db.VarChar(255)
  plugin_metademands_fields_id      Int       @default(0) @db.UnsignedInt
  plugin_metademands_metademands_id Int       @default(0) @db.UnsignedInt
  date_creation                     DateTime? @db.Timestamp(0)
  date_mod                          DateTime? @db.Timestamp(0)

  @@index([plugin_metademands_fields_id], map: "plugin_metademands_fields_id")
  @@index([plugin_metademands_metademands_id], map: "plugin_metademands_metademands_id")
}

model glpi_plugin_metademands_fieldtranslations {
  id       Int     @id @default(autoincrement()) @db.UnsignedInt
  items_id Int     @default(0) @db.UnsignedInt
  itemtype String? @db.VarChar(100)
  language String? @db.VarChar(5)
  field    String? @db.VarChar(100)
  value    String? @db.Text
}

model glpi_plugin_metademands_forms {
  id                                Int      @id @default(autoincrement()) @db.UnsignedInt
  name                              String   @default("0") @db.VarChar(255)
  plugin_metademands_metademands_id Int      @default(0) @db.UnsignedInt
  items_id                          Int      @default(0) @db.UnsignedInt
  itemtype                          String?  @db.VarChar(100)
  users_id                          Int      @default(0) @db.UnsignedInt
  date                              DateTime @default(now()) @db.Timestamp(0)
  is_model                          Int      @default(0) @db.TinyInt
  resources_id                      Int      @default(0) @db.UnsignedInt

  @@index([plugin_metademands_metademands_id], map: "plugin_metademands_metademands_id")
}

model glpi_plugin_metademands_forms_values {
  id                           Int    @id @default(autoincrement()) @db.UnsignedInt
  plugin_metademands_forms_id  Int    @default(0) @db.UnsignedInt
  plugin_metademands_fields_id Int    @default(0) @db.UnsignedInt
  value                        String @db.Text
  value2                       String @db.Text

  @@index([plugin_metademands_fields_id], map: "plugin_metademands_fields_id")
  @@index([plugin_metademands_forms_id], map: "plugin_metademands_forms_id")
}

model glpi_plugin_metademands_groupconfigs {
  id                                Int @id @default(autoincrement()) @db.UnsignedInt
  entities_id                       Int @default(0) @db.UnsignedInt
  visibility                        Int @default(0)
  plugin_metademands_metademands_id Int @default(0) @db.UnsignedInt

  @@index([entities_id], map: "entities_id")
  @@index([plugin_metademands_metademands_id], map: "plugin_metademands_metademands_id")
}

model glpi_plugin_metademands_groups {
  id                                Int @id @default(autoincrement()) @db.UnsignedInt
  entities_id                       Int @default(0) @db.UnsignedInt
  groups_id                         Int @default(0) @db.UnsignedInt
  plugin_metademands_metademands_id Int @default(0) @db.UnsignedInt

  @@index([entities_id], map: "entities_id")
  @@index([groups_id], map: "groups_id")
  @@index([plugin_metademands_metademands_id], map: "plugin_metademands_metademands_id")
}

model glpi_plugin_metademands_interticketfollowups {
  id                Int       @id @default(autoincrement()) @db.UnsignedInt
  tickets_id        Int       @default(0) @db.UnsignedInt
  targets_id        Int       @default(0) @db.UnsignedInt
  date              DateTime? @db.Timestamp(0)
  users_id          Int       @default(0) @db.UnsignedInt
  users_id_editor   Int       @default(0) @db.UnsignedInt
  content           String?   @db.LongText
  is_private        Int       @default(0) @db.TinyInt
  requesttypes_id   Int       @default(0) @db.UnsignedInt
  date_mod          DateTime? @db.Timestamp(0)
  date_creation     DateTime? @db.Timestamp(0)
  timeline_position Int       @default(0) @db.TinyInt
}

model glpi_plugin_metademands_mailtasks {
  id                          Int     @id @default(autoincrement()) @db.UnsignedInt
  content                     String? @db.Text
  users_id_recipient          Int     @default(0) @db.UnsignedInt
  groups_id_recipient         Int     @default(0) @db.UnsignedInt
  plugin_metademands_tasks_id Int     @default(0) @db.UnsignedInt
  itilcategories_id           Int     @default(0) @db.UnsignedInt
  email                       String? @db.VarChar(255)

  @@index([groups_id_recipient], map: "groups_id_recipient")
  @@index([itilcategories_id], map: "itilcategories_id")
  @@index([plugin_metademands_tasks_id], map: "plugin_metademands_tasks_id")
  @@index([users_id_recipient], map: "users_id_recipient")
}

model glpi_plugin_metademands_metademands {
  id                               Int       @id @default(autoincrement()) @db.UnsignedInt
  name                             String?   @db.VarChar(255)
  entities_id                      Int       @default(0) @db.UnsignedInt
  is_recursive                     Int       @default(0)
  is_template                      Int       @default(0) @db.TinyInt
  template_name                    String?   @db.VarChar(255)
  is_active                        Int       @default(1) @db.TinyInt
  maintenance_mode                 Int       @default(0) @db.TinyInt
  can_update                       Int       @default(0) @db.TinyInt
  can_clone                        Int       @default(0) @db.TinyInt
  comment                          String?   @db.Text
  object_to_create                 String?   @db.VarChar(255)
  type                             Int       @default(0)
  itilcategories_id                String    @db.Text
  icon                             String?   @db.VarChar(255)
  is_order                         Int?      @default(0) @db.TinyInt
  create_one_ticket                Int       @default(0) @db.TinyInt
  force_create_tasks               Int       @default(0) @db.TinyInt
  date_creation                    DateTime? @db.Timestamp(0)
  date_mod                         DateTime? @db.Timestamp(0)
  validation_subticket             Int       @default(0) @db.TinyInt
  is_deleted                       Int       @default(0) @db.TinyInt
  hide_no_field                    Int?      @default(0) @db.TinyInt
  title_color                      String?   @default("#000000") @db.VarChar(255)
  background_color                 String?   @default("#FFFFFF") @db.VarChar(255)
  step_by_step_mode                Int       @default(0) @db.TinyInt
  show_rule                        Int       @default(1) @db.TinyInt
  initial_requester_childs_tickets Int       @default(1) @db.TinyInt
  is_basket                        Int?      @default(0) @db.TinyInt

  @@index([entities_id], map: "entities_id")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_recursive], map: "is_recursive")
  @@index([is_template], map: "is_template")
  @@index([name], map: "name")
}

model glpi_plugin_metademands_metademands_resources {
  id                                Int @id @default(autoincrement()) @db.UnsignedInt
  entities_id                       Int @default(0) @db.UnsignedInt
  plugin_resources_contracttypes_id Int @default(0) @db.UnsignedInt
  plugin_metademands_metademands_id Int @default(0) @db.UnsignedInt

  @@index([entities_id], map: "entities_id")
  @@index([plugin_metademands_metademands_id], map: "plugin_metademands_metademands_id")
  @@index([plugin_resources_contracttypes_id], map: "plugin_resources_contracttypes_id")
}

model glpi_plugin_metademands_metademandtasks {
  id                                Int @id @default(autoincrement()) @db.UnsignedInt
  entities_id                       Int @default(0) @db.UnsignedInt
  plugin_metademands_metademands_id Int @default(0) @db.UnsignedInt
  plugin_metademands_tasks_id       Int @default(0) @db.UnsignedInt

  @@index([entities_id], map: "entities_id")
  @@index([plugin_metademands_metademands_id], map: "plugin_metademands_metademands_id")
  @@index([plugin_metademands_tasks_id], map: "plugin_metademands_tasks_id")
}

model glpi_plugin_metademands_metademandtranslations {
  id       Int     @id @default(autoincrement()) @db.UnsignedInt
  items_id Int     @default(0) @db.UnsignedInt
  itemtype String? @db.VarChar(100)
  language String? @db.VarChar(5)
  field    String? @db.VarChar(100)
  value    String? @db.Text
}

model glpi_plugin_metademands_metademandvalidations {
  id                                Int      @id @default(autoincrement()) @db.UnsignedInt
  tickets_id                        Int      @default(0) @db.UnsignedInt
  plugin_metademands_metademands_id Int      @default(0) @db.UnsignedInt
  users_id                          Int      @default(0) @db.UnsignedInt
  validate                          Int      @default(0) @db.TinyInt
  date                              DateTime @default(now()) @db.Timestamp(0)
  tickets_to_create                 String   @db.Text

  @@index([plugin_metademands_metademands_id], map: "plugin_metademands_metademands_id")
  @@index([tickets_id], map: "tickets_id")
  @@index([users_id], map: "users_id")
}

model glpi_plugin_metademands_pluginfields {
  id                                Int @id @default(autoincrement()) @db.UnsignedInt
  plugin_fields_fields_id           Int @default(0) @db.UnsignedInt
  plugin_metademands_fields_id      Int @default(0) @db.UnsignedInt
  plugin_metademands_metademands_id Int @default(0) @db.UnsignedInt

  @@index([plugin_fields_fields_id], map: "plugin_fields_fields_id")
  @@index([plugin_metademands_fields_id], map: "plugin_metademands_fields_id")
  @@index([plugin_metademands_metademands_id], map: "plugin_metademands_metademands_id")
}

model glpi_plugin_metademands_stepforms {
  id                                Int       @id @default(autoincrement()) @db.UnsignedInt
  name                              String    @default("0") @db.VarChar(255)
  plugin_metademands_metademands_id Int       @default(0) @db.UnsignedInt
  items_id                          Int       @default(0) @db.UnsignedInt
  users_id                          Int       @default(0) @db.UnsignedInt
  groups_id_dest                    Int       @default(0) @db.UnsignedInt
  users_id_dest                     Int       @default(0) @db.UnsignedInt
  date                              DateTime? @db.Timestamp(0)
  reminder_date                     DateTime? @db.Timestamp(0)
  block_id                          Int       @default(0) @db.UnsignedInt

  @@index([plugin_metademands_metademands_id], map: "plugin_metademands_metademands_id")
}

model glpi_plugin_metademands_stepforms_actors {
  id                              Int @id @default(autoincrement()) @db.UnsignedInt
  plugin_metademands_stepforms_id Int @default(0) @db.UnsignedInt
  users_id                        Int @default(0) @db.UnsignedInt

  @@index([plugin_metademands_stepforms_id], map: "plugin_metademands_stepforms_id")
  @@index([users_id], map: "users_id")
}

model glpi_plugin_metademands_stepforms_values {
  id                              Int     @id @default(autoincrement()) @db.UnsignedInt
  plugin_metademands_stepforms_id Int     @default(0) @db.UnsignedInt
  plugin_metademands_fields_id    Int     @default(0) @db.UnsignedInt
  value                           String? @db.Text
  value2                          String? @db.Text

  @@index([plugin_metademands_fields_id], map: "plugin_metademands_fields_id")
  @@index([plugin_metademands_stepforms_id], map: "plugin_metademands_stepforms_id")
}

model glpi_plugin_metademands_steps {
  id                                Int     @id @default(autoincrement()) @db.UnsignedInt
  plugin_metademands_metademands_id Int     @default(0) @db.UnsignedInt
  block_id                          Int     @default(0) @db.UnsignedInt
  groups_id                         Int     @default(0) @db.UnsignedInt
  reminder_delay                    String? @db.Text
  message                           String? @db.Text

  @@index([plugin_metademands_metademands_id], map: "plugin_metademands_metademands_id")
}

model glpi_plugin_metademands_tasks {
  id                                Int     @id @default(autoincrement()) @db.UnsignedInt
  name                              String? @db.VarChar(255)
  completename                      String? @db.VarChar(255)
  comment                           String? @db.Text
  entities_id                       Int     @default(0) @db.UnsignedInt
  is_recursive                      Int     @default(0)
  level                             Int     @default(0)
  type                              Int     @default(0)
  ancestors_cache                   String? @db.Text
  sons_cache                        String? @db.Text
  plugin_metademands_tasks_id       Int     @default(0) @db.UnsignedInt
  plugin_metademands_metademands_id Int     @default(0) @db.UnsignedInt
  block_use                         String  @default("[]") @db.VarChar(255)
  useBlock                          Int     @default(1) @db.TinyInt
  formatastable                     Int     @default(1) @db.TinyInt
  block_parent_ticket_resolution    Int     @default(1) @db.TinyInt

  @@index([entities_id], map: "entities_id")
  @@index([plugin_metademands_metademands_id], map: "plugin_metademands_metademands_id")
  @@index([plugin_metademands_tasks_id], map: "plugin_metademands_tasks_id")
  @@index([type], map: "type")
}

model glpi_plugin_metademands_ticketfields {
  id                                Int     @id @default(autoincrement()) @db.UnsignedInt
  num                               Int?
  value                             String? @db.Text
  entities_id                       Int     @default(0) @db.UnsignedInt
  is_recursive                      Int     @default(0)
  is_mandatory                      Int     @default(0)
  is_deletable                      Int     @default(1)
  plugin_metademands_metademands_id Int     @default(0) @db.UnsignedInt

  @@index([entities_id], map: "entities_id")
  @@index([plugin_metademands_metademands_id], map: "plugin_metademands_metademands_id")
}

model glpi_plugin_metademands_tickets_fields {
  id                           Int     @id @default(autoincrement()) @db.UnsignedInt
  value                        String? @db.Text
  value2                       String? @db.Text
  tickets_id                   Int     @default(0) @db.UnsignedInt
  plugin_metademands_fields_id Int     @default(0) @db.UnsignedInt

  @@index([plugin_metademands_fields_id], map: "plugin_metademands_fields_id")
  @@index([tickets_id], map: "tickets_id")
}

model glpi_plugin_metademands_tickets_metademands {
  id                                Int @id @default(autoincrement()) @db.UnsignedInt
  plugin_metademands_metademands_id Int @default(0) @db.UnsignedInt
  tickets_id                        Int @default(0) @db.UnsignedInt
  parent_tickets_id                 Int @default(0) @db.UnsignedInt
  tickettemplates_id                Int @default(0) @db.UnsignedInt
  status                            Int @default(1) @db.TinyInt

  @@index([parent_tickets_id], map: "parent_tickets_id")
  @@index([plugin_metademands_metademands_id], map: "plugin_metademands_metademands_id")
  @@index([tickets_id], map: "tickets_id")
  @@index([tickettemplates_id], map: "tickettemplates_id")
}

model glpi_plugin_metademands_tickets_tasks {
  id                          Int @id @default(autoincrement()) @db.UnsignedInt
  plugin_metademands_tasks_id Int @default(0) @db.UnsignedInt
  level                       Int @default(0)
  tickets_id                  Int @default(0) @db.UnsignedInt
  parent_tickets_id           Int @default(0) @db.UnsignedInt

  @@index([parent_tickets_id], map: "parent_tickets_id")
  @@index([plugin_metademands_tasks_id], map: "plugin_metademands_tasks_id")
  @@index([tickets_id], map: "tickets_id")
}

model glpi_plugin_metademands_tickettasks {
  id                          Int     @id @default(autoincrement()) @db.UnsignedInt
  entities_id                 Int     @default(0) @db.UnsignedInt
  content                     String? @db.Text
  itilcategories_id           Int?    @default(0) @db.UnsignedInt
  type                        Int     @default(0)
  status                      String? @db.VarChar(255)
  actiontime                  Int     @default(0)
  requesttypes_id             Int     @default(0) @db.UnsignedInt
  groups_id_assign            Int     @default(0) @db.UnsignedInt
  users_id_assign             Int     @default(0) @db.UnsignedInt
  groups_id_requester         Int     @default(0) @db.UnsignedInt
  users_id_requester          Int     @default(0) @db.UnsignedInt
  groups_id_observer          Int     @default(0) @db.UnsignedInt
  users_id_observer           Int     @default(0) @db.UnsignedInt
  plugin_metademands_tasks_id Int     @default(0) @db.UnsignedInt

  @@index([groups_id_assign], map: "groups_id_assign")
  @@index([groups_id_observer], map: "groups_id_observer")
  @@index([groups_id_requester], map: "groups_id_requester")
  @@index([itilcategories_id], map: "itilcategories_id")
  @@index([plugin_metademands_tasks_id], map: "plugin_metademands_tasks_id")
  @@index([users_id_assign], map: "users_id_assign")
  @@index([users_id_observer], map: "users_id_observer")
  @@index([users_id_requester], map: "users_id_requester")
}

model glpi_plugin_mycustomview_config {
  id          Int  @id @default(autoincrement()) @db.UnsignedInt
  max_filters Int? @db.TinyInt
}

model glpi_plugin_mycustomview_savedsearch_list {
  id             Int  @id @default(autoincrement()) @db.UnsignedInt
  user_id        Int  @db.UnsignedInt
  savedsearch_id Int
  order          Int? @db.TinyInt
  screen_mode    Int? @db.TinyInt
  height         Int?
}

model glpi_plugin_mycustomview_user_settings {
  id              Int  @id @default(autoincrement()) @db.UnsignedInt
  user_id         Int  @db.UnsignedInt
  default_page    Int? @db.TinyInt
  list_limit      Int?
  settings_hidden Int? @db.TinyInt
}

model glpi_plugin_mydashboard_alerts {
  id                Int @id @default(autoincrement()) @db.UnsignedInt
  reminders_id      Int @db.UnsignedInt
  impact            Int @db.TinyInt
  type              Int @db.TinyInt
  is_public         Int @db.TinyInt
  itilcategories_id Int @default(0) @db.UnsignedInt
}

model glpi_plugin_mydashboard_configs {
  id                        Int     @id @default(autoincrement()) @db.UnsignedInt
  enable_fullscreen         Int     @default(1) @db.TinyInt
  display_menu              Int     @default(1) @db.TinyInt
  replace_central           Int     @default(0) @db.UnsignedInt
  google_api_key            String? @db.VarChar(255)
  impact_1                  String  @default("#228b22") @db.VarChar(200)
  impact_2                  String  @default("#fff03a") @db.VarChar(200)
  impact_3                  String  @default("#ffa500") @db.VarChar(200)
  impact_4                  String  @default("#cd5c5c") @db.VarChar(200)
  impact_5                  String  @default("#8b0000") @db.VarChar(200)
  levelCat                  Int     @default(2) @db.UnsignedInt
  title_alerts_widget       String? @db.VarChar(255)
  title_maintenances_widget String? @db.VarChar(255)
  title_informations_widget String? @db.VarChar(255)
}

model glpi_plugin_mydashboard_configtranslations {
  id       Int     @id @default(autoincrement()) @db.UnsignedInt
  items_id Int     @default(0) @db.UnsignedInt
  itemtype String? @db.VarChar(100)
  language String? @db.VarChar(5)
  field    String? @db.VarChar(100)
  value    String? @db.Text
}

model glpi_plugin_mydashboard_customswidgets {
  id      Int     @id @default(autoincrement()) @db.UnsignedInt
  name    String  @db.VarChar(255)
  comment String? @db.Text
  content String  @db.Text
}

model glpi_plugin_mydashboard_dashboards {
  id             Int     @id @default(autoincrement()) @db.UnsignedInt
  users_id       Int     @default(0) @db.UnsignedInt
  grid           String? @db.LongText
  profiles_id    Int     @default(0) @db.UnsignedInt
  grid_statesave String? @db.LongText
}

model glpi_plugin_mydashboard_groupprofiles {
  id          Int    @id @default(autoincrement()) @db.UnsignedInt
  groups_id   String @default("[]") @db.VarChar(255)
  profiles_id Int    @default(0) @db.UnsignedInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_plugin_mydashboard_itilalerts {
  id           Int    @id @default(autoincrement()) @db.UnsignedInt
  reminders_id Int    @default(0) @db.UnsignedInt
  items_id     Int    @default(0) @db.UnsignedInt
  itemtype     String @db.VarChar(100)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_plugin_mydashboard_preferences {
  id                       Int    @id @db.UnsignedInt
  automatic_refresh        Int    @default(0) @db.TinyInt
  automatic_refresh_delay  Int    @default(10) @db.UnsignedInt
  nb_widgets_width         Int    @default(3) @db.UnsignedInt
  replace_central          Int    @default(0) @db.TinyInt
  prefered_group           String @default("[]") @db.VarChar(255)
  prefered_entity          Int    @default(0) @db.UnsignedInt
  edit_mode                Int    @default(0) @db.TinyInt
  drag_mode                Int    @default(0) @db.TinyInt
  requester_prefered_group String @default("[]") @db.VarChar(255)
  color_palette            String @default("") @db.VarChar(50)
  prefered_type            Int    @default(0) @db.UnsignedInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_plugin_mydashboard_preferenceuserblacklists {
  id          Int    @id @default(autoincrement()) @db.UnsignedInt
  users_id    Int    @db.UnsignedInt
  plugin_name String @db.VarChar(255)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_plugin_mydashboard_profileauthorizedwidgets {
  id          Int @id @default(autoincrement()) @db.UnsignedInt
  profiles_id Int @default(0) @db.UnsignedInt
  widgets_id  Int @default(0) @db.UnsignedInt
}

model glpi_plugin_mydashboard_stockticketindicators {
  id           Int @id @default(autoincrement()) @db.UnsignedInt
  year         Int @db.UnsignedInt
  week         Int @db.UnsignedInt
  nbTickets    Int @db.UnsignedInt
  indicator_id Int @db.UnsignedInt
  groups_id    Int @default(0) @db.UnsignedInt
  entities_id  Int @db.UnsignedInt
}

model glpi_plugin_mydashboard_stocktickets {
  id             Int      @id @default(autoincrement()) @db.UnsignedInt
  date           DateTime @db.Date
  nbstocktickets Int      @db.UnsignedInt
  entities_id    Int      @db.UnsignedInt
  groups_id      Int      @default(0) @db.UnsignedInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_plugin_mydashboard_stockwidgets {
  id              Int     @id @default(autoincrement()) @db.UnsignedInt
  entities_id     Int     @default(0) @db.UnsignedInt
  is_recursive    Int     @default(0) @db.TinyInt
  name            String  @db.VarChar(255)
  states          String? @db.LongText
  itemtype        String  @db.VarChar(100)
  icon            String  @db.VarChar(255)
  types           String? @db.LongText
  alarm_threshold Int     @default(5) @db.UnsignedInt

  @@index([entities_id], map: "entities_id")
  @@index([name], map: "name")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_plugin_mydashboard_userwidgets {
  id          Int @id @default(autoincrement()) @db.UnsignedInt
  users_id    Int @db.UnsignedInt
  profiles_id Int @default(0) @db.UnsignedInt
  widgets_id  Int @db.UnsignedInt
  interface   Int @default(0) @db.TinyInt

  @@index([users_id], map: "users_id")
}

model glpi_plugin_mydashboard_widgets {
  id   Int    @id @default(autoincrement()) @db.UnsignedInt
  name String @unique(map: "name") @db.VarChar(255)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_plugins {
  id        Int     @id @default(autoincrement()) @db.UnsignedInt
  directory String  @unique(map: "unicity") @db.VarChar(255)
  name      String  @db.VarChar(255)
  version   String  @db.VarChar(255)
  state     Int     @default(0)
  author    String? @db.VarChar(255)
  homepage  String? @db.VarChar(255)
  license   String? @db.VarChar(255)

  @@index([name], map: "name")
  @@index([state], map: "state")
}

model glpi_plugs {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_printerlogs {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  printers_id   Int       @db.UnsignedInt
  total_pages   Int       @default(0)
  bw_pages      Int       @default(0)
  color_pages   Int       @default(0)
  rv_pages      Int       @default(0)
  prints        Int       @default(0)
  bw_prints     Int       @default(0)
  color_prints  Int       @default(0)
  copies        Int       @default(0)
  bw_copies     Int       @default(0)
  color_copies  Int       @default(0)
  scanned       Int       @default(0)
  date          DateTime? @db.Date
  date_creation DateTime? @db.Timestamp(0)
  date_mod      DateTime? @db.Timestamp(0)
  faxed         Int       @default(0)

  @@unique([printers_id, date], map: "unicity")
  @@index([date], map: "date")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
}

model glpi_printermodels {
  id             Int       @id @default(autoincrement()) @db.UnsignedInt
  name           String?   @db.VarChar(255)
  comment        String?   @db.Text
  product_number String?   @db.VarChar(255)
  date_mod       DateTime? @db.Timestamp(0)
  date_creation  DateTime? @db.Timestamp(0)
  picture_front  String?   @db.Text
  picture_rear   String?   @db.Text
  pictures       String?   @db.Text

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
  @@index([product_number], map: "product_number")
}

model glpi_printers {
  id                    Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id           Int       @default(0) @db.UnsignedInt
  is_recursive          Int       @default(0) @db.TinyInt
  name                  String?   @db.VarChar(255)
  date_mod              DateTime? @db.Timestamp(0)
  contact               String?   @db.VarChar(255)
  contact_num           String?   @db.VarChar(255)
  users_id_tech         Int       @default(0) @db.UnsignedInt
  groups_id_tech        Int       @default(0) @db.UnsignedInt
  serial                String?   @db.VarChar(255)
  otherserial           String?   @db.VarChar(255)
  have_serial           Int       @default(0) @db.TinyInt
  have_parallel         Int       @default(0) @db.TinyInt
  have_usb              Int       @default(0) @db.TinyInt
  have_wifi             Int       @default(0) @db.TinyInt
  have_ethernet         Int       @default(0) @db.TinyInt
  comment               String?   @db.Text
  memory_size           String?   @db.VarChar(255)
  locations_id          Int       @default(0) @db.UnsignedInt
  networks_id           Int       @default(0) @db.UnsignedInt
  printertypes_id       Int       @default(0) @db.UnsignedInt
  printermodels_id      Int       @default(0) @db.UnsignedInt
  manufacturers_id      Int       @default(0) @db.UnsignedInt
  is_global             Int       @default(0) @db.TinyInt
  is_deleted            Int       @default(0) @db.TinyInt
  is_template           Int       @default(0) @db.TinyInt
  template_name         String?   @db.VarChar(255)
  init_pages_counter    Int       @default(0)
  last_pages_counter    Int       @default(0)
  users_id              Int       @default(0) @db.UnsignedInt
  groups_id             Int       @default(0) @db.UnsignedInt
  states_id             Int       @default(0) @db.UnsignedInt
  ticket_tco            Decimal?  @default(0.0000) @db.Decimal(20, 4)
  is_dynamic            Int       @default(0) @db.TinyInt
  uuid                  String?   @db.VarChar(255)
  date_creation         DateTime? @db.Timestamp(0)
  sysdescr              String?   @db.Text
  last_inventory_update DateTime? @db.Timestamp(0)
  snmpcredentials_id    Int       @default(0) @db.UnsignedInt
  autoupdatesystems_id  Int       @default(0) @db.UnsignedInt

  @@index([autoupdatesystems_id], map: "autoupdatesystems_id")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([groups_id], map: "groups_id")
  @@index([groups_id_tech], map: "groups_id_tech")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_dynamic], map: "is_dynamic")
  @@index([is_global], map: "is_global")
  @@index([is_recursive], map: "is_recursive")
  @@index([is_template], map: "is_template")
  @@index([last_pages_counter], map: "last_pages_counter")
  @@index([locations_id], map: "locations_id")
  @@index([manufacturers_id], map: "manufacturers_id")
  @@index([name], map: "name")
  @@index([networks_id], map: "networks_id")
  @@index([otherserial], map: "otherserial")
  @@index([printermodels_id], map: "printermodels_id")
  @@index([printertypes_id], map: "printertypes_id")
  @@index([serial], map: "serial")
  @@index([snmpcredentials_id], map: "snmpcredentials_id")
  @@index([states_id], map: "states_id")
  @@index([users_id], map: "users_id")
  @@index([users_id_tech], map: "users_id_tech")
  @@index([uuid], map: "uuid")
}

model glpi_printers_cartridgeinfos {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  printers_id   Int       @db.UnsignedInt
  property      String    @db.VarChar(255)
  value         String    @db.VarChar(255)
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([printers_id], map: "printers_id")
}

model glpi_printertypes {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_problemcosts {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  problems_id   Int       @default(0) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  begin_date    DateTime? @db.Date
  end_date      DateTime? @db.Date
  actiontime    Int       @default(0)
  cost_time     Decimal   @default(0.0000) @db.Decimal(20, 4)
  cost_fixed    Decimal   @default(0.0000) @db.Decimal(20, 4)
  cost_material Decimal   @default(0.0000) @db.Decimal(20, 4)
  budgets_id    Int       @default(0) @db.UnsignedInt
  entities_id   Int       @default(0) @db.UnsignedInt

  @@index([begin_date], map: "begin_date")
  @@index([budgets_id], map: "budgets_id")
  @@index([end_date], map: "end_date")
  @@index([entities_id], map: "entities_id")
  @@index([name], map: "name")
  @@index([problems_id], map: "problems_id")
}

model glpi_problems {
  id                   Int       @id @default(autoincrement()) @db.UnsignedInt
  name                 String?   @db.VarChar(255)
  entities_id          Int       @default(0) @db.UnsignedInt
  is_recursive         Int       @default(0) @db.TinyInt
  is_deleted           Int       @default(0) @db.TinyInt
  status               Int       @default(1)
  content              String?   @db.LongText
  date_mod             DateTime? @db.Timestamp(0)
  date                 DateTime? @db.Timestamp(0)
  solvedate            DateTime? @db.Timestamp(0)
  closedate            DateTime? @db.Timestamp(0)
  time_to_resolve      DateTime? @db.Timestamp(0)
  users_id_recipient   Int       @default(0) @db.UnsignedInt
  users_id_lastupdater Int       @default(0) @db.UnsignedInt
  urgency              Int       @default(1)
  impact               Int       @default(1)
  priority             Int       @default(1)
  itilcategories_id    Int       @default(0) @db.UnsignedInt
  impactcontent        String?   @db.LongText
  causecontent         String?   @db.LongText
  symptomcontent       String?   @db.LongText
  actiontime           Int       @default(0)
  begin_waiting_date   DateTime? @db.Timestamp(0)
  waiting_duration     Int       @default(0)
  close_delay_stat     Int       @default(0)
  solve_delay_stat     Int       @default(0)
  date_creation        DateTime? @db.Timestamp(0)
  locations_id         Int       @default(0) @db.UnsignedInt

  @@index([closedate], map: "closedate")
  @@index([date], map: "date")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([impact], map: "impact")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_recursive], map: "is_recursive")
  @@index([itilcategories_id], map: "itilcategories_id")
  @@index([locations_id], map: "locations_id")
  @@index([name], map: "name")
  @@index([priority], map: "priority")
  @@index([solvedate], map: "solvedate")
  @@index([status], map: "status")
  @@index([time_to_resolve], map: "time_to_resolve")
  @@index([urgency], map: "urgency")
  @@index([users_id_lastupdater], map: "users_id_lastupdater")
  @@index([users_id_recipient], map: "users_id_recipient")
}

model glpi_problems_suppliers {
  id                Int     @id @default(autoincrement()) @db.UnsignedInt
  problems_id       Int     @default(0) @db.UnsignedInt
  suppliers_id      Int     @default(0) @db.UnsignedInt
  type              Int     @default(1)
  use_notification  Int     @default(0) @db.TinyInt
  alternative_email String? @db.VarChar(255)

  @@unique([problems_id, type, suppliers_id], map: "unicity")
  @@index([suppliers_id, type], map: "group")
}

model glpi_problems_tickets {
  id          Int @id @default(autoincrement()) @db.UnsignedInt
  problems_id Int @default(0) @db.UnsignedInt
  tickets_id  Int @default(0) @db.UnsignedInt

  @@unique([problems_id, tickets_id], map: "unicity")
  @@index([tickets_id], map: "tickets_id")
}

model glpi_problems_users {
  id                Int     @id @default(autoincrement()) @db.UnsignedInt
  problems_id       Int     @default(0) @db.UnsignedInt
  users_id          Int     @default(0) @db.UnsignedInt
  type              Int     @default(1)
  use_notification  Int     @default(0) @db.TinyInt
  alternative_email String? @db.VarChar(255)

  @@unique([problems_id, type, users_id, alternative_email], map: "unicity")
  @@index([users_id, type], map: "user")
}

model glpi_problemtasks {
  id                Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid              String?   @unique(map: "uuid") @db.VarChar(255)
  problems_id       Int       @default(0) @db.UnsignedInt
  taskcategories_id Int       @default(0) @db.UnsignedInt
  date              DateTime? @db.Timestamp(0)
  begin             DateTime? @db.Timestamp(0)
  end               DateTime? @db.Timestamp(0)
  users_id          Int       @default(0) @db.UnsignedInt
  users_id_editor   Int       @default(0) @db.UnsignedInt
  users_id_tech     Int       @default(0) @db.UnsignedInt
  groups_id_tech    Int       @default(0) @db.UnsignedInt
  content           String?   @db.LongText
  actiontime        Int       @default(0)
  state             Int       @default(0)
  date_mod          DateTime? @db.Timestamp(0)
  date_creation     DateTime? @db.Timestamp(0)
  tasktemplates_id  Int       @default(0) @db.UnsignedInt
  timeline_position Int       @default(0) @db.TinyInt
  is_private        Int       @default(0) @db.TinyInt

  @@index([begin], map: "begin")
  @@index([date], map: "date")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([end], map: "end")
  @@index([groups_id_tech], map: "groups_id_tech")
  @@index([is_private], map: "is_private")
  @@index([problems_id], map: "problems_id")
  @@index([state], map: "state")
  @@index([taskcategories_id], map: "taskcategories_id")
  @@index([tasktemplates_id], map: "tasktemplates_id")
  @@index([users_id], map: "users_id")
  @@index([users_id_editor], map: "users_id_editor")
  @@index([users_id_tech], map: "users_id_tech")
}

model glpi_problemtemplatehiddenfields {
  id                  Int @id @default(autoincrement()) @db.UnsignedInt
  problemtemplates_id Int @default(0) @db.UnsignedInt
  num                 Int @default(0)

  @@unique([problemtemplates_id, num], map: "unicity")
}

model glpi_problemtemplatemandatoryfields {
  id                  Int @id @default(autoincrement()) @db.UnsignedInt
  problemtemplates_id Int @default(0) @db.UnsignedInt
  num                 Int @default(0)

  @@unique([problemtemplates_id, num], map: "unicity")
}

model glpi_problemtemplatepredefinedfields {
  id                  Int     @id @default(autoincrement()) @db.UnsignedInt
  problemtemplates_id Int     @default(0) @db.UnsignedInt
  num                 Int     @default(0)
  value               String? @db.Text

  @@index([problemtemplates_id], map: "problemtemplates_id")
}

model glpi_problemtemplates {
  id           Int     @id @default(autoincrement()) @db.UnsignedInt
  name         String? @db.VarChar(255)
  entities_id  Int     @default(0) @db.UnsignedInt
  is_recursive Int     @default(0) @db.TinyInt
  comment      String? @db.Text

  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
}

model glpi_profilerights {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  profiles_id Int     @default(0) @db.UnsignedInt
  name        String? @db.VarChar(255)
  rights      Int     @default(0)

  @@unique([profiles_id, name], map: "unicity")
  @@index([name], map: "name")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_profiles {
  id                        Int       @id @default(autoincrement()) @db.UnsignedInt
  name                      String?   @db.VarChar(255)
  interface                 String?   @default("helpdesk") @db.VarChar(255)
  is_default                Int       @default(0) @db.TinyInt
  helpdesk_hardware         Int       @default(0)
  helpdesk_item_type        String?   @db.Text
  ticket_status             String?   @db.Text
  date_mod                  DateTime? @db.Timestamp(0)
  comment                   String?   @db.Text
  problem_status            String?   @db.Text
  create_ticket_on_login    Int       @default(0) @db.TinyInt
  tickettemplates_id        Int       @default(0) @db.UnsignedInt
  changetemplates_id        Int       @default(0) @db.UnsignedInt
  problemtemplates_id       Int       @default(0) @db.UnsignedInt
  change_status             String?   @db.Text
  managed_domainrecordtypes String?   @db.Text
  date_creation             DateTime? @db.Timestamp(0)

  @@index([changetemplates_id], map: "changetemplates_id")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([interface], map: "interface")
  @@index([is_default], map: "is_default")
  @@index([name], map: "name")
  @@index([problemtemplates_id], map: "problemtemplates_id")
  @@index([tickettemplates_id], map: "tickettemplates_id")
}

model glpi_profiles_reminders {
  id                    Int  @id @default(autoincrement()) @db.UnsignedInt
  reminders_id          Int  @default(0) @db.UnsignedInt
  profiles_id           Int  @default(0) @db.UnsignedInt
  entities_id           Int? @db.UnsignedInt
  is_recursive          Int  @default(0) @db.TinyInt
  no_entity_restriction Int  @default(0) @db.TinyInt

  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([profiles_id], map: "profiles_id")
  @@index([reminders_id], map: "reminders_id")
}

model glpi_profiles_rssfeeds {
  id                    Int  @id @default(autoincrement()) @db.UnsignedInt
  rssfeeds_id           Int  @default(0) @db.UnsignedInt
  profiles_id           Int  @default(0) @db.UnsignedInt
  entities_id           Int? @db.UnsignedInt
  is_recursive          Int  @default(0) @db.TinyInt
  no_entity_restriction Int  @default(0) @db.TinyInt

  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([profiles_id], map: "profiles_id")
  @@index([rssfeeds_id], map: "rssfeeds_id")
}

model glpi_profiles_users {
  id                 Int @id @default(autoincrement()) @db.UnsignedInt
  users_id           Int @default(0) @db.UnsignedInt
  profiles_id        Int @default(0) @db.UnsignedInt
  entities_id        Int @default(0) @db.UnsignedInt
  is_recursive       Int @default(1) @db.TinyInt
  is_dynamic         Int @default(0) @db.TinyInt
  is_default_profile Int @default(0) @db.TinyInt

  @@index([entities_id], map: "entities_id")
  @@index([is_dynamic], map: "is_dynamic")
  @@index([is_recursive], map: "is_recursive")
  @@index([profiles_id], map: "profiles_id")
  @@index([users_id], map: "users_id")
}

model glpi_projectcosts {
  id           Int       @id @default(autoincrement()) @db.UnsignedInt
  projects_id  Int       @default(0) @db.UnsignedInt
  name         String?   @db.VarChar(255)
  comment      String?   @db.Text
  begin_date   DateTime? @db.Date
  end_date     DateTime? @db.Date
  cost         Decimal   @default(0.0000) @db.Decimal(20, 4)
  budgets_id   Int       @default(0) @db.UnsignedInt
  entities_id  Int       @default(0) @db.UnsignedInt
  is_recursive Int       @default(0) @db.TinyInt

  @@index([begin_date], map: "begin_date")
  @@index([budgets_id], map: "budgets_id")
  @@index([end_date], map: "end_date")
  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
  @@index([projects_id], map: "projects_id")
}

model glpi_projects {
  id                   Int       @id @default(autoincrement()) @db.UnsignedInt
  name                 String?   @db.VarChar(255)
  code                 String?   @db.VarChar(255)
  priority             Int       @default(1)
  entities_id          Int       @default(0) @db.UnsignedInt
  is_recursive         Int       @default(0) @db.TinyInt
  projects_id          Int       @default(0) @db.UnsignedInt
  projectstates_id     Int       @default(0) @db.UnsignedInt
  projecttypes_id      Int       @default(0) @db.UnsignedInt
  date                 DateTime? @db.Timestamp(0)
  date_mod             DateTime? @db.Timestamp(0)
  users_id             Int       @default(0) @db.UnsignedInt
  groups_id            Int       @default(0) @db.UnsignedInt
  plan_start_date      DateTime? @db.Timestamp(0)
  plan_end_date        DateTime? @db.Timestamp(0)
  real_start_date      DateTime? @db.Timestamp(0)
  real_end_date        DateTime? @db.Timestamp(0)
  percent_done         Int       @default(0)
  auto_percent_done    Int       @default(0) @db.TinyInt
  show_on_global_gantt Int       @default(0) @db.TinyInt
  content              String?   @db.LongText
  comment              String?   @db.LongText
  is_deleted           Int       @default(0) @db.TinyInt
  date_creation        DateTime? @db.Timestamp(0)
  projecttemplates_id  Int       @default(0) @db.UnsignedInt
  is_template          Int       @default(0) @db.TinyInt
  template_name        String?   @db.VarChar(255)

  @@index([code], map: "code")
  @@index([date], map: "date")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([groups_id], map: "groups_id")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_recursive], map: "is_recursive")
  @@index([is_template], map: "is_template")
  @@index([name], map: "name")
  @@index([percent_done], map: "percent_done")
  @@index([plan_end_date], map: "plan_end_date")
  @@index([plan_start_date], map: "plan_start_date")
  @@index([priority], map: "priority")
  @@index([projects_id], map: "projects_id")
  @@index([projectstates_id], map: "projectstates_id")
  @@index([projecttemplates_id], map: "projecttemplates_id")
  @@index([projecttypes_id], map: "projecttypes_id")
  @@index([real_end_date], map: "real_end_date")
  @@index([real_start_date], map: "real_start_date")
  @@index([show_on_global_gantt], map: "show_on_global_gantt")
  @@index([users_id], map: "users_id")
}

model glpi_projectstates {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  color         String?   @db.VarChar(255)
  is_finished   Int       @default(0) @db.TinyInt
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([is_finished], map: "is_finished")
  @@index([name], map: "name")
}

model glpi_projecttasklinks {
  id                     Int    @id @default(autoincrement()) @db.UnsignedInt
  projecttasks_id_source Int    @db.UnsignedInt
  source_uuid            String @db.VarChar(255)
  projecttasks_id_target Int    @db.UnsignedInt
  target_uuid            String @db.VarChar(255)
  type                   Int    @default(0) @db.TinyInt
  lag                    Int?   @default(0) @db.SmallInt
  lead                   Int?   @default(0) @db.SmallInt

  @@index([projecttasks_id_source], map: "projecttasks_id_source")
  @@index([projecttasks_id_target], map: "projecttasks_id_target")
}

model glpi_projecttasks {
  id                      Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid                    String?   @unique(map: "uuid") @db.VarChar(255)
  name                    String?   @db.VarChar(255)
  content                 String?   @db.LongText
  comment                 String?   @db.LongText
  entities_id             Int       @default(0) @db.UnsignedInt
  is_recursive            Int       @default(0) @db.TinyInt
  projects_id             Int       @default(0) @db.UnsignedInt
  projecttasks_id         Int       @default(0) @db.UnsignedInt
  date_creation           DateTime? @db.Timestamp(0)
  date_mod                DateTime? @db.Timestamp(0)
  plan_start_date         DateTime? @db.Timestamp(0)
  plan_end_date           DateTime? @db.Timestamp(0)
  real_start_date         DateTime? @db.Timestamp(0)
  real_end_date           DateTime? @db.Timestamp(0)
  planned_duration        Int       @default(0)
  effective_duration      Int       @default(0)
  projectstates_id        Int       @default(0) @db.UnsignedInt
  projecttasktypes_id     Int       @default(0) @db.UnsignedInt
  users_id                Int       @default(0) @db.UnsignedInt
  percent_done            Int       @default(0)
  auto_percent_done       Int       @default(0) @db.TinyInt
  is_milestone            Int       @default(0) @db.TinyInt
  projecttasktemplates_id Int       @default(0) @db.UnsignedInt
  is_template             Int       @default(0) @db.TinyInt
  template_name           String?   @db.VarChar(255)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([is_milestone], map: "is_milestone")
  @@index([is_recursive], map: "is_recursive")
  @@index([is_template], map: "is_template")
  @@index([name], map: "name")
  @@index([percent_done], map: "percent_done")
  @@index([plan_end_date], map: "plan_end_date")
  @@index([plan_start_date], map: "plan_start_date")
  @@index([projects_id], map: "projects_id")
  @@index([projectstates_id], map: "projectstates_id")
  @@index([projecttasks_id], map: "projecttasks_id")
  @@index([projecttasktemplates_id], map: "projecttasktemplates_id")
  @@index([projecttasktypes_id], map: "projecttasktypes_id")
  @@index([real_end_date], map: "real_end_date")
  @@index([real_start_date], map: "real_start_date")
  @@index([users_id], map: "users_id")
}

model glpi_projecttasks_tickets {
  id              Int @id @default(autoincrement()) @db.UnsignedInt
  tickets_id      Int @default(0) @db.UnsignedInt
  projecttasks_id Int @default(0) @db.UnsignedInt

  @@unique([tickets_id, projecttasks_id], map: "unicity")
  @@index([projecttasks_id], map: "projects_id")
}

model glpi_projecttaskteams {
  id              Int     @id @default(autoincrement()) @db.UnsignedInt
  projecttasks_id Int     @default(0) @db.UnsignedInt
  itemtype        String? @db.VarChar(100)
  items_id        Int     @default(0) @db.UnsignedInt

  @@unique([projecttasks_id, itemtype, items_id], map: "unicity")
  @@index([itemtype, items_id], map: "item")
}

model glpi_projecttasktemplates {
  id                  Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id         Int       @default(0) @db.UnsignedInt
  is_recursive        Int       @default(0) @db.TinyInt
  name                String?   @db.VarChar(255)
  description         String?   @db.LongText
  comment             String?   @db.LongText
  projects_id         Int       @default(0) @db.UnsignedInt
  projecttasks_id     Int       @default(0) @db.UnsignedInt
  plan_start_date     DateTime? @db.Timestamp(0)
  plan_end_date       DateTime? @db.Timestamp(0)
  real_start_date     DateTime? @db.Timestamp(0)
  real_end_date       DateTime? @db.Timestamp(0)
  planned_duration    Int       @default(0)
  effective_duration  Int       @default(0)
  projectstates_id    Int       @default(0) @db.UnsignedInt
  projecttasktypes_id Int       @default(0) @db.UnsignedInt
  users_id            Int       @default(0) @db.UnsignedInt
  percent_done        Int       @default(0)
  is_milestone        Int       @default(0) @db.TinyInt
  comments            String?   @db.Text
  date_mod            DateTime? @db.Timestamp(0)
  date_creation       DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([is_milestone], map: "is_milestone")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
  @@index([percent_done], map: "percent_done")
  @@index([plan_end_date], map: "plan_end_date")
  @@index([plan_start_date], map: "plan_start_date")
  @@index([projects_id], map: "projects_id")
  @@index([projectstates_id], map: "projectstates_id")
  @@index([projecttasks_id], map: "projecttasks_id")
  @@index([projecttasktypes_id], map: "projecttasktypes_id")
  @@index([real_end_date], map: "real_end_date")
  @@index([real_start_date], map: "real_start_date")
  @@index([users_id], map: "users_id")
}

model glpi_projecttasktypes {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_projectteams {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  projects_id Int     @default(0) @db.UnsignedInt
  itemtype    String? @db.VarChar(100)
  items_id    Int     @default(0) @db.UnsignedInt

  @@unique([projects_id, itemtype, items_id], map: "unicity")
  @@index([itemtype, items_id], map: "item")
}

model glpi_projecttypes {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_queuednotifications {
  id                       Int       @id @default(autoincrement()) @db.UnsignedInt
  itemtype                 String?   @db.VarChar(100)
  items_id                 Int       @default(0) @db.UnsignedInt
  notificationtemplates_id Int       @default(0) @db.UnsignedInt
  entities_id              Int       @default(0) @db.UnsignedInt
  is_deleted               Int       @default(0) @db.TinyInt
  sent_try                 Int       @default(0)
  create_time              DateTime? @db.Timestamp(0)
  send_time                DateTime? @db.Timestamp(0)
  sent_time                DateTime? @db.Timestamp(0)
  name                     String?   @db.Text
  sender                   String?   @db.Text
  sendername               String?   @db.Text
  recipient                String?   @db.Text
  recipientname            String?   @db.Text
  replyto                  String?   @db.Text
  replytoname              String?   @db.Text
  headers                  String?   @db.Text
  body_html                String?   @db.LongText
  body_text                String?   @db.LongText
  messageid                String?   @db.Text
  documents                String?   @db.Text
  mode                     String    @db.VarChar(20)
  event                    String?   @db.VarChar(255)

  @@index([create_time], map: "create_time")
  @@index([entities_id], map: "entities_id")
  @@index([is_deleted], map: "is_deleted")
  @@index([itemtype, items_id, notificationtemplates_id], map: "item")
  @@index([mode], map: "mode")
  @@index([notificationtemplates_id], map: "notificationtemplates_id")
  @@index([recipient(length: 255)], map: "recipient")
  @@index([send_time], map: "send_time")
  @@index([sent_time], map: "sent_time")
  @@index([sent_try], map: "sent_try")
}

model glpi_rackmodels {
  id             Int       @id @default(autoincrement()) @db.UnsignedInt
  name           String?   @db.VarChar(255)
  comment        String?   @db.Text
  product_number String?   @db.VarChar(255)
  date_mod       DateTime? @db.Timestamp(0)
  date_creation  DateTime? @db.Timestamp(0)
  pictures       String?   @db.Text

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
  @@index([product_number], map: "product_number")
}

model glpi_racks {
  id               Int       @id @default(autoincrement()) @db.UnsignedInt
  name             String?   @db.VarChar(255)
  comment          String?   @db.Text
  entities_id      Int       @default(0) @db.UnsignedInt
  is_recursive     Int       @default(0) @db.TinyInt
  locations_id     Int       @default(0) @db.UnsignedInt
  serial           String?   @db.VarChar(255)
  otherserial      String?   @db.VarChar(255)
  rackmodels_id    Int?      @db.UnsignedInt
  manufacturers_id Int       @default(0) @db.UnsignedInt
  racktypes_id     Int       @default(0) @db.UnsignedInt
  states_id        Int       @default(0) @db.UnsignedInt
  users_id_tech    Int       @default(0) @db.UnsignedInt
  groups_id_tech   Int       @default(0) @db.UnsignedInt
  width            Int?
  height           Int?
  depth            Int?
  number_units     Int?      @default(0)
  is_template      Int       @default(0) @db.TinyInt
  template_name    String?   @db.VarChar(255)
  is_deleted       Int       @default(0) @db.TinyInt
  dcrooms_id       Int       @default(0) @db.UnsignedInt
  room_orientation Int       @default(0)
  position         String?   @db.VarChar(50)
  bgcolor          String?   @db.VarChar(7)
  max_power        Int       @default(0)
  mesured_power    Int       @default(0)
  max_weight       Int       @default(0)
  date_mod         DateTime? @db.Timestamp(0)
  date_creation    DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([dcrooms_id], map: "dcrooms_id")
  @@index([entities_id], map: "entities_id")
  @@index([groups_id_tech], map: "group_id_tech")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_recursive], map: "is_recursive")
  @@index([is_template], map: "is_template")
  @@index([locations_id], map: "locations_id")
  @@index([manufacturers_id], map: "manufacturers_id")
  @@index([name], map: "name")
  @@index([rackmodels_id], map: "rackmodels_id")
  @@index([racktypes_id], map: "racktypes_id")
  @@index([states_id], map: "states_id")
  @@index([users_id_tech], map: "users_id_tech")
}

model glpi_racktypes {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id   Int       @default(0) @db.UnsignedInt
  is_recursive  Int       @default(0) @db.TinyInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_creation DateTime? @db.Timestamp(0)
  date_mod      DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
}

model glpi_recurrentchanges {
  id                 Int       @id @default(autoincrement()) @db.UnsignedInt
  name               String?   @db.VarChar(255)
  comment            String?   @db.Text
  entities_id        Int       @default(0) @db.UnsignedInt
  is_recursive       Int       @default(0) @db.TinyInt
  is_active          Int       @default(0) @db.TinyInt
  changetemplates_id Int       @default(0) @db.UnsignedInt
  begin_date         DateTime? @db.Timestamp(0)
  periodicity        String?   @db.VarChar(255)
  create_before      Int       @default(0)
  next_creation_date DateTime? @db.Timestamp(0)
  calendars_id       Int       @default(0) @db.UnsignedInt
  end_date           DateTime? @db.Timestamp(0)

  @@index([calendars_id], map: "calendars_id")
  @@index([changetemplates_id], map: "changetemplates_id")
  @@index([entities_id], map: "entities_id")
  @@index([is_active], map: "is_active")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
  @@index([next_creation_date], map: "next_creation_date")
}

model glpi_refusedequipments {
  id                   Int       @id @default(autoincrement()) @db.UnsignedInt
  name                 String?   @db.VarChar(255)
  itemtype             String?   @db.VarChar(100)
  entities_id          Int       @default(0) @db.UnsignedInt
  ip                   String?   @db.Text
  mac                  String?   @db.Text
  rules_id             Int       @default(0) @db.UnsignedInt
  method               String?   @db.VarChar(255)
  serial               String?   @db.VarChar(255)
  uuid                 String?   @db.VarChar(255)
  agents_id            Int       @default(0) @db.UnsignedInt
  autoupdatesystems_id Int       @default(0) @db.UnsignedInt
  date_creation        DateTime? @db.Timestamp(0)
  date_mod             DateTime? @db.Timestamp(0)

  @@index([agents_id], map: "agents_id")
  @@index([autoupdatesystems_id], map: "autoupdatesystems_id")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([name], map: "name")
  @@index([rules_id], map: "rules_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_registeredids {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  name        String? @db.VarChar(255)
  items_id    Int     @default(0) @db.UnsignedInt
  itemtype    String  @db.VarChar(100)
  device_type String  @db.VarChar(100)

  @@index([device_type], map: "device_type")
  @@index([itemtype, items_id], map: "item")
  @@index([name], map: "name")
}

model glpi_reminders {
  id              Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid            String?   @unique(map: "uuid") @db.VarChar(255)
  date            DateTime? @db.Timestamp(0)
  users_id        Int       @default(0) @db.UnsignedInt
  name            String?   @db.VarChar(255)
  text            String?   @db.Text
  begin           DateTime? @db.Timestamp(0)
  end             DateTime? @db.Timestamp(0)
  is_planned      Int       @default(0) @db.TinyInt
  date_mod        DateTime? @db.Timestamp(0)
  state           Int       @default(0)
  begin_view_date DateTime? @db.Timestamp(0)
  end_view_date   DateTime? @db.Timestamp(0)
  date_creation   DateTime? @db.Timestamp(0)

  @@index([begin], map: "begin")
  @@index([date], map: "date")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([end], map: "end")
  @@index([is_planned], map: "is_planned")
  @@index([name], map: "name")
  @@index([state], map: "state")
  @@index([users_id], map: "users_id")
}

model glpi_reminders_users {
  id           Int @id @default(autoincrement()) @db.UnsignedInt
  reminders_id Int @default(0) @db.UnsignedInt
  users_id     Int @default(0) @db.UnsignedInt

  @@index([reminders_id], map: "reminders_id")
  @@index([users_id], map: "users_id")
}

model glpi_remindertranslations {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  reminders_id  Int       @default(0) @db.UnsignedInt
  language      String?   @db.VarChar(5)
  name          String?   @db.Text
  text          String?   @db.LongText
  users_id      Int       @default(0) @db.UnsignedInt
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([reminders_id, language], map: "item")
  @@index([users_id], map: "users_id")
}

model glpi_requesttypes {
  id                      Int       @id @default(autoincrement()) @db.UnsignedInt
  name                    String?   @db.VarChar(255)
  is_helpdesk_default     Int       @default(0) @db.TinyInt
  is_followup_default     Int       @default(0) @db.TinyInt
  is_mail_default         Int       @default(0) @db.TinyInt
  is_mailfollowup_default Int       @default(0) @db.TinyInt
  is_active               Int       @default(1) @db.TinyInt
  is_ticketheader         Int       @default(1) @db.TinyInt
  is_itilfollowup         Int       @default(1) @db.TinyInt
  comment                 String?   @db.Text
  date_mod                DateTime? @db.Timestamp(0)
  date_creation           DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([is_active], map: "is_active")
  @@index([is_followup_default], map: "is_followup_default")
  @@index([is_helpdesk_default], map: "is_helpdesk_default")
  @@index([is_itilfollowup], map: "is_itilfollowup")
  @@index([is_mail_default], map: "is_mail_default")
  @@index([is_mailfollowup_default], map: "is_mailfollowup_default")
  @@index([is_ticketheader], map: "is_ticketheader")
  @@index([name], map: "name")
}

model glpi_reservationitems {
  id           Int     @id @default(autoincrement()) @db.UnsignedInt
  itemtype     String  @db.VarChar(100)
  entities_id  Int     @default(0) @db.UnsignedInt
  is_recursive Int     @default(0) @db.TinyInt
  items_id     Int     @default(0) @db.UnsignedInt
  comment      String? @db.Text
  is_active    Int     @default(1) @db.TinyInt

  @@unique([itemtype, items_id], map: "unicity")
  @@index([entities_id], map: "entities_id")
  @@index([is_active], map: "is_active")
  @@index([is_recursive], map: "is_recursive")
  @@index([itemtype, items_id], map: "item")
}

model glpi_reservations {
  id                  Int       @id @default(autoincrement()) @db.UnsignedInt
  reservationitems_id Int       @default(0) @db.UnsignedInt
  begin               DateTime? @db.Timestamp(0)
  end                 DateTime? @db.Timestamp(0)
  users_id            Int       @default(0) @db.UnsignedInt
  comment             String?   @db.Text
  group               Int       @default(0)

  @@index([begin], map: "begin")
  @@index([end], map: "end")
  @@index([reservationitems_id, group], map: "resagroup")
  @@index([users_id], map: "users_id")
}

model glpi_rssfeeds {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  users_id      Int       @default(0) @db.UnsignedInt
  comment       String?   @db.Text
  url           String?   @db.Text
  refresh_rate  Int       @default(86400)
  max_items     Int       @default(20)
  have_error    Int       @default(0) @db.TinyInt
  is_active     Int       @default(0) @db.TinyInt
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([have_error], map: "have_error")
  @@index([is_active], map: "is_active")
  @@index([name], map: "name")
  @@index([users_id], map: "users_id")
}

model glpi_rssfeeds_users {
  id          Int @id @default(autoincrement()) @db.UnsignedInt
  rssfeeds_id Int @default(0) @db.UnsignedInt
  users_id    Int @default(0) @db.UnsignedInt

  @@index([rssfeeds_id], map: "rssfeeds_id")
  @@index([users_id], map: "users_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_ruleactions {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  rules_id    Int     @default(0) @db.UnsignedInt
  action_type String? @db.VarChar(255)
  field       String? @db.VarChar(255)
  value       String? @db.VarChar(255)

  @@index([field(length: 50), value(length: 50)], map: "field_value")
  @@index([rules_id], map: "rules_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_rulecriterias {
  id        Int     @id @default(autoincrement()) @db.UnsignedInt
  rules_id  Int     @default(0) @db.UnsignedInt
  criteria  String? @db.VarChar(255)
  condition Int     @default(0)
  pattern   String? @db.Text

  @@index([condition], map: "condition")
  @@index([rules_id], map: "rules_id")
}

model glpi_rulematchedlogs {
  id        Int       @id @default(autoincrement()) @db.UnsignedInt
  date      DateTime? @db.Timestamp(0)
  items_id  Int       @default(0) @db.UnsignedInt
  itemtype  String?   @db.VarChar(100)
  rules_id  Int?      @db.UnsignedInt
  agents_id Int       @default(0) @db.UnsignedInt
  method    String?   @db.VarChar(255)

  @@index([agents_id], map: "agents_id")
  @@index([itemtype, items_id], map: "item")
  @@index([rules_id], map: "rules_id")
}

model glpi_rulerightparameters {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  value         String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_rules {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id   Int       @default(0) @db.UnsignedInt
  sub_type      String    @default("") @db.VarChar(255)
  ranking       Int       @default(0)
  name          String?   @db.VarChar(255)
  description   String?   @db.Text
  match         String?   @db.Char(10)
  is_active     Int       @default(1) @db.TinyInt
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  is_recursive  Int       @default(0) @db.TinyInt
  uuid          String?   @db.VarChar(255)
  condition     Int       @default(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([condition], map: "condition")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([is_active], map: "is_active")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
  @@index([sub_type], map: "sub_type")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_savedsearches {
  id                  Int       @id @default(autoincrement()) @db.UnsignedInt
  name                String?   @db.VarChar(255)
  type                Int       @default(0)
  itemtype            String    @db.VarChar(100)
  users_id            Int       @default(0) @db.UnsignedInt
  is_private          Int       @default(1) @db.TinyInt
  entities_id         Int       @default(0) @db.UnsignedInt
  is_recursive        Int       @default(0) @db.TinyInt
  query               String?   @db.Text
  last_execution_time Int?
  do_count            Int       @default(2) @db.TinyInt
  last_execution_date DateTime? @db.Timestamp(0)
  counter             Int       @default(0)

  @@index([do_count], map: "do_count")
  @@index([entities_id], map: "entities_id")
  @@index([is_private], map: "is_private")
  @@index([is_recursive], map: "is_recursive")
  @@index([itemtype], map: "itemtype")
  @@index([last_execution_date], map: "last_execution_date")
  @@index([last_execution_time], map: "last_execution_time")
  @@index([name], map: "name")
  @@index([type], map: "type")
  @@index([users_id], map: "users_id")
}

model glpi_savedsearches_alerts {
  id               Int       @id @default(autoincrement()) @db.UnsignedInt
  savedsearches_id Int       @default(0) @db.UnsignedInt
  name             String?   @db.VarChar(255)
  is_active        Int       @default(0) @db.TinyInt
  operator         Int       @db.TinyInt
  value            Int
  date_mod         DateTime? @db.Timestamp(0)
  date_creation    DateTime? @db.Timestamp(0)
  frequency        Int       @default(0)

  @@unique([savedsearches_id, operator, value], map: "unicity")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([is_active], map: "is_active")
  @@index([name], map: "name")
}

model glpi_savedsearches_users {
  id               Int    @id @default(autoincrement()) @db.UnsignedInt
  users_id         Int    @default(0) @db.UnsignedInt
  itemtype         String @db.VarChar(100)
  savedsearches_id Int    @default(0) @db.UnsignedInt

  @@unique([users_id, itemtype], map: "unicity")
  @@index([savedsearches_id], map: "savedsearches_id")
}

model glpi_slalevelactions {
  id           Int     @id @default(autoincrement()) @db.UnsignedInt
  slalevels_id Int     @default(0) @db.UnsignedInt
  action_type  String? @db.VarChar(255)
  field        String? @db.VarChar(255)
  value        String? @db.VarChar(255)

  @@index([slalevels_id], map: "slalevels_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_slalevelcriterias {
  id           Int     @id @default(autoincrement()) @db.UnsignedInt
  slalevels_id Int     @default(0) @db.UnsignedInt
  criteria     String? @db.VarChar(255)
  condition    Int     @default(0)
  pattern      String? @db.VarChar(255)

  @@index([condition], map: "condition")
  @@index([slalevels_id], map: "slalevels_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_slalevels {
  id             Int     @id @default(autoincrement()) @db.UnsignedInt
  name           String? @db.VarChar(255)
  slas_id        Int     @default(0) @db.UnsignedInt
  execution_time Int
  is_active      Int     @default(1) @db.TinyInt
  entities_id    Int     @default(0) @db.UnsignedInt
  is_recursive   Int     @default(0) @db.TinyInt
  match          String? @db.Char(10)
  uuid           String? @db.VarChar(255)

  @@index([entities_id], map: "entities_id")
  @@index([is_active], map: "is_active")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
  @@index([slas_id], map: "slas_id")
}

model glpi_slalevels_tickets {
  id           Int       @id @default(autoincrement()) @db.UnsignedInt
  tickets_id   Int       @default(0) @db.UnsignedInt
  slalevels_id Int       @default(0) @db.UnsignedInt
  date         DateTime? @db.Timestamp(0)

  @@unique([tickets_id, slalevels_id], map: "unicity")
  @@index([slalevels_id], map: "slalevels_id")
}

model glpi_slas {
  id                  Int       @id @default(autoincrement()) @db.UnsignedInt
  name                String?   @db.VarChar(255)
  entities_id         Int       @default(0) @db.UnsignedInt
  is_recursive        Int       @default(0) @db.TinyInt
  type                Int       @default(0)
  comment             String?   @db.Text
  number_time         Int
  use_ticket_calendar Int       @default(0) @db.TinyInt
  calendars_id        Int       @default(0) @db.UnsignedInt
  date_mod            DateTime? @db.Timestamp(0)
  definition_time     String?   @db.VarChar(255)
  end_of_working_day  Int       @default(0) @db.TinyInt
  date_creation       DateTime? @db.Timestamp(0)
  slms_id             Int       @default(0) @db.UnsignedInt

  @@index([calendars_id], map: "calendars_id")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
  @@index([slms_id], map: "slms_id")
}

model glpi_slms {
  id                  Int       @id @default(autoincrement()) @db.UnsignedInt
  name                String?   @db.VarChar(255)
  entities_id         Int       @default(0) @db.UnsignedInt
  is_recursive        Int       @default(0) @db.TinyInt
  comment             String?   @db.Text
  use_ticket_calendar Int       @default(0) @db.TinyInt
  calendars_id        Int       @default(0) @db.UnsignedInt
  date_mod            DateTime? @db.Timestamp(0)
  date_creation       DateTime? @db.Timestamp(0)

  @@index([calendars_id], map: "calendars_id")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
}

model glpi_snmpcredentials {
  id              Int     @id @default(autoincrement()) @db.UnsignedInt
  name            String? @db.VarChar(64)
  snmpversion     String  @default("1") @db.VarChar(8)
  community       String? @db.VarChar(255)
  username        String? @db.VarChar(255)
  authentication  String? @db.VarChar(255)
  auth_passphrase String? @db.VarChar(255)
  encryption      String? @db.VarChar(255)
  priv_passphrase String? @db.VarChar(255)
  is_deleted      Int     @default(0) @db.TinyInt

  @@index([is_deleted], map: "is_deleted")
  @@index([name], map: "name")
  @@index([snmpversion], map: "snmpversion")
}

model glpi_socketmodels {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_sockets {
  id              Int       @id @default(autoincrement()) @db.UnsignedInt
  position        Int       @default(0)
  locations_id    Int       @default(0) @db.UnsignedInt
  name            String?   @db.VarChar(255)
  socketmodels_id Int       @default(0) @db.UnsignedInt
  wiring_side     Int?      @default(1) @db.TinyInt
  itemtype        String?   @db.VarChar(255)
  items_id        Int       @default(0) @db.UnsignedInt
  networkports_id Int       @default(0) @db.UnsignedInt
  comment         String?   @db.Text
  date_mod        DateTime? @db.Timestamp(0)
  date_creation   DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([itemtype, items_id], map: "item")
  @@index([locations_id, name], map: "location_name")
  @@index([name], map: "name")
  @@index([networkports_id], map: "networkports_id")
  @@index([socketmodels_id], map: "socketmodels_id")
  @@index([wiring_side], map: "wiring_side")
}

model glpi_softwarecategories {
  id                    Int     @id @default(autoincrement()) @db.UnsignedInt
  name                  String? @db.VarChar(255)
  comment               String? @db.Text
  softwarecategories_id Int     @default(0) @db.UnsignedInt
  completename          String? @db.Text
  level                 Int     @default(0)
  ancestors_cache       String? @db.LongText
  sons_cache            String? @db.LongText

  @@index([level], map: "level")
  @@index([name], map: "name")
  @@index([softwarecategories_id], map: "softwarecategories_id")
}

model glpi_softwarelicenses {
  id                      Int       @id @default(autoincrement()) @db.UnsignedInt
  softwares_id            Int       @default(0) @db.UnsignedInt
  softwarelicenses_id     Int       @default(0) @db.UnsignedInt
  completename            String?   @db.Text
  level                   Int       @default(0)
  ancestors_cache         String?   @db.LongText
  sons_cache              String?   @db.LongText
  entities_id             Int       @default(0) @db.UnsignedInt
  is_recursive            Int       @default(0) @db.TinyInt
  number                  Int       @default(0)
  softwarelicensetypes_id Int       @default(0) @db.UnsignedInt
  name                    String?   @db.VarChar(255)
  serial                  String?   @db.VarChar(255)
  otherserial             String?   @db.VarChar(255)
  softwareversions_id_buy Int       @default(0) @db.UnsignedInt
  softwareversions_id_use Int       @default(0) @db.UnsignedInt
  expire                  DateTime? @db.Date
  comment                 String?   @db.Text
  date_mod                DateTime? @db.Timestamp(0)
  is_valid                Int       @default(1) @db.TinyInt
  date_creation           DateTime? @db.Timestamp(0)
  is_deleted              Int       @default(0) @db.TinyInt
  locations_id            Int       @default(0) @db.UnsignedInt
  users_id_tech           Int       @default(0) @db.UnsignedInt
  users_id                Int       @default(0) @db.UnsignedInt
  groups_id_tech          Int       @default(0) @db.UnsignedInt
  groups_id               Int       @default(0) @db.UnsignedInt
  is_helpdesk_visible     Int       @default(0) @db.TinyInt
  is_template             Int       @default(0) @db.TinyInt
  template_name           String?   @db.VarChar(255)
  states_id               Int       @default(0) @db.UnsignedInt
  manufacturers_id        Int       @default(0) @db.UnsignedInt
  contact                 String?   @db.VarChar(255)
  contact_num             String?   @db.VarChar(255)
  allow_overquota         Int       @default(0) @db.TinyInt
  pictures                String?   @db.Text

  @@index([allow_overquota], map: "allow_overquota")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([expire], map: "expire")
  @@index([groups_id], map: "groups_id")
  @@index([groups_id_tech], map: "groups_id_tech")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_helpdesk_visible], map: "is_helpdesk_visible")
  @@index([is_recursive], map: "is_recursive")
  @@index([is_template], map: "is_template")
  @@index([level], map: "level")
  @@index([locations_id], map: "locations_id")
  @@index([manufacturers_id], map: "manufacturers_id")
  @@index([name], map: "name")
  @@index([otherserial], map: "otherserial")
  @@index([serial], map: "serial")
  @@index([softwarelicenses_id], map: "softwarelicenses_id")
  @@index([softwarelicensetypes_id], map: "softwarelicensetypes_id")
  @@index([softwares_id, expire, number], map: "softwares_id_expire_number")
  @@index([softwareversions_id_buy], map: "softwareversions_id_buy")
  @@index([softwareversions_id_use], map: "softwareversions_id_use")
  @@index([states_id], map: "states_id")
  @@index([users_id], map: "users_id")
  @@index([users_id_tech], map: "users_id_tech")
}

model glpi_softwarelicensetypes {
  id                      Int       @id @default(autoincrement()) @db.UnsignedInt
  name                    String?   @db.VarChar(255)
  comment                 String?   @db.Text
  date_mod                DateTime? @db.Timestamp(0)
  date_creation           DateTime? @db.Timestamp(0)
  softwarelicensetypes_id Int       @default(0) @db.UnsignedInt
  level                   Int       @default(0)
  ancestors_cache         String?   @db.LongText
  sons_cache              String?   @db.LongText
  entities_id             Int       @default(0) @db.UnsignedInt
  is_recursive            Int       @default(0) @db.TinyInt
  completename            String?   @db.Text

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([level], map: "level")
  @@index([name], map: "name")
  @@index([softwarelicensetypes_id], map: "softwarelicensetypes_id")
}

model glpi_softwares {
  id                    Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id           Int       @default(0) @db.UnsignedInt
  is_recursive          Int       @default(0) @db.TinyInt
  name                  String?   @db.VarChar(255)
  comment               String?   @db.Text
  locations_id          Int       @default(0) @db.UnsignedInt
  users_id_tech         Int       @default(0) @db.UnsignedInt
  groups_id_tech        Int       @default(0) @db.UnsignedInt
  is_update             Int       @default(0) @db.TinyInt
  softwares_id          Int       @default(0) @db.UnsignedInt
  manufacturers_id      Int       @default(0) @db.UnsignedInt
  is_deleted            Int       @default(0) @db.TinyInt
  is_template           Int       @default(0) @db.TinyInt
  template_name         String?   @db.VarChar(255)
  date_mod              DateTime? @db.Timestamp(0)
  users_id              Int       @default(0) @db.UnsignedInt
  groups_id             Int       @default(0) @db.UnsignedInt
  ticket_tco            Decimal?  @default(0.0000) @db.Decimal(20, 4)
  is_helpdesk_visible   Int       @default(1) @db.TinyInt
  softwarecategories_id Int       @default(0) @db.UnsignedInt
  is_valid              Int       @default(1) @db.TinyInt
  date_creation         DateTime? @db.Timestamp(0)
  pictures              String?   @db.Text

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([groups_id], map: "groups_id")
  @@index([groups_id_tech], map: "groups_id_tech")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_helpdesk_visible], map: "is_helpdesk_visible")
  @@index([is_recursive], map: "is_recursive")
  @@index([is_template], map: "is_template")
  @@index([is_update], map: "is_update")
  @@index([locations_id], map: "locations_id")
  @@index([manufacturers_id], map: "manufacturers_id")
  @@index([name], map: "name")
  @@index([softwarecategories_id], map: "softwarecategories_id")
  @@index([softwares_id], map: "softwares_id")
  @@index([users_id], map: "users_id")
  @@index([users_id_tech], map: "users_id_tech")
}

model glpi_softwareversions {
  id                  Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id         Int       @default(0) @db.UnsignedInt
  is_recursive        Int       @default(0) @db.TinyInt
  softwares_id        Int       @default(0) @db.UnsignedInt
  states_id           Int       @default(0) @db.UnsignedInt
  name                String?   @db.VarChar(255)
  arch                String?   @db.VarChar(255)
  comment             String?   @db.Text
  operatingsystems_id Int       @default(0) @db.UnsignedInt
  date_mod            DateTime? @db.Timestamp(0)
  date_creation       DateTime? @db.Timestamp(0)

  @@index([arch], map: "arch")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
  @@index([operatingsystems_id], map: "operatingsystems_id")
  @@index([softwares_id], map: "softwares_id")
  @@index([states_id], map: "states_id")
}

model glpi_solutiontemplates {
  id               Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id      Int       @default(0) @db.UnsignedInt
  is_recursive     Int       @default(0) @db.TinyInt
  name             String?   @db.VarChar(255)
  content          String?   @db.MediumText
  solutiontypes_id Int       @default(0) @db.UnsignedInt
  comment          String?   @db.Text
  date_mod         DateTime? @db.Timestamp(0)
  date_creation    DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
  @@index([solutiontypes_id], map: "solutiontypes_id")
}

model glpi_solutiontypes {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  entities_id   Int       @default(0) @db.UnsignedInt
  is_recursive  Int       @default(1) @db.TinyInt
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
}

model glpi_ssovariables {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_states {
  id                            Int       @id @default(autoincrement()) @db.UnsignedInt
  name                          String?   @db.VarChar(255)
  entities_id                   Int       @default(0) @db.UnsignedInt
  is_recursive                  Int       @default(0) @db.TinyInt
  comment                       String?   @db.Text
  states_id                     Int       @default(0) @db.UnsignedInt
  completename                  String?   @db.Text
  level                         Int       @default(0)
  ancestors_cache               String?   @db.LongText
  sons_cache                    String?   @db.LongText
  is_visible_computer           Int       @default(1) @db.TinyInt
  is_visible_monitor            Int       @default(1) @db.TinyInt
  is_visible_networkequipment   Int       @default(1) @db.TinyInt
  is_visible_peripheral         Int       @default(1) @db.TinyInt
  is_visible_phone              Int       @default(1) @db.TinyInt
  is_visible_printer            Int       @default(1) @db.TinyInt
  is_visible_softwareversion    Int       @default(1) @db.TinyInt
  is_visible_softwarelicense    Int       @default(1) @db.TinyInt
  is_visible_line               Int       @default(1) @db.TinyInt
  is_visible_certificate        Int       @default(1) @db.TinyInt
  is_visible_rack               Int       @default(1) @db.TinyInt
  is_visible_passivedcequipment Int       @default(1) @db.TinyInt
  is_visible_enclosure          Int       @default(1) @db.TinyInt
  is_visible_pdu                Int       @default(1) @db.TinyInt
  is_visible_cluster            Int       @default(1) @db.TinyInt
  is_visible_contract           Int       @default(1) @db.TinyInt
  is_visible_appliance          Int       @default(1) @db.TinyInt
  is_visible_databaseinstance   Int       @default(1) @db.TinyInt
  is_visible_cable              Int       @default(1) @db.TinyInt
  is_visible_unmanaged          Int       @default(1) @db.TinyInt
  date_mod                      DateTime? @db.Timestamp(0)
  date_creation                 DateTime? @db.Timestamp(0)

  @@unique([states_id, name], map: "unicity")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([is_visible_appliance], map: "is_visible_appliance")
  @@index([is_visible_cable], map: "is_visible_cable")
  @@index([is_visible_certificate], map: "is_visible_certificate")
  @@index([is_visible_cluster], map: "is_visible_cluster")
  @@index([is_visible_computer], map: "is_visible_computer")
  @@index([is_visible_contract], map: "is_visible_contract")
  @@index([is_visible_databaseinstance], map: "is_visible_databaseinstance")
  @@index([is_visible_enclosure], map: "is_visible_enclosure")
  @@index([is_visible_line], map: "is_visible_line")
  @@index([is_visible_monitor], map: "is_visible_monitor")
  @@index([is_visible_networkequipment], map: "is_visible_networkequipment")
  @@index([is_visible_passivedcequipment], map: "is_visible_passivedcequipment")
  @@index([is_visible_pdu], map: "is_visible_pdu")
  @@index([is_visible_peripheral], map: "is_visible_peripheral")
  @@index([is_visible_phone], map: "is_visible_phone")
  @@index([is_visible_printer], map: "is_visible_printer")
  @@index([is_visible_rack], map: "is_visible_rack")
  @@index([is_visible_softwarelicense], map: "is_visible_softwarelicense")
  @@index([is_visible_softwareversion], map: "is_visible_softwareversion")
  @@index([is_visible_unmanaged], map: "is_visible_unmanaged")
  @@index([level], map: "level")
  @@index([name], map: "name")
}

model glpi_suppliers {
  id                  Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id         Int       @default(0) @db.UnsignedInt
  is_recursive        Int       @default(0) @db.TinyInt
  name                String?   @db.VarChar(255)
  suppliertypes_id    Int       @default(0) @db.UnsignedInt
  registration_number String?   @db.VarChar(255)
  address             String?   @db.Text
  postcode            String?   @db.VarChar(255)
  town                String?   @db.VarChar(255)
  state               String?   @db.VarChar(255)
  country             String?   @db.VarChar(255)
  website             String?   @db.VarChar(255)
  phonenumber         String?   @db.VarChar(255)
  comment             String?   @db.Text
  is_deleted          Int       @default(0) @db.TinyInt
  fax                 String?   @db.VarChar(255)
  email               String?   @db.VarChar(255)
  date_mod            DateTime? @db.Timestamp(0)
  date_creation       DateTime? @db.Timestamp(0)
  is_active           Int       @default(0) @db.TinyInt
  pictures            String?   @db.Text

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([is_active], map: "is_active")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
  @@index([suppliertypes_id], map: "suppliertypes_id")
}

model glpi_suppliers_tickets {
  id                Int     @id @default(autoincrement()) @db.UnsignedInt
  tickets_id        Int     @default(0) @db.UnsignedInt
  suppliers_id      Int     @default(0) @db.UnsignedInt
  type              Int     @default(1)
  use_notification  Int     @default(1) @db.TinyInt
  alternative_email String? @db.VarChar(255)

  @@unique([tickets_id, type, suppliers_id], map: "unicity")
  @@index([suppliers_id, type], map: "group")
}

model glpi_suppliertypes {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_taskcategories {
  id                        Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id               Int       @default(0) @db.UnsignedInt
  is_recursive              Int       @default(0) @db.TinyInt
  taskcategories_id         Int       @default(0) @db.UnsignedInt
  name                      String?   @db.VarChar(255)
  completename              String?   @db.Text
  comment                   String?   @db.Text
  level                     Int       @default(0)
  ancestors_cache           String?   @db.LongText
  sons_cache                String?   @db.LongText
  is_active                 Int       @default(1) @db.TinyInt
  is_helpdeskvisible        Int       @default(1) @db.TinyInt
  date_mod                  DateTime? @db.Timestamp(0)
  date_creation             DateTime? @db.Timestamp(0)
  knowbaseitemcategories_id Int       @default(0) @db.UnsignedInt

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([is_active], map: "is_active")
  @@index([is_helpdeskvisible], map: "is_helpdeskvisible")
  @@index([is_recursive], map: "is_recursive")
  @@index([knowbaseitemcategories_id], map: "knowbaseitemcategories_id")
  @@index([level], map: "level")
  @@index([name], map: "name")
  @@index([taskcategories_id], map: "taskcategories_id")
}

model glpi_tasktemplates {
  id                Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id       Int       @default(0) @db.UnsignedInt
  is_recursive      Int       @default(0) @db.TinyInt
  name              String?   @db.VarChar(255)
  content           String?   @db.MediumText
  taskcategories_id Int       @default(0) @db.UnsignedInt
  actiontime        Int       @default(0)
  comment           String?   @db.Text
  date_mod          DateTime? @db.Timestamp(0)
  date_creation     DateTime? @db.Timestamp(0)
  state             Int       @default(0)
  is_private        Int       @default(0) @db.TinyInt
  users_id_tech     Int       @default(0) @db.UnsignedInt
  groups_id_tech    Int       @default(0) @db.UnsignedInt

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([groups_id_tech], map: "groups_id_tech")
  @@index([is_private], map: "is_private")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
  @@index([taskcategories_id], map: "taskcategories_id")
  @@index([users_id_tech], map: "users_id_tech")
}

model glpi_ticketcosts {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  tickets_id    Int       @default(0) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  begin_date    DateTime? @db.Date
  end_date      DateTime? @db.Date
  actiontime    Int       @default(0)
  cost_time     Decimal   @default(0.0000) @db.Decimal(20, 4)
  cost_fixed    Decimal   @default(0.0000) @db.Decimal(20, 4)
  cost_material Decimal   @default(0.0000) @db.Decimal(20, 4)
  budgets_id    Int       @default(0) @db.UnsignedInt
  entities_id   Int       @default(0) @db.UnsignedInt

  @@index([begin_date], map: "begin_date")
  @@index([budgets_id], map: "budgets_id")
  @@index([end_date], map: "end_date")
  @@index([entities_id], map: "entities_id")
  @@index([name], map: "name")
  @@index([tickets_id], map: "tickets_id")
}

model glpi_ticketrecurrents {
  id                 Int       @id @default(autoincrement()) @db.UnsignedInt
  name               String?   @db.VarChar(255)
  comment            String?   @db.Text
  entities_id        Int       @default(0) @db.UnsignedInt
  is_recursive       Int       @default(0) @db.TinyInt
  is_active          Int       @default(0) @db.TinyInt
  tickettemplates_id Int       @default(0) @db.UnsignedInt
  begin_date         DateTime? @db.Timestamp(0)
  periodicity        String?   @db.VarChar(255)
  create_before      Int       @default(0)
  next_creation_date DateTime? @db.Timestamp(0)
  calendars_id       Int       @default(0) @db.UnsignedInt
  end_date           DateTime? @db.Timestamp(0)

  @@index([calendars_id], map: "calendars_id")
  @@index([entities_id], map: "entities_id")
  @@index([is_active], map: "is_active")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
  @@index([next_creation_date], map: "next_creation_date")
  @@index([tickettemplates_id], map: "tickettemplates_id")
}

model glpi_tickets {
  id                         Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id                Int       @default(0) @db.UnsignedInt
  name                       String?   @db.VarChar(255)
  date                       DateTime? @db.Timestamp(0)
  closedate                  DateTime? @db.Timestamp(0)
  solvedate                  DateTime? @db.Timestamp(0)
  takeintoaccountdate        DateTime? @db.Timestamp(0)
  date_mod                   DateTime? @db.Timestamp(0)
  users_id_lastupdater       Int       @default(0) @db.UnsignedInt
  status                     Int       @default(1)
  users_id_recipient         Int       @default(0) @db.UnsignedInt
  requesttypes_id            Int       @default(0) @db.UnsignedInt
  content                    String?   @db.LongText
  urgency                    Int       @default(1)
  impact                     Int       @default(1)
  priority                   Int       @default(1)
  itilcategories_id          Int       @default(0) @db.UnsignedInt
  type                       Int       @default(1)
  global_validation          Int       @default(1)
  slas_id_ttr                Int       @default(0) @db.UnsignedInt
  slas_id_tto                Int       @default(0) @db.UnsignedInt
  slalevels_id_ttr           Int       @default(0) @db.UnsignedInt
  time_to_resolve            DateTime? @db.Timestamp(0)
  time_to_own                DateTime? @db.Timestamp(0)
  begin_waiting_date         DateTime? @db.Timestamp(0)
  sla_waiting_duration       Int       @default(0)
  ola_waiting_duration       Int       @default(0)
  olas_id_tto                Int       @default(0) @db.UnsignedInt
  olas_id_ttr                Int       @default(0) @db.UnsignedInt
  olalevels_id_ttr           Int       @default(0) @db.UnsignedInt
  ola_tto_begin_date         DateTime? @db.Timestamp(0)
  ola_ttr_begin_date         DateTime? @db.Timestamp(0)
  internal_time_to_resolve   DateTime? @db.Timestamp(0)
  internal_time_to_own       DateTime? @db.Timestamp(0)
  waiting_duration           Int       @default(0)
  close_delay_stat           Int       @default(0)
  solve_delay_stat           Int       @default(0)
  takeintoaccount_delay_stat Int       @default(0)
  actiontime                 Int       @default(0)
  is_deleted                 Int       @default(0) @db.TinyInt
  locations_id               Int       @default(0) @db.UnsignedInt
  validation_percent         Int       @default(0)
  date_creation              DateTime? @db.Timestamp(0)

  @@index([closedate], map: "closedate")
  @@index([date], map: "date")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([global_validation], map: "global_validation")
  @@index([impact], map: "impact")
  @@index([internal_time_to_own], map: "internal_time_to_own")
  @@index([internal_time_to_resolve], map: "internal_time_to_resolve")
  @@index([is_deleted], map: "is_deleted")
  @@index([itilcategories_id], map: "itilcategories_id")
  @@index([locations_id], map: "locations_id")
  @@index([name], map: "name")
  @@index([ola_waiting_duration], map: "ola_waiting_duration")
  @@index([olalevels_id_ttr], map: "olalevels_id_ttr")
  @@index([olas_id_tto], map: "olas_id_tto")
  @@index([olas_id_ttr], map: "olas_id_ttr")
  @@index([priority], map: "priority")
  @@index([requesttypes_id], map: "request_type")
  @@index([slalevels_id_ttr], map: "slalevels_id_ttr")
  @@index([slas_id_tto], map: "slas_id_tto")
  @@index([slas_id_ttr], map: "slas_id_ttr")
  @@index([solvedate], map: "solvedate")
  @@index([status], map: "status")
  @@index([takeintoaccountdate], map: "takeintoaccountdate")
  @@index([time_to_own], map: "time_to_own")
  @@index([time_to_resolve], map: "time_to_resolve")
  @@index([type], map: "type")
  @@index([urgency], map: "urgency")
  @@index([users_id_lastupdater], map: "users_id_lastupdater")
  @@index([users_id_recipient], map: "users_id_recipient")
}

model glpi_tickets_contracts {
  id           Int @id @default(autoincrement()) @db.UnsignedInt
  tickets_id   Int @default(0) @db.UnsignedInt
  contracts_id Int @default(0) @db.UnsignedInt

  @@unique([tickets_id, contracts_id], map: "unicity")
  @@index([contracts_id], map: "contracts_id")
}

model glpi_tickets_tickets {
  id           Int @id @default(autoincrement()) @db.UnsignedInt
  tickets_id_1 Int @default(0) @db.UnsignedInt
  tickets_id_2 Int @default(0) @db.UnsignedInt
  link         Int @default(1)

  @@unique([tickets_id_1, tickets_id_2], map: "unicity")
  @@index([tickets_id_2], map: "tickets_id_2")
}

model glpi_tickets_users {
  id                Int     @id @default(autoincrement()) @db.UnsignedInt
  tickets_id        Int     @default(0) @db.UnsignedInt
  users_id          Int     @default(0) @db.UnsignedInt
  type              Int     @default(1)
  use_notification  Int     @default(1) @db.TinyInt
  alternative_email String? @db.VarChar(255)

  @@unique([tickets_id, type, users_id, alternative_email], map: "unicity")
  @@index([users_id, type], map: "user")
}

model glpi_ticketsatisfactions {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  tickets_id    Int       @unique(map: "tickets_id") @default(0) @db.UnsignedInt
  type          Int       @default(1)
  date_begin    DateTime? @db.Timestamp(0)
  date_answered DateTime? @db.Timestamp(0)
  satisfaction  Int?
  comment       String?   @db.Text
}

model glpi_tickettasks {
  id                Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid              String?   @unique(map: "uuid") @db.VarChar(255)
  tickets_id        Int       @default(0) @db.UnsignedInt
  taskcategories_id Int       @default(0) @db.UnsignedInt
  date              DateTime? @db.Timestamp(0)
  users_id          Int       @default(0) @db.UnsignedInt
  users_id_editor   Int       @default(0) @db.UnsignedInt
  content           String?   @db.LongText
  is_private        Int       @default(0) @db.TinyInt
  actiontime        Int       @default(0)
  begin             DateTime? @db.Timestamp(0)
  end               DateTime? @db.Timestamp(0)
  state             Int       @default(1)
  users_id_tech     Int       @default(0) @db.UnsignedInt
  groups_id_tech    Int       @default(0) @db.UnsignedInt
  date_mod          DateTime? @db.Timestamp(0)
  date_creation     DateTime? @db.Timestamp(0)
  tasktemplates_id  Int       @default(0) @db.UnsignedInt
  timeline_position Int       @default(0) @db.TinyInt
  sourceitems_id    Int       @default(0) @db.UnsignedInt
  sourceof_items_id Int       @default(0) @db.UnsignedInt

  @@index([begin], map: "begin")
  @@index([date], map: "date")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([end], map: "end")
  @@index([groups_id_tech], map: "groups_id_tech")
  @@index([is_private], map: "is_private")
  @@index([sourceitems_id], map: "sourceitems_id")
  @@index([sourceof_items_id], map: "sourceof_items_id")
  @@index([state], map: "state")
  @@index([taskcategories_id], map: "taskcategories_id")
  @@index([tasktemplates_id], map: "tasktemplates_id")
  @@index([tickets_id], map: "tickets_id")
  @@index([users_id], map: "users_id")
  @@index([users_id_editor], map: "users_id_editor")
  @@index([users_id_tech], map: "users_id_tech")
}

model glpi_tickettemplatehiddenfields {
  id                 Int @id @default(autoincrement()) @db.UnsignedInt
  tickettemplates_id Int @default(0) @db.UnsignedInt
  num                Int @default(0)

  @@unique([tickettemplates_id, num], map: "unicity")
}

model glpi_tickettemplatemandatoryfields {
  id                 Int @id @default(autoincrement()) @db.UnsignedInt
  tickettemplates_id Int @default(0) @db.UnsignedInt
  num                Int @default(0)

  @@unique([tickettemplates_id, num], map: "unicity")
}

model glpi_tickettemplatepredefinedfields {
  id                 Int     @id @default(autoincrement()) @db.UnsignedInt
  tickettemplates_id Int     @default(0) @db.UnsignedInt
  num                Int     @default(0)
  value              String? @db.Text

  @@index([tickettemplates_id], map: "tickettemplates_id")
}

model glpi_tickettemplates {
  id           Int     @id @default(autoincrement()) @db.UnsignedInt
  name         String? @db.VarChar(255)
  entities_id  Int     @default(0) @db.UnsignedInt
  is_recursive Int     @default(0) @db.TinyInt
  comment      String? @db.Text

  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
}

model glpi_ticketvalidations {
  id                 Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id        Int       @default(0) @db.UnsignedInt
  users_id           Int       @default(0) @db.UnsignedInt
  tickets_id         Int       @default(0) @db.UnsignedInt
  users_id_validate  Int       @default(0) @db.UnsignedInt
  comment_submission String?   @db.Text
  comment_validation String?   @db.Text
  status             Int       @default(2)
  submission_date    DateTime? @db.Timestamp(0)
  validation_date    DateTime? @db.Timestamp(0)
  timeline_position  Int       @default(0) @db.TinyInt

  @@index([entities_id], map: "entities_id")
  @@index([status], map: "status")
  @@index([submission_date], map: "submission_date")
  @@index([tickets_id], map: "tickets_id")
  @@index([users_id], map: "users_id")
  @@index([users_id_validate], map: "users_id_validate")
  @@index([validation_date], map: "validation_date")
}

model glpi_transfers {
  id                  Int       @id @default(autoincrement()) @db.UnsignedInt
  name                String?   @db.VarChar(255)
  keep_ticket         Int       @default(0)
  keep_networklink    Int       @default(0)
  keep_reservation    Int       @default(0)
  keep_history        Int       @default(0)
  keep_device         Int       @default(0)
  keep_infocom        Int       @default(0)
  keep_dc_monitor     Int       @default(0)
  clean_dc_monitor    Int       @default(0)
  keep_dc_phone       Int       @default(0)
  clean_dc_phone      Int       @default(0)
  keep_dc_peripheral  Int       @default(0)
  clean_dc_peripheral Int       @default(0)
  keep_dc_printer     Int       @default(0)
  clean_dc_printer    Int       @default(0)
  keep_supplier       Int       @default(0)
  clean_supplier      Int       @default(0)
  keep_contact        Int       @default(0)
  clean_contact       Int       @default(0)
  keep_contract       Int       @default(0)
  clean_contract      Int       @default(0)
  keep_software       Int       @default(0)
  clean_software      Int       @default(0)
  keep_document       Int       @default(0)
  clean_document      Int       @default(0)
  keep_cartridgeitem  Int       @default(0)
  clean_cartridgeitem Int       @default(0)
  keep_cartridge      Int       @default(0)
  keep_consumable     Int       @default(0)
  date_mod            DateTime? @db.Timestamp(0)
  date_creation       DateTime? @db.Timestamp(0)
  comment             String?   @db.Text
  keep_disk           Int       @default(0)
  keep_certificate    Int       @default(0)
  clean_certificate   Int       @default(0)
  lock_updated_fields Int       @default(0)
  keep_location       Int       @default(1)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_unmanageds {
  id                    Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id           Int       @default(0) @db.UnsignedInt
  is_recursive          Int       @default(0) @db.TinyInt
  name                  String?   @db.VarChar(255)
  serial                String?   @db.VarChar(255)
  otherserial           String?   @db.VarChar(255)
  contact               String?   @db.VarChar(255)
  contact_num           String?   @db.VarChar(255)
  date_mod              DateTime? @db.Timestamp(0)
  comment               String?   @db.Text
  locations_id          Int       @default(0) @db.UnsignedInt
  networks_id           Int       @default(0) @db.UnsignedInt
  manufacturers_id      Int       @default(0) @db.UnsignedInt
  is_deleted            Int       @default(0) @db.TinyInt
  users_id              Int       @default(0) @db.UnsignedInt
  groups_id             Int       @default(0) @db.UnsignedInt
  states_id             Int       @default(0) @db.UnsignedInt
  users_id_tech         Int       @default(0) @db.UnsignedInt
  groups_id_tech        Int       @default(0) @db.UnsignedInt
  is_dynamic            Int       @default(0) @db.TinyInt
  date_creation         DateTime? @db.Timestamp(0)
  autoupdatesystems_id  Int       @default(0) @db.UnsignedInt
  sysdescr              String?   @db.Text
  agents_id             Int       @default(0) @db.UnsignedInt
  itemtype              String?   @db.VarChar(100)
  accepted              Int       @default(0) @db.TinyInt
  hub                   Int       @default(0) @db.TinyInt
  ip                    String?   @db.VarChar(255)
  snmpcredentials_id    Int       @default(0) @db.UnsignedInt
  last_inventory_update DateTime? @db.Timestamp(0)

  @@index([agents_id], map: "agents_id")
  @@index([autoupdatesystems_id], map: "autoupdatesystems_id")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([groups_id], map: "groups_id")
  @@index([groups_id_tech], map: "groups_id_tech")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_dynamic], map: "is_dynamic")
  @@index([is_recursive], map: "is_recursive")
  @@index([locations_id], map: "locations_id")
  @@index([manufacturers_id], map: "manufacturers_id")
  @@index([name], map: "name")
  @@index([networks_id], map: "networks_id")
  @@index([otherserial], map: "otherserial")
  @@index([serial], map: "serial")
  @@index([snmpcredentials_id], map: "snmpcredentials_id")
  @@index([states_id], map: "states_id")
  @@index([users_id], map: "users_id")
  @@index([users_id_tech], map: "users_id_tech")
}

model glpi_usbvendors {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id   Int       @default(0) @db.UnsignedInt
  is_recursive  Int       @default(0) @db.TinyInt
  vendorid      String    @db.VarChar(4)
  deviceid      String?   @db.VarChar(4)
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_creation DateTime? @db.Timestamp(0)
  date_mod      DateTime? @db.Timestamp(0)

  @@unique([vendorid, deviceid], map: "unicity")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([deviceid], map: "deviceid")
  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
}

model glpi_usercategories {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_useremails {
  id         Int     @id @default(autoincrement()) @db.UnsignedInt
  users_id   Int     @default(0) @db.UnsignedInt
  is_default Int     @default(0) @db.TinyInt
  is_dynamic Int     @default(0) @db.TinyInt
  email      String? @db.VarChar(255)

  @@unique([users_id, email], map: "unicity")
  @@index([email], map: "email")
  @@index([is_default], map: "is_default")
  @@index([is_dynamic], map: "is_dynamic")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_users {
  id                                     Int       @id @default(autoincrement()) @db.UnsignedInt
  name                                   String?   @db.VarChar(255)
  password                               String?   @db.VarChar(255)
  password_last_update                   DateTime? @db.Timestamp(0)
  phone                                  String?   @db.VarChar(255)
  phone2                                 String?   @db.VarChar(255)
  mobile                                 String?   @db.VarChar(255)
  realname                               String?   @db.VarChar(255)
  firstname                              String?   @db.VarChar(255)
  locations_id                           Int       @default(0) @db.UnsignedInt
  language                               String?   @db.Char(10)
  use_mode                               Int       @default(0)
  list_limit                             Int?
  is_active                              Int       @default(1) @db.TinyInt
  comment                                String?   @db.Text
  auths_id                               Int       @default(0) @db.UnsignedInt
  authtype                               Int       @default(0)
  last_login                             DateTime? @db.Timestamp(0)
  date_mod                               DateTime? @db.Timestamp(0)
  date_sync                              DateTime? @db.Timestamp(0)
  is_deleted                             Int       @default(0) @db.TinyInt
  profiles_id                            Int       @default(0) @db.UnsignedInt
  entities_id                            Int?      @default(0) @db.UnsignedInt
  usertitles_id                          Int       @default(0) @db.UnsignedInt
  usercategories_id                      Int       @default(0) @db.UnsignedInt
  date_format                            Int?
  number_format                          Int?
  names_format                           Int?
  csv_delimiter                          String?   @db.Char(1)
  is_ids_visible                         Int?      @db.TinyInt
  use_flat_dropdowntree                  Int?      @db.TinyInt
  use_flat_dropdowntree_on_search_result Int?      @db.TinyInt
  show_jobs_at_login                     Int?      @db.TinyInt
  priority_1                             String?   @db.Char(20)
  priority_2                             String?   @db.Char(20)
  priority_3                             String?   @db.Char(20)
  priority_4                             String?   @db.Char(20)
  priority_5                             String?   @db.Char(20)
  priority_6                             String?   @db.Char(20)
  followup_private                       Int?      @db.TinyInt
  task_private                           Int?      @db.TinyInt
  default_requesttypes_id                Int?      @db.UnsignedInt
  password_forget_token                  String?   @db.Char(40)
  password_forget_token_date             DateTime? @db.Timestamp(0)
  user_dn                                String?   @db.Text
  user_dn_hash                           String?   @db.VarChar(32)
  registration_number                    String?   @db.VarChar(255)
  show_count_on_tabs                     Int?      @db.TinyInt
  refresh_views                          Int?
  set_default_tech                       Int?      @db.TinyInt
  personal_token                         String?   @db.VarChar(255)
  personal_token_date                    DateTime? @db.Timestamp(0)
  api_token                              String?   @db.VarChar(255)
  api_token_date                         DateTime? @db.Timestamp(0)
  cookie_token                           String?   @db.VarChar(255)
  cookie_token_date                      DateTime? @db.Timestamp(0)
  display_count_on_home                  Int?
  notification_to_myself                 Int?      @db.TinyInt
  duedateok_color                        String?   @db.VarChar(255)
  duedatewarning_color                   String?   @db.VarChar(255)
  duedatecritical_color                  String?   @db.VarChar(255)
  duedatewarning_less                    Int?
  duedatecritical_less                   Int?
  duedatewarning_unit                    String?   @db.VarChar(255)
  duedatecritical_unit                   String?   @db.VarChar(255)
  display_options                        String?   @db.Text
  is_deleted_ldap                        Int       @default(0) @db.TinyInt
  pdffont                                String?   @db.VarChar(255)
  picture                                String?   @db.VarChar(255)
  begin_date                             DateTime? @db.Timestamp(0)
  end_date                               DateTime? @db.Timestamp(0)
  keep_devices_when_purging_item         Int?      @db.TinyInt
  privatebookmarkorder                   String?   @db.LongText
  backcreated                            Int?      @db.TinyInt
  task_state                             Int?
  palette                                String?   @db.Char(20)
  page_layout                            String?   @db.Char(20)
  fold_menu                              Int?      @db.TinyInt
  fold_search                            Int?      @db.TinyInt
  savedsearches_pinned                   String?   @db.Text
  timeline_order                         String?   @db.Char(20)
  itil_layout                            String?   @db.Text
  richtext_layout                        String?   @db.Char(20)
  set_default_requester                  Int?      @db.TinyInt
  lock_autolock_mode                     Int?      @db.TinyInt
  lock_directunlock_notification         Int?      @db.TinyInt
  date_creation                          DateTime? @db.Timestamp(0)
  highcontrast_css                       Int?      @default(0) @db.TinyInt
  plannings                              String?   @db.Text
  sync_field                             String?   @db.VarChar(255)
  groups_id                              Int       @default(0) @db.UnsignedInt
  users_id_supervisor                    Int       @default(0) @db.UnsignedInt
  timezone                               String?   @db.VarChar(50)
  default_dashboard_central              String?   @db.VarChar(100)
  default_dashboard_assets               String?   @db.VarChar(100)
  default_dashboard_helpdesk             String?   @db.VarChar(100)
  default_dashboard_mini_ticket          String?   @db.VarChar(100)
  default_central_tab                    Int?      @default(0) @db.TinyInt
  nickname                               String?   @db.VarChar(255)
  timeline_action_btn_layout             Int?      @db.TinyInt
  timeline_date_format                   Int?      @db.TinyInt

  @@unique([name, authtype, auths_id], map: "unicityloginauth")
  @@index([authtype, auths_id], map: "authitem")
  @@index([auths_id], map: "auths_id")
  @@index([begin_date], map: "begin_date")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([default_requesttypes_id], map: "default_requesttypes_id")
  @@index([end_date], map: "end_date")
  @@index([entities_id], map: "entities_id")
  @@index([firstname], map: "firstname")
  @@index([groups_id], map: "groups_id")
  @@index([is_active], map: "is_active")
  @@index([is_deleted], map: "is_deleted")
  @@index([is_deleted_ldap], map: "is_deleted_ldap")
  @@index([locations_id], map: "locations_id")
  @@index([profiles_id], map: "profiles_id")
  @@index([realname], map: "realname")
  @@index([sync_field], map: "sync_field")
  @@index([user_dn_hash], map: "user_dn_hash")
  @@index([usercategories_id], map: "usercategories_id")
  @@index([users_id_supervisor], map: "users_id_supervisor")
  @@index([usertitles_id], map: "usertitles_id")
}

model glpi_usertitles {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_virtualmachinestates {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String    @default("") @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_virtualmachinesystems {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String    @default("") @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_virtualmachinetypes {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  name          String    @default("") @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([name], map: "name")
}

model glpi_vlans {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id   Int       @default(0) @db.UnsignedInt
  is_recursive  Int       @default(0) @db.TinyInt
  name          String?   @db.VarChar(255)
  comment       String?   @db.Text
  tag           Int       @default(0)
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
  @@index([tag], map: "tag")
}

model glpi_vobjects {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  itemtype      String?   @db.VarChar(100)
  items_id      Int       @default(0) @db.UnsignedInt
  data          String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@unique([itemtype, items_id], map: "unicity")
  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([itemtype, items_id], map: "item")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model glpi_wifinetworks {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  entities_id   Int       @default(0) @db.UnsignedInt
  is_recursive  Int       @default(0) @db.TinyInt
  name          String?   @db.VarChar(255)
  essid         String?   @db.VarChar(255)
  mode          String?   @db.VarChar(255)
  comment       String?   @db.Text
  date_mod      DateTime? @db.Timestamp(0)
  date_creation DateTime? @db.Timestamp(0)

  @@index([date_creation], map: "date_creation")
  @@index([date_mod], map: "date_mod")
  @@index([entities_id], map: "entities_id")
  @@index([essid], map: "essid")
  @@index([is_recursive], map: "is_recursive")
  @@index([name], map: "name")
}
